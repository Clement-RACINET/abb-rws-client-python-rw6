pages:
  image: ghcr.io/prefix-dev/pixi:latest

  cache:
    key:
      files:
        - pixi.lock
    paths:
      - .pixi

  script:
    - pixi run -e dev docs

  artifacts:
    paths:
      - public

  only:
    - main
