pages:
  image: ghcr.io/prefix-dev/pixi:latest

  before_script:
    - apt-get update
    - apt-get install -y git

  cache:
    key:
      files:
        - pixi.lock
    paths:
      - .pixi

  script:
    - pixi run -e dev docs

  artifacts:
    paths:
      - public

  only:
    - main
