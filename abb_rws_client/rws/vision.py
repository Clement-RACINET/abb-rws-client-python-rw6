# This file is AUTO-GENERATED by utils/generator/main.py
# DO NOT EDIT MANUALLY — run the generator to regenerate.
#  Generator author: Clement RACINET
"""
RWS module: RobotWare Services → Integrated Vision (IV) Service → Get Vision Manager Resource

1:1 mirror of the ABB RobotWare 6 REST API.
Each function maps to exactly one HTTP endpoint.
No composed logic — see highlevel/ for wrappers.
"""
from __future__ import annotations

import httpx

from abb_rws_client.core.client import RWSClient


async def get_vision_manager_resource(client: RWSClient) -> httpx.Response:
    """
    Get Vision Manager Resource.

    Route: ``GET /rw/vision``
    ABB constraints: Not supported in bootserver mode

    Args:
        client: Open RWSClient instance.

    Returns:
        Raw HTTP response. Expected success: HTTP 200.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: NOT_FOUND(404)

    Example:
        ```python
        >>> await get_vision_manager_resource(client)
        <Response [200]>
        ```
    """
    return await client.get("/rw/vision")

async def get_number_of_cameras_of_iv(
    client: RWSClient,
    resource: str | None = None,
) -> httpx.Response:
    """
    Get Number of Cameras of IV.

    Route: ``GET /rw/vision``
    ABB constraints: Not supported in bootserver mode

    Args:
        client: Open RWSClient instance.
        resource: Query parameter. Optional.

    Returns:
        Raw HTTP response. Expected success: HTTP 200.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: BAD_REQUEST(400)

    Example:
        ```python
        >>> await get_number_of_cameras_of_iv(client)
        <Response [200]>
        ```
    """
    return await client.get(
        "/rw/vision",
        params={k: v for k, v in {"resource": resource}.items() if v is not None},
    )

async def get_iv_camera_validity(
    client: RWSClient,
    resource: str | None = None,
    name: str | None = None,
) -> httpx.Response:
    """
    Get IV Camera Validity.

    Route: ``GET /rw/vision``
    ABB constraints: Not supported in bootserver mode

    Args:
        client: Open RWSClient instance.
        resource: Query parameter. Optional.
        name: Query parameter. Optional.

    Returns:
        Raw HTTP response. Expected success: HTTP 200.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: BAD_REQUEST(400)

    Example:
        ```python
        >>> await get_iv_camera_validity(client)
        <Response [200]>
        ```
    """
    return await client.get(
        "/rw/vision",
        params={k: v for k, v in {"resource": resource, "name": name}.items() if v is not None},
    )

async def get_vision_camera_resource_actions(
    client: RWSClient,
    action: str | None = None,
) -> httpx.Response:
    """
    Get Vision (camera) Resource Actions.

    Route: ``GET /rw/vision``
    ABB constraints: Not supported in bootserver mode

    Args:
        client: Open RWSClient instance.
        action: Query parameter. Optional.

    Returns:
        Raw HTTP response. Expected success: HTTP 200.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: BAD_REQUEST(400)

    Example:
        ```python
        >>> await get_vision_camera_resource_actions(client)
        <Response [200]>
        ```
    """
    return await client.get(
        "/rw/vision",
        params={k: v for k, v in {"action": action}.items() if v is not None},
    )

async def get_camera_jobname(
    client: RWSClient,
    resource: str | None = None,
    name: str | None = None,
) -> httpx.Response:
    """
    Get Camera Jobname.

    Route: ``GET /rw/vision``
    ABB constraints: Not supported in bootserver mode

    Args:
        client: Open RWSClient instance.
        resource: Query parameter. Optional.
        name: Query parameter. Optional.

    Returns:
        Raw HTTP response. Expected success: HTTP 200.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: BAD_REQUEST(400)

    Example:
        ```python
        >>> await get_camera_jobname(client)
        <Response [200]>
        ```
    """
    return await client.get(
        "/rw/vision",
        params={k: v for k, v in {"resource": resource, "name": name}.items() if v is not None},
    )

async def restart_camera(
    client: RWSClient,
    action: str | None = None,
    name: str | None = None,
) -> httpx.Response:
    """
    Restart Camera.

    Route: ``POST /rw/vision``
    ABB constraints: Not supported in bootserver mode

    Args:
        client: Open RWSClient instance.
        action: Query parameter. Optional.
        name: Body parameter. Optional.

    Returns:
        Raw HTTP response. Expected success: HTTP 204.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: BAD_REQUEST(400)

    Example:
        ```python
        >>> await restart_camera(client)
        <Response [204]>
        ```
    """
    return await client.post(
        "/rw/vision",
        params={k: v for k, v in {"action": action}.items() if v is not None},
        data={k: v for k, v in {"name": name}.items() if v is not None},
    )

async def post_flash_led_of_camera(
    client: RWSClient,
    action: str | None = None,
    name: str | None = None,
) -> httpx.Response:
    """
    Flash LED(s) of Camera.

    Route: ``POST /rw/vision``
    ABB constraints: Not supported in bootserver mode

    Args:
        client: Open RWSClient instance.
        action: Query parameter. Optional.
        name: Body parameter. Optional.

    Returns:
        Raw HTTP response. Expected success: HTTP 204.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: BAD_REQUEST(400)

    Example:
        ```python
        >>> await post_flash_led_of_camera(client)
        <Response [204]>
        ```
    """
    return await client.post(
        "/rw/vision",
        params={k: v for k, v in {"action": action}.items() if v is not None},
        data={k: v for k, v in {"name": name}.items() if v is not None},
    )

async def set_camera_state(
    client: RWSClient,
    action: str | None = None,
    name: str | None = None,
    state: str | None = None,
) -> httpx.Response:
    """
    Set Camera State.

    Route: ``POST /rw/vision``
    ABB constraints: Not supported in bootserver mode

    Args:
        client: Open RWSClient instance.
        action: Query parameter. Optional.
        name: Body parameter. Optional.
        state: Body parameter. Optional.

    Returns:
        Raw HTTP response. Expected success: HTTP 204.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: BAD_REQUEST(400)

    Example:
        ```python
        >>> await set_camera_state(client)
        <Response [204]>
        ```
    """
    return await client.post(
        "/rw/vision",
        params={k: v for k, v in {"action": action}.items() if v is not None},
        data={k: v for k, v in {"name": name, "state": state}.items() if v is not None},
    )

async def post_refesh_the_camera(
    client: RWSClient,
    action: str | None = None,
) -> httpx.Response:
    """
    Refesh the camera(s).

    Route: ``POST /rw/vision``
    ABB constraints: Not supported in bootserver mode

    Args:
        client: Open RWSClient instance.
        action: Query parameter. Optional.

    Returns:
        Raw HTTP response. Expected success: HTTP 204.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: BAD_REQUEST(400)

    Example:
        ```python
        >>> await post_refesh_the_camera(client)
        <Response [204]>
        ```
    """
    return await client.post(
        "/rw/vision",
        params={k: v for k, v in {"action": action}.items() if v is not None},
        data={},
    )

async def set_hostname_of_the_camera(
    client: RWSClient,
    action: str | None = None,
    name: str | None = None,
    host: str | None = None,
) -> httpx.Response:
    """
    Set Hostname of the Camera.

    Route: ``POST /rw/vision``
    ABB constraints: A restart for the camera module is needed for the change to be visible.
        Switch Off and switch On the camera modeule. Not supported in bootserver mode

    Args:
        client: Open RWSClient instance.
        action: Query parameter. Optional.
        name: Body parameter. Optional.
        host: Body parameter. Optional.

    Returns:
        Raw HTTP response. Expected success: HTTP 204.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: BAD_REQUEST(400)

    Example:
        ```python
        >>> await set_hostname_of_the_camera(client)
        <Response [204]>
        ```
    """
    return await client.post(
        "/rw/vision",
        params={k: v for k, v in {"action": action}.items() if v is not None},
        data={k: v for k, v in {"name": name, "host": host}.items() if v is not None},
    )

async def set_camera_to_be_dhcp_client(
    client: RWSClient,
    action: str | None = None,
    name: str | None = None,
) -> httpx.Response:
    """
    Set camera to be a DHCP client.

    Route: ``POST /rw/vision``
    ABB constraints: Not supported in bootserver mode Camera restart is required after DHCP
        settings

    Args:
        client: Open RWSClient instance.
        action: Query parameter. Optional.
        name: Body parameter. Optional.

    Returns:
        Raw HTTP response. Expected success: HTTP 204.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: BAD_REQUEST(400),FORBIDDEN(403),NOT_FOUND(404)

    Example:
        ```python
        >>> await set_camera_to_be_dhcp_client(client)
        <Response [204]>
        ```
    """
    return await client.post(
        "/rw/vision",
        params={k: v for k, v in {"action": action}.items() if v is not None},
        data={k: v for k, v in {"name": name}.items() if v is not None},
    )

async def set_camera_dns_settings(
    client: RWSClient,
    action: str | None = None,
    name: str | None = None,
    dns_server: str | None = None,
    dns_suffix: str | None = None,
) -> httpx.Response:
    """
    Set Camera DNS Settings.

    Route: ``POST /rw/vision``
    ABB constraints: Not supported in bootserver mode Camera restart is required after DNS
        settings

    Args:
        client: Open RWSClient instance.
        action: Query parameter. Optional.
        name: Body parameter. Optional.
        dns_server: Body parameter. Optional.
        dns_suffix: Body parameter. Optional.

    Returns:
        Raw HTTP response. Expected success: HTTP 204.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: BAD_REQUEST(400),FORBIDDEN(403),NOT_FOUND(404)

    Example:
        ```python
        >>> await set_camera_dns_settings(client)
        <Response [204]>
        ```
    """
    return await client.post(
        "/rw/vision",
        params={k: v for k, v in {"action": action}.items() if v is not None},
        data={
            k: v
            for k, v in {
                "name": name,
                "dns_server": dns_server,
                "dns_suffix": dns_suffix
            }.items()
            if v is not None
        },
    )

async def get_camera_status(
    client: RWSClient,
    resource: str | None = None,
    name: str | None = None,
) -> httpx.Response:
    """
    Get Camera Status.

    Route: ``GET /rw/vision``
    ABB constraints: Not supported in bootserver mode

    Args:
        client: Open RWSClient instance.
        resource: Query parameter. Optional.
        name: Query parameter. Optional.

    Returns:
        Raw HTTP response. Expected success: HTTP 200.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: BAD_REQUEST(400)

    Example:
        ```python
        >>> await get_camera_status(client)
        <Response [200]>
        ```
    """
    return await client.get(
        "/rw/vision",
        params={k: v for k, v in {"resource": resource, "name": name}.items() if v is not None},
    )

async def get_camera_info_using_index_of_the(
    client: RWSClient,
    resource: str | None = None,
    index: str | None = None,
) -> httpx.Response:
    """
    Get Camera Info Using Index of the camera.

    Route: ``GET /rw/vision``
    ABB constraints: Not supported in bootserver mode

    Args:
        client: Open RWSClient instance.
        resource: Query parameter. Optional.
        index: Query parameter. Optional.

    Returns:
        Raw HTTP response. Expected success: HTTP 200.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: BAD_REQUEST(400)

    Example:
        ```python
        >>> await get_camera_info_using_index_of_the(client)
        <Response [200]>
        ```
    """
    return await client.get(
        "/rw/vision",
        params={
            k: v
            for k, v in {
                "resource": resource,
                "index": index
            }.items()
            if v is not None
        },
    )

async def set_camera_name(
    client: RWSClient,
    action: str | None = None,
    index: str | None = None,
    name: str | None = None,
) -> httpx.Response:
    """
    Set Camera Name.

    Route: ``POST /rw/vision``
    ABB constraints: Not supported in bootserver mode Restart the controller to aplly changes

    Args:
        client: Open RWSClient instance.
        action: Query parameter. Optional.
        index: Body parameter. Optional.
        name: Body parameter. Optional.

    Returns:
        Raw HTTP response. Expected success: HTTP 204.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: BAD_REQUEST(400)

    Example:
        ```python
        >>> await set_camera_name(client)
        <Response [204]>
        ```
    """
    return await client.post(
        "/rw/vision",
        params={k: v for k, v in {"action": action}.items() if v is not None},
        data={k: v for k, v in {"index": index, "name": name}.items() if v is not None},
    )

async def set_camera_user_credentials(
    client: RWSClient,
    action: str | None = None,
    index: str | None = None,
    user: str | None = None,
    password: str | None = None,
) -> httpx.Response:
    """
    Set Camera User Credentials.

    Route: ``POST /rw/vision``
    ABB constraints: Not supported in bootserver mode Restart is required to be activated

    Args:
        client: Open RWSClient instance.
        action: Query parameter. Optional.
        index: Body parameter. Optional.
        user: Body parameter. Optional.
        password: Body parameter. Optional.

    Returns:
        Raw HTTP response. Expected success: HTTP 204.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: BAD_REQUEST(400)

    Example:
        ```python
        >>> await set_camera_user_credentials(client)
        <Response [204]>
        ```
    """
    return await client.post(
        "/rw/vision",
        params={k: v for k, v in {"action": action}.items() if v is not None},
        data={
            k: v
            for k, v in {
                "index": index,
                "user": user,
                "password": password
            }.items()
            if v is not None
        },
    )

async def set_camera_ip_settings(
    client: RWSClient,
    action: str | None = None,
    name: str | None = None,
    address: str | None = None,
    netmask: str | None = None,
    gateway: str | None = None,
) -> httpx.Response:
    """
    Set Camera IP Settings.

    Route: ``POST /rw/vision``
    ABB constraints: Not supported in bootserver mode. Restart the controller to apply IP
        settings.

    Args:
        client: Open RWSClient instance.
        action: Query parameter. Optional.
        name: Body parameter. Optional.
        address: Body parameter. Optional.
        netmask: Body parameter. Optional.
        gateway: Body parameter. Optional.

    Returns:
        Raw HTTP response. Expected success: HTTP 204.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: BAD_REQUEST(400),FORBIDDEN(403),NOT_FOUND(404)

    Example:
        ```python
        >>> await set_camera_ip_settings(client)
        <Response [204]>
        ```
    """
    return await client.post(
        "/rw/vision",
        params={k: v for k, v in {"action": action}.items() if v is not None},
        data={
            k: v
            for k, v in {
                "name": name,
                "address": address,
                "netmask": netmask,
                "gateway": gateway
            }.items()
            if v is not None
        },
    )

async def get_iv_camera_info(
    client: RWSClient,
    resource: str | None = None,
    name: str | None = None,
) -> httpx.Response:
    """
    Get IV Camera Info.

    Route: ``GET /rw/vision``
    ABB constraints: Not supported in bootserver mode

    Args:
        client: Open RWSClient instance.
        resource: Query parameter. Optional.
        name: Query parameter. Optional.

    Returns:
        Raw HTTP response. Expected success: HTTP 200.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: BAD_REQUEST(400)

    Example:
        ```python
        >>> await get_iv_camera_info(client)
        <Response [200]>
        ```
    """
    return await client.get(
        "/rw/vision",
        params={k: v for k, v in {"resource": resource, "name": name}.items() if v is not None},
    )
