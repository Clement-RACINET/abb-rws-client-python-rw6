# This file is AUTO-GENERATED by contrib/generator/main.py
# DO NOT EDIT MANUALLY — run the generator to regenerate.
#  Generator author: Clément RACINET
"""
RWS module: RobotWare Services → Elog service → Get Elog Resources

1:1 mirror of the ABB RobotWare 6 REST API.
Each function maps to exactly one HTTP endpoint.
No composed logic — see highlevel/ for wrappers.
"""
from __future__ import annotations

import httpx

from abb_rws_client._core.client import RWSClient


async def get_elog_resources(
    client: RWSClient,
    lang: str | None = None,
    example_lang: str | None = None,
    resource: str | None = None,
    example_resource: str | None = None,
) -> httpx.Response:
    """
    Get Elog Resources.

    Route: ``GET /rw/elog``
    ABB constraints: Not supported in bootserver mode.

    Args:
        client: Open RWSClient instance.
        lang: Query parameter. Optional.
        example_lang: Query parameter. Optional.
        resource: Query parameter. Optional.
        example_resource: Query parameter. Optional.

    Returns:
        Raw HTTP response. Expected success: HTTP 200.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.

    Example:
        # curl --digest -u "Default User":robotics "http://localhost/rw/elog"
    """
    return await client.get("/rw/elog", params={k: v for k, v in {"lang": lang, "example_lang": example_lang, "resource": resource, "example_resource": example_resource}.items() if v is not None})

async def get_elog_actions(
    client: RWSClient,
    action: str | None = None,
) -> httpx.Response:
    """
    Get Elog Actions.

    Route: ``GET /rw/elog``
    ABB constraints: Not supported in bootserver mode

    Args:
        client: Open RWSClient instance.
        action: Query parameter. Optional.

    Returns:
        Raw HTTP response. Expected success: HTTP 200.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.

    Example:
        # curl --digest -u "Default User":robotics "http://localhost/rw/elog?action=show"
    """
    return await client.get("/rw/elog", params={k: v for k, v in {"action": action}.items() if v is not None})

async def post_clear_elog_messages(
    client: RWSClient,
    action: str | None = None,
) -> httpx.Response:
    """
    Clear elog messages.

    Route: ``POST /rw/elog``
    ABB constraints: Not supported in bootserver mode

    Args:
        client: Open RWSClient instance.
        action: Query parameter. Optional.

    Returns:
        Raw HTTP response. Expected success: HTTP 204.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: UNSUPPORTED_MEDIA(415),BAD_REQUEST(400)

    Example:
        # Clear all elog messages
    """
    return await client.post("/rw/elog", params={k: v for k, v in {"action": action}.items() if v is not None})

async def save_elog_in_system_dump_format(
    client: RWSClient,
    action: str | None = None,
    path: str | None = None,
) -> httpx.Response:
    """
    Save elog in system dump format.

    Route: ``POST /rw/elog``
    ABB constraints: Since saving an elog in system dump format is an asynchronous task, the value of location header can be subscribed on to get information about the status of the task.

    Args:
        client: Open RWSClient instance.
        action: Query parameter. Optional.
        path: Body parameter. Optional.

    Returns:
        Raw HTTP response. Expected success: HTTP 202.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: UNSUPPORTED_MEDIA(415), UNAUTHORIZED(401)

    Example:
        # Generate elog in system dump format.
    """
    return await client.post("/rw/elog", params={k: v for k, v in {"action": action}.items() if v is not None}, data={k: v for k, v in {"path": path}.items() if v is not None})

async def get_elog_messages_in_domain(
    client: RWSClient,
    domain_number: str,
    example_lang: str | None = None,
    example_resource: str | None = None,
    example_order: str | None = None,
    example_elogseqnum: str | None = None,
) -> httpx.Response:
    """
    Get elog messages in domain.

    Route: ``GET /rw/elog/{domain-number}``
    ABB constraints: Not supported in bootserver mode

    Args:
        client: Open RWSClient instance.
        domain_number: URL path parameter.
        example_lang: Query parameter. Optional.
        example_resource: Query parameter. Optional.
        example_order: Query parameter. Optional.
        example_elogseqnum: Query parameter. Optional.

    Returns:
        Raw HTTP response. Expected success: HTTP 200.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: NOT_FOUND(404)

    Example:
        # curl --digest -u "Default User":robotics "http://localhost/rw/elog/0"
    """
    return await client.get(f"/rw/elog/{domain_number}", params={k: v for k, v in {"example_lang": example_lang, "example_resource": example_resource, "example_order": example_order, "example_elogseqnum": example_elogseqnum}.items() if v is not None})

async def get_actions_on_elog_domain(
    client: RWSClient,
    domain_number: str,
) -> httpx.Response:
    """
    Get Actions on elog domain.

    Route: ``GET /rw/elog/{domain-number}``
    ABB constraints: Not supported in bootserver mode

    Args:
        client: Open RWSClient instance.
        domain_number: URL path parameter.

    Returns:
        Raw HTTP response. Expected success: HTTP 200.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: NOT_FOUND(404)

    Example:
        # curl --digest -u "Default User":robotics "http://localhost/rw/elog/0?action=show"
    """
    return await client.get(f"/rw/elog/{domain_number}")

async def post_clear_elog_messages(
    client: RWSClient,
    domain_number: str,
    action: str | None = None,
) -> httpx.Response:
    """
    Clear elog messages.

    Route: ``POST /rw/elog/{domain-number}``
    ABB constraints: Not supported in bootserver mode

    Args:
        client: Open RWSClient instance.
        domain_number: URL path parameter.
        action: Query parameter. Optional.

    Returns:
        Raw HTTP response. Expected success: HTTP 204.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: UNSUPPORTED_MEDIA(415), BAD_REQUEST(400), NOT_FOUND(404)

    Example:
        # Clear all elog messages
    """
    return await client.post(f"/rw/elog/{domain_number}", params={k: v for k, v in {"action": action}.items() if v is not None})

async def subscribe_on_elog_domain(
    client: RWSClient,
    identifier: str | None = None,
    identifier_p: str | None = None,
) -> httpx.Response:
    """
    Subscribe on elog domain.

    Route: ``POST /subscription``
    ABB constraints: Not supported in bootserver mode

    Args:
        client: Open RWSClient instance.
        identifier: Body parameter. Optional.
        identifier_p: Body parameter. Optional.

    Returns:
        Raw HTTP response. Expected success: HTTP 201.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: UNSUPPORTED_MEDIA(415), BAD_REQUEST(400)

    Example:
        # Subscribe on elog domain 0
    """
    return await client.post("/subscription", data={k: v for k, v in {"identifier": identifier, "identifier_p": identifier_p}.items() if v is not None})

async def get_elog_message_in_domain(
    client: RWSClient,
    domain_number: str,
    sequence_number: str,
    lang: str | None = None,
    example_lang: str | None = None,
) -> httpx.Response:
    """
    Get Elog Message in domain.

    Route: ``GET /rw/elog/{domain-number}/{sequence-number}``
    ABB constraints: Not supported in bootserver mode

    Args:
        client: Open RWSClient instance.
        domain_number: URL path parameter.
        sequence_number: URL path parameter.
        lang: Query parameter. Optional.
        example_lang: Query parameter. Optional.

    Returns:
        Raw HTTP response. Expected success: HTTP 200.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: NOT_FOUND(404), BAD_REQUEST(400)

    Example:
        # curl --digest -u "Default User":robotics "http://localhost/rw/elog/0/8?lang=en"
    """
    return await client.get(f"/rw/elog/{domain_number}/{sequence_number}", params={k: v for k, v in {"lang": lang, "example_lang": example_lang}.items() if v is not None})
