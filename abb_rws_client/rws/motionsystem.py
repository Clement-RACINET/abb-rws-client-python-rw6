# This file is AUTO-GENERATED by contrib/generator/main.py
# DO NOT EDIT MANUALLY — run the generator to regenerate.
#  Generator author: Clement RACINET
"""
RWS module: RobotWare Services → Motion System → Get Motion System

1:1 mirror of the ABB RobotWare 6 REST API.
Each function maps to exactly one HTTP endpoint.
No composed logic — see highlevel/ for wrappers.
"""
from __future__ import annotations

import httpx

from abb_rws_client._core.client import RWSClient


async def get_motion_system(
    client: RWSClient,
    continue_on_err: str | None = None,
    resource: str | None = None,
) -> httpx.Response:
    """
    Get Motion System.

    Route: ``GET /rw/motionsystem``

    Args:
        client: Open RWSClient instance.
        continue_on_err: Query parameter. Optional.
        resource: Query parameter. Optional.

    Returns:
        Raw HTTP response. Expected success: HTTP 200.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: BAD_REQUEST(400)

    Example:
        ```python
        >>> await get_motion_system(client)
        <Response [200]>
        ```
    """
    return await client.get(
        "/rw/motionsystem",
        params={
            k: v
            for k, v in {
                "continue_on_err": continue_on_err,
                "resource": resource
            }.items()
            if v is not None
        },
    )

async def get_motion_system_action(
    client: RWSClient,
    action: str | None = None,
) -> httpx.Response:
    """
    Get Motion System action.

    Route: ``GET /rw/motionsystem``

    Args:
        client: Open RWSClient instance.
        action: Query parameter. Optional.

    Returns:
        Raw HTTP response. Expected success: HTTP 200.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: BAD_REQUEST(400)

    Example:
        ```python
        >>> await get_motion_system_action(client)
        <Response [200]>
        ```
    """
    return await client.get(
        "/rw/motionsystem",
        params={k: v for k, v in {"action": action}.items() if v is not None},
    )

async def set_mechunit_for_jogging(
    client: RWSClient,
    action: str | None = None,
    mechunit_name: str | None = None,
) -> httpx.Response:
    """
    Set Mechunit for jogging.

    Route: ``POST /rw/motionsystem``

    Args:
        client: Open RWSClient instance.
        action: Query parameter. Optional.
        mechunit_name: Body parameter. Optional.

    Returns:
        Raw HTTP response. Expected success: HTTP 204.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: BAD_REQUEST(400), FORBIDDEN(403)

    Example:
        ```python
        >>> await set_mechunit_for_jogging(client)
        <Response [204]>
        ```
    """
    return await client.post(
        "/rw/motionsystem",
        params={k: v for k, v in {"action": action}.items() if v is not None},
        data={k: v for k, v in {"mechunit_name": mechunit_name}.items() if v is not None},
    )

async def post_perform_jogging(
    client: RWSClient,
    action: str | None = None,
    axis1: str | None = None,
    axis2: str | None = None,
    axis3: str | None = None,
    axis4: str | None = None,
    axis5: str | None = None,
    axis6: str | None = None,
    ccount: str | None = None,
    inc_mode: str | None = None,
) -> httpx.Response:
    """
    Perform Jogging.

    Route: ``POST /rw/motionsystem``

    Args:
        client: Open RWSClient instance.
        action: Query parameter. Optional.
        axis1: Body parameter. Optional.
        axis2: Body parameter. Optional.
        axis3: Body parameter. Optional.
        axis4: Body parameter. Optional.
        axis5: Body parameter. Optional.
        axis6: Body parameter. Optional.
        ccount: Body parameter. Optional.
        inc_mode: Body parameter. Optional.

    Returns:
        Raw HTTP response. Expected success: HTTP 204.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: BAD_REQUEST(400), FORBIDDEN(403)

    Example:
        ```python
        >>> await post_perform_jogging(client)
        <Response [204]>
        ```
    """
    return await client.post(
        "/rw/motionsystem",
        params={k: v for k, v in {"action": action}.items() if v is not None},
        data={
            k: v
            for k, v in {
                "axis1": axis1,
                "axis2": axis2,
                "axis3": axis3,
                "axis4": axis4,
                "axis5": axis5,
                "axis6": axis6,
                "ccount": ccount,
                "inc_mode": inc_mode
            }.items()
            if v is not None
        },
    )

async def set_robo_target_position(
    client: RWSClient,
    action: str | None = None,
    pos_x: str | None = None,
    pos_y: str | None = None,
    pos_z: str | None = None,
    orient_q1: str | None = None,
    orient_q2: str | None = None,
    orient_q3: str | None = None,
    orient_q4: str | None = None,
    config_j1: str | None = None,
    config_j4: str | None = None,
    config_j6: str | None = None,
) -> httpx.Response:
    """
    Set Robo Target Position.

    Route: ``POST /rw/motionsystem``

    Args:
        client: Open RWSClient instance.
        action: Query parameter. Optional.
        pos_x: Body parameter. Optional.
        pos_y: Body parameter. Optional.
        pos_z: Body parameter. Optional.
        orient_q1: Body parameter. Optional.
        orient_q2: Body parameter. Optional.
        orient_q3: Body parameter. Optional.
        orient_q4: Body parameter. Optional.
        config_j1: Body parameter. Optional.
        config_j4: Body parameter. Optional.
        config_j6: Body parameter. Optional.

    Returns:
        Raw HTTP response. Expected success: HTTP 204.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: BAD_REQUEST(400), FORBIDDEN(403)

    Example:
        ```python
        >>> await set_robo_target_position(client)
        <Response [204]>
        ```
    """
    return await client.post(
        "/rw/motionsystem",
        params={k: v for k, v in {"action": action}.items() if v is not None},
        data={
            k: v
            for k, v in {
                "pos_x": pos_x,
                "pos_y": pos_y,
                "pos_z": pos_z,
                "orient_q1": orient_q1,
                "orient_q2": orient_q2,
                "orient_q3": orient_q3,
                "orient_q4": orient_q4,
                "config_j1": config_j1,
                "config_j4": config_j4,
                "config_j6": config_j6
            }.items()
            if v is not None
        },
    )

async def get_check_change_count(
    client: RWSClient,
    changecount: str | None = None,
) -> httpx.Response:
    """
    Get check change count.

    Route: ``GET /rw/motionsystem/checkchangecount``

    Args:
        client: Open RWSClient instance.
        changecount: Query parameter. Optional.

    Returns:
        Raw HTTP response. Expected success: HTTP 200.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: BAD_REQUEST(400)

    Example:
        ```python
        >>> await get_check_change_count(client)
        <Response [200]>
        ```
    """
    return await client.get(
        "/rw/motionsystem/checkchangecount",
        params={k: v for k, v in {"changecount": changecount}.items() if v is not None},
    )

async def subscribe_on_error_eventchange(
    client: RWSClient,
    identifier: str | None = None,
    identifier_p: str | None = None,
) -> httpx.Response:
    """
    Subscribe on Error EventChange.

    Route: ``POST /subscription``
    ABB constraints: On subscription an empty initial event will be generated. Not supported in
        bootserver mode.

    Args:
        client: Open RWSClient instance.
        identifier: Body parameter. Optional.
        identifier_p: Body parameter. Optional.

    Returns:
        Raw HTTP response. Expected success: HTTP 201.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: BAD_REQUEST(400), UNSUPPORTED_MEDIA(415)

    Example:
        ```python
        >>> await subscribe_on_error_eventchange(client)
        <Response [201]>
        ```
    """
    return await client.post(
        "/subscription",
        data={
            k: v
            for k, v in {
                "identifier": identifier,
                "identifier_p": identifier_p
            }.items()
            if v is not None
        },
    )

async def get_error_state(client: RWSClient) -> httpx.Response:
    """
    Get Error State.

    Route: ``GET /rw/motionsystem/errorstate``

    Args:
        client: Open RWSClient instance.

    Returns:
        Raw HTTP response. Expected success: HTTP 200.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.

    Example:
        ```python
        >>> await get_error_state(client)
        <Response [200]>
        ```
    """
    return await client.get("/rw/motionsystem/errorstate")

async def get_motion_supervision(
    client: RWSClient,
    mechunit: str | None = None,
) -> httpx.Response:
    """
    Get Motion Supervision.

    Route: ``GET /rw/motionsystem/motionsupervision``

    Args:
        client: Open RWSClient instance.
        mechunit: Query parameter. Optional.

    Returns:
        Raw HTTP response. Expected success: HTTP 200.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: BAD_REQUEST(400)

    Example:
        ```python
        >>> await get_motion_supervision(client)
        <Response [200]>
        ```
    """
    return await client.get(
        "/rw/motionsystem/motionsupervision",
        params={k: v for k, v in {"mechunit": mechunit}.items() if v is not None},
    )

async def get_motion_supervision_actions(
    client: RWSClient,
    action: str | None = None,
) -> httpx.Response:
    """
    Get Motion Supervision Actions.

    Route: ``GET /rw/motionsystem/motionsupervision``

    Args:
        client: Open RWSClient instance.
        action: Query parameter. Optional.

    Returns:
        Raw HTTP response. Expected success: HTTP 200.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: BAD_REQUEST(400)

    Example:
        ```python
        >>> await get_motion_supervision_actions(client)
        <Response [200]>
        ```
    """
    return await client.get(
        "/rw/motionsystem/motionsupervision",
        params={k: v for k, v in {"action": action}.items() if v is not None},
    )

async def set_motion_supervision_mode_jog_supervision_mode(
    client: RWSClient,
    action: str | None = None,
    mechunit_name: str | None = None,
    mode: str | None = None,
) -> httpx.Response:
    """
    Set Motion Supervision Mode (Jog Supervision Mode).

    Route: ``POST /rw/motionsystem/motionsupervision``

    Args:
        client: Open RWSClient instance.
        action: Query parameter. Optional.
        mechunit_name: Body parameter. Optional.
        mode: Body parameter. Optional.

    Returns:
        Raw HTTP response. Expected success: HTTP 204.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: BAD_REQUEST(400), FORBIDDEN(403)

    Example:
        ```python
        >>> await set_motion_supervision_mode_jog_supervision_mode(client)
        <Response [204]>
        ```
    """
    return await client.post(
        "/rw/motionsystem/motionsupervision",
        params={k: v for k, v in {"action": action}.items() if v is not None},
        data={
            k: v
            for k, v in {
                "mechunit_name": mechunit_name,
                "mode": mode
            }.items()
            if v is not None
        },
    )

async def set_motion_supervision_sensitivity_jog_supervision_sensitivity(
    client: RWSClient,
    action: str | None = None,
    mechunit_name: str | None = None,
    sensitivity: str | None = None,
) -> httpx.Response:
    """
    Set Motion Supervision Sensitivity (Jog Supervision Sensitivity).

    Route: ``POST /rw/motionsystem/motionsupervision``

    Args:
        client: Open RWSClient instance.
        action: Query parameter. Optional.
        mechunit_name: Body parameter. Optional.
        sensitivity: Body parameter. Optional.

    Returns:
        Raw HTTP response. Expected success: HTTP 204.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: BAD_REQUEST(400), FORBIDDEN(403)

    Example:
        ```python
        >>> await set_motion_supervision_sensitivity_jog_supervision_sensitivity(client)
        <Response [204]>
        ```
    """
    return await client.post(
        "/rw/motionsystem/motionsupervision",
        params={k: v for k, v in {"action": action}.items() if v is not None},
        data={
            k: v
            for k, v in {
                "mechunit_name": mechunit_name,
                "sensitivity": sensitivity
            }.items()
            if v is not None
        },
    )

async def get_motion_supervision_collision_prediction_mode(
    client: RWSClient,
    action: str | None = None,
) -> httpx.Response:
    """
    Get Motion Supervision Collision Prediction Mode.

    Route: ``GET /rw/motionsystem/motionsupervision``

    Args:
        client: Open RWSClient instance.
        action: Query parameter. Optional.

    Returns:
        Raw HTTP response. Expected success: HTTP 200.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: BAD_REQUEST(400)

    Example:
        ```python
        >>> await get_motion_supervision_collision_prediction_mode(client)
        <Response [200]>
        ```
    """
    return await client.get(
        "/rw/motionsystem/motionsupervision",
        params={k: v for k, v in {"action": action}.items() if v is not None},
    )

async def set_motion_supervision_collision_prediction_mode(
    client: RWSClient,
    action: str | None = None,
    mode: str | None = None,
) -> httpx.Response:
    """
    Set Motion Supervision Collision Prediction Mode.

    Route: ``POST /rw/motionsystem/motionsupervision``

    Args:
        client: Open RWSClient instance.
        action: Query parameter. Optional.
        mode: Body parameter. Optional.

    Returns:
        Raw HTTP response. Expected success: HTTP 204.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: BAD_REQUEST(400), FORBIDDEN(403)

    Example:
        ```python
        >>> await set_motion_supervision_collision_prediction_mode(client)
        <Response [204]>
        ```
    """
    return await client.post(
        "/rw/motionsystem/motionsupervision",
        params={k: v for k, v in {"action": action}.items() if v is not None},
        data={k: v for k, v in {"mode": mode}.items() if v is not None},
    )

async def get_path_supervision(
    client: RWSClient,
    mechunit: str | None = None,
) -> httpx.Response:
    """
    Get Path Supervision.

    Route: ``GET /rw/motionsystem/pathsupervision``

    Args:
        client: Open RWSClient instance.
        mechunit: Query parameter. Optional.

    Returns:
        Raw HTTP response. Expected success: HTTP 200.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: BAD_REQUEST(400)

    Example:
        ```python
        >>> await get_path_supervision(client)
        <Response [200]>
        ```
    """
    return await client.get(
        "/rw/motionsystem/pathsupervision",
        params={k: v for k, v in {"mechunit": mechunit}.items() if v is not None},
    )

async def get_path_supervision_actions(
    client: RWSClient,
    action: str | None = None,
) -> httpx.Response:
    """
    Get Path Supervision Actions.

    Route: ``GET /rw/motionsystem/pathsupervision``

    Args:
        client: Open RWSClient instance.
        action: Query parameter. Optional.

    Returns:
        Raw HTTP response. Expected success: HTTP 200.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: BAD_REQUEST(400)

    Example:
        ```python
        >>> await get_path_supervision_actions(client)
        <Response [200]>
        ```
    """
    return await client.get(
        "/rw/motionsystem/pathsupervision",
        params={k: v for k, v in {"action": action}.items() if v is not None},
    )

async def set_path_supervision_mode(
    client: RWSClient,
    action: str | None = None,
    mechunit: str | None = None,
    mode: str | None = None,
) -> httpx.Response:
    """
    Set Path Supervision Mode.

    Route: ``POST /rw/motionsystem/pathsupervision``

    Args:
        client: Open RWSClient instance.
        action: Query parameter. Optional.
        mechunit: Body parameter. Optional.
        mode: Body parameter. Optional.

    Returns:
        Raw HTTP response. Expected success: HTTP 204.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: BAD_REQUEST(400), FORBIDDEN(403)

    Example:
        ```python
        >>> await set_path_supervision_mode(client)
        <Response [204]>
        ```
    """
    return await client.post(
        "/rw/motionsystem/pathsupervision",
        params={k: v for k, v in {"action": action}.items() if v is not None},
        data={k: v for k, v in {"mechunit": mechunit, "mode": mode}.items() if v is not None},
    )

async def set_path_supervision_level(
    client: RWSClient,
    action: str | None = None,
    mechunit: str | None = None,
    level: str | None = None,
) -> httpx.Response:
    """
    Set Path Supervision Level.

    Route: ``POST /rw/motionsystem/pathsupervision``

    Args:
        client: Open RWSClient instance.
        action: Query parameter. Optional.
        mechunit: Body parameter. Optional.
        level: Body parameter. Optional.

    Returns:
        Raw HTTP response. Expected success: HTTP 204.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: BAD_REQUEST(400), FORBIDDEN(403)

    Example:
        ```python
        >>> await set_path_supervision_level(client)
        <Response [204]>
        ```
    """
    return await client.post(
        "/rw/motionsystem/pathsupervision",
        params={k: v for k, v in {"action": action}.items() if v is not None},
        data={k: v for k, v in {"mechunit": mechunit, "level": level}.items() if v is not None},
    )

async def get_non_motion_execution_mode(client: RWSClient) -> httpx.Response:
    """
    Get Non Motion Execution Mode.

    Route: ``GET /rw/motionsystem/nonmotionexecution``

    Args:
        client: Open RWSClient instance.

    Returns:
        Raw HTTP response. Expected success: HTTP 200.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: BAD_REQUEST(400)

    Example:
        ```python
        >>> await get_non_motion_execution_mode(client)
        <Response [200]>
        ```
    """
    return await client.get("/rw/motionsystem/nonmotionexecution")

async def get_nonmotion_execution_actions(
    client: RWSClient,
    action: str | None = None,
) -> httpx.Response:
    """
    Get NonMotion Execution Actions.

    Route: ``GET /rw/motionsystem/nonmotionexecution``

    Args:
        client: Open RWSClient instance.
        action: Query parameter. Optional.

    Returns:
        Raw HTTP response. Expected success: HTTP 200.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: BAD_REQUEST(400)

    Example:
        ```python
        >>> await get_nonmotion_execution_actions(client)
        <Response [200]>
        ```
    """
    return await client.get(
        "/rw/motionsystem/nonmotionexecution",
        params={k: v for k, v in {"action": action}.items() if v is not None},
    )

async def set_nonmotion_execution_mode(
    client: RWSClient,
    action: str | None = None,
    mode: str | None = None,
) -> httpx.Response:
    """
    Set NonMotion Execution Mode.

    Route: ``POST /rw/motionsystem/nonmotionexecution``

    Args:
        client: Open RWSClient instance.
        action: Query parameter. Optional.
        mode: Body parameter. Optional.

    Returns:
        Raw HTTP response. Expected success: HTTP 204.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: BAD_REQUEST (400), FORBIDDEN (403)

    Example:
        ```python
        >>> await set_nonmotion_execution_mode(client)
        <Response [204]>
        ```
    """
    return await client.post(
        "/rw/motionsystem/nonmotionexecution",
        params={k: v for k, v in {"action": action}.items() if v is not None},
        data={k: v for k, v in {"mode": mode}.items() if v is not None},
    )

async def get_mechunits(client: RWSClient) -> httpx.Response:
    """
    Get Mechunits.

    Route: ``GET /rw/motionsystem/mechunits``

    Args:
        client: Open RWSClient instance.

    Returns:
        Raw HTTP response. Expected success: HTTP 200.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: BAD_REQUEST (400)

    Example:
        ```python
        >>> await get_mechunits(client)
        <Response [200]>
        ```
    """
    return await client.get("/rw/motionsystem/mechunits")

async def get_mechunit(
    client: RWSClient,
    mechunit: str,
    continue_on_err: str | None = None,
    resource: str | None = None,
) -> httpx.Response:
    """
    Get Mechunit.

    Route: ``GET /rw/motionsystem/mechunits/{mechunit}``

    Args:
        client: Open RWSClient instance.
        mechunit: URL path parameter.
        continue_on_err: Query parameter. Optional.
        resource: Query parameter. Optional.

    Returns:
        Raw HTTP response. Expected success: HTTP 200.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: Not Found(404),BAD_REQUEST(400)

    Example:
        ```python
        >>> await get_mechunit(client, "mechunit_value")
        <Response [200]>
        ```
    """
    return await client.get(
        f"/rw/motionsystem/mechunits/{mechunit}",
        params={
            k: v
            for k, v in {
                "continue_on_err": continue_on_err,
                "resource": resource
            }.items()
            if v is not None
        },
    )

async def get_mechunit_action(
    client: RWSClient,
    mechunit: str,
    action: str | None = None,
) -> httpx.Response:
    """
    Get Mechunit action.

    Route: ``GET /rw/motionsystem/mechunits/{mechunit}``

    Args:
        client: Open RWSClient instance.
        mechunit: URL path parameter.
        action: Query parameter. Optional.

    Returns:
        Raw HTTP response. Expected success: HTTP 200.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: BAD_REQUEST(400)

    Example:
        ```python
        >>> await get_mechunit_action(client, "mechunit_value")
        <Response [200]>
        ```
    """
    return await client.get(
        f"/rw/motionsystem/mechunits/{mechunit}",
        params={k: v for k, v in {"action": action}.items() if v is not None},
    )

async def set_mechunit(
    client: RWSClient,
    mechunit: str,
    action: str | None = None,
    continue_on_err: str | None = None,
    tool: str | None = None,
    wobj: str | None = None,
    payload: str | None = None,
    total_payload: str | None = None,
    mode: str | None = None,
    jog_mode: str | None = None,
    coord_system: str | None = None,
) -> httpx.Response:
    """
    Set Mechunit.

    Route: ``POST /rw/motionsystem/mechunits/{mechunit}``

    Args:
        client: Open RWSClient instance.
        mechunit: URL path parameter.
        action: Query parameter. Optional.
        continue_on_err: Query parameter. Optional.
        tool: Body parameter. Optional.
        wobj: Body parameter. Optional.
        payload: Body parameter. Optional.
        total_payload: Body parameter. Optional.
        mode: Body parameter. Optional.
        jog_mode: Body parameter. Optional.
        coord_system: Body parameter. Optional.

    Returns:
        Raw HTTP response. Expected success: HTTP 204.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: BAD_REQUEST(400)

    Example:
        ```python
        >>> await set_mechunit(client, "mechunit_value")
        <Response [204]>
        ```
    """
    return await client.post(
        f"/rw/motionsystem/mechunits/{mechunit}",
        params={
            k: v
            for k, v in {
                "action": action,
                "continue_on_err": continue_on_err
            }.items()
            if v is not None
        },
        data={
            k: v
            for k, v in {
                "tool": tool,
                "wobj": wobj,
                "payload": payload,
                "total_payload": total_payload,
                "mode": mode,
                "jog_mode": jog_mode,
                "coord_system": coord_system
            }.items()
            if v is not None
        },
    )

async def set_compliance_lead_through(
    client: RWSClient,
    mechunit: str,
    action: str | None = None,
    status: str | None = None,
) -> httpx.Response:
    """
    Set Compliance Lead Through.

    Route: ``POST /rw/motionsystem/mechunits/{mechunit}``

    Args:
        client: Open RWSClient instance.
        mechunit: URL path parameter.
        action: Query parameter. Optional.
        status: Body parameter. Optional.

    Returns:
        Raw HTTP response. Expected success: HTTP 204.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: BAD_REQUEST(400)

    Example:
        ```python
        >>> await set_compliance_lead_through(client, "mechunit_value")
        <Response [204]>
        ```
    """
    return await client.post(
        f"/rw/motionsystem/mechunits/{mechunit}",
        params={k: v for k, v in {"action": action}.items() if v is not None},
        data={k: v for k, v in {"status": status}.items() if v is not None},
    )

async def get_compliance_lead_through(
    client: RWSClient,
    mechunit: str,
    resource: str | None = None,
) -> httpx.Response:
    """
    Get Compliance Lead Through.

    Route: ``GET /rw/motionsystem/mechunits/{mechunit}``

    Args:
        client: Open RWSClient instance.
        mechunit: URL path parameter.
        resource: Query parameter. Optional.

    Returns:
        Raw HTTP response. Expected success: HTTP 200.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: BAD_REQUEST(400)

    Example:
        ```python
        >>> await get_compliance_lead_through(client, "mechunit_value")
        <Response [200]>
        ```
    """
    return await client.get(
        f"/rw/motionsystem/mechunits/{mechunit}",
        params={k: v for k, v in {"resource": resource}.items() if v is not None},
    )

async def set_fine_calibration(
    client: RWSClient,
    mechunit: str,
    action: str | None = None,
    axis: str | None = None,
) -> httpx.Response:
    """
    Set Fine Calibration.

    Route: ``POST /rw/motionsystem/mechunits/{mechunit}``

    Args:
        client: Open RWSClient instance.
        mechunit: URL path parameter.
        action: Query parameter. Optional.
        axis: Body parameter. Optional.

    Returns:
        Raw HTTP response. Expected success: HTTP 204.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: BAD_REQUEST(400)

    Example:
        ```python
        >>> await set_fine_calibration(client, "mechunit_value")
        <Response [204]>
        ```
    """
    return await client.post(
        f"/rw/motionsystem/mechunits/{mechunit}",
        params={k: v for k, v in {"action": action}.items() if v is not None},
        data={k: v for k, v in {"axis": axis}.items() if v is not None},
    )

async def update_syncronize_revolution_counter(
    client: RWSClient,
    mechunit: str,
    action: str | None = None,
    axis: str | None = None,
) -> httpx.Response:
    """
    Update (Syncronize) Revolution Counter.

    Route: ``POST /rw/motionsystem/mechunits/{mechunit}``

    Args:
        client: Open RWSClient instance.
        mechunit: URL path parameter.
        action: Query parameter. Optional.
        axis: Body parameter. Optional.

    Returns:
        Raw HTTP response. Expected success: HTTP 204.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: BAD_REQUEST(400)

    Example:
        ```python
        >>> await update_syncronize_revolution_counter(client, "mechunit_value")
        <Response [204]>
        ```
    """
    return await client.post(
        f"/rw/motionsystem/mechunits/{mechunit}",
        params={k: v for k, v in {"action": action}.items() if v is not None},
        data={k: v for k, v in {"axis": axis}.items() if v is not None},
    )

async def get_physical_joints(
    client: RWSClient,
    mechunit: str,
) -> httpx.Response:
    """
    Get Physical Joints.

    Route: ``GET /rw/motionsystem/mechunits/{mechunit}/pjoints``

    Args:
        client: Open RWSClient instance.
        mechunit: URL path parameter.

    Returns:
        Raw HTTP response. Expected success: HTTP 200.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: BAD_REQUEST(400)

    Example:
        ```python
        >>> await get_physical_joints(client, "mechunit_value")
        <Response [200]>
        ```
    """
    return await client.get(f"/rw/motionsystem/mechunits/{mechunit}/pjoints")

async def get_cartesian_value(
    client: RWSClient,
    mechunit: str,
    tool: str | None = None,
    wobj: str | None = None,
    coordinate: str | None = None,
    elog_at_err: str | None = None,
) -> httpx.Response:
    """
    Get Cartesian Value.

    Route: ``GET /rw/motionsystem/mechunits/{mechunit}/cartesian``

    Args:
        client: Open RWSClient instance.
        mechunit: URL path parameter.
        tool: Query parameter. Optional.
        wobj: Query parameter. Optional.
        coordinate: Query parameter. Optional.
        elog_at_err: Query parameter. Optional.

    Returns:
        Raw HTTP response. Expected success: HTTP 200.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: BAD_REQUEST(400)

    Example:
        ```python
        >>> await get_cartesian_value(client, "mechunit_value")
        <Response [200]>
        ```
    """
    return await client.get(
        f"/rw/motionsystem/mechunits/{mechunit}/cartesian",
        params={
            k: v
            for k, v in {
                "tool": tool,
                "wobj": wobj,
                "coordinate": coordinate,
                "elog_at_err": elog_at_err
            }.items()
            if v is not None
        },
    )

async def set_mechanical_unit(
    client: RWSClient,
    mechunit: str,
    action: str | None = None,
    rob_joint: str | None = None,
    ext_joint: str | None = None,
) -> httpx.Response:
    """
    Set Mechanical Unit.

    Route: ``POST /rw/motionsystem/mechunits/{mechunit}``

    Args:
        client: Open RWSClient instance.
        mechunit: URL path parameter.
        action: Query parameter. Optional.
        rob_joint: Body parameter. Optional.
        ext_joint: Body parameter. Optional.

    Returns:
        Raw HTTP response. Expected success: HTTP 204.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: BAD_REQUEST(400)

    Example:
        ```python
        >>> await set_mechanical_unit(client, "mechunit_value")
        <Response [204]>
        ```
    """
    return await client.post(
        f"/rw/motionsystem/mechunits/{mechunit}",
        params={k: v for k, v in {"action": action}.items() if v is not None},
        data={
            k: v
            for k, v in {
                "rob_joint": rob_joint,
                "ext_joint": ext_joint
            }.items()
            if v is not None
        },
    )

async def get_robtarget(
    client: RWSClient,
    mechunit: str,
    tool: str | None = None,
    wobj: str | None = None,
    coordinate: str | None = None,
) -> httpx.Response:
    """
    Get Robtarget.

    Route: ``GET /rw/motionsystem/mechunits/{mechunit}/robtarget``

    Args:
        client: Open RWSClient instance.
        mechunit: URL path parameter.
        tool: Query parameter. Optional.
        wobj: Query parameter. Optional.
        coordinate: Query parameter. Optional.

    Returns:
        Raw HTTP response. Expected success: HTTP 200.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: BAD_REQUEST(400)

    Example:
        ```python
        >>> await get_robtarget(client, "mechunit_value")
        <Response [200]>
        ```
    """
    return await client.get(
        f"/rw/motionsystem/mechunits/{mechunit}/robtarget",
        params={
            k: v
            for k, v in {
                "tool": tool,
                "wobj": wobj,
                "coordinate": coordinate
            }.items()
            if v is not None
        },
    )

async def get_joint_target(
    client: RWSClient,
    mechunit: str,
) -> httpx.Response:
    """
    Get Joint target.

    Route: ``GET /rw/motionsystem/mechunits/{mechunit}/jointtarget``

    Args:
        client: Open RWSClient instance.
        mechunit: URL path parameter.

    Returns:
        Raw HTTP response. Expected success: HTTP 200.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: BAD_REQUEST(400)

    Example:
        ```python
        >>> await get_joint_target(client, "mechunit_value")
        <Response [200]>
        ```
    """
    return await client.get(f"/rw/motionsystem/mechunits/{mechunit}/jointtarget")

async def subscribe_on_mechunit_mode_change(
    client: RWSClient,
    identifier: str | None = None,
    identifier_p: str | None = None,
) -> httpx.Response:
    """
    Subscribe on Mechunit Mode Change.

    Route: ``POST /subscription``
    ABB constraints: Not supported in bootserver mode.

    Args:
        client: Open RWSClient instance.
        identifier: Body parameter. Optional.
        identifier_p: Body parameter. Optional.

    Returns:
        Raw HTTP response. Expected success: HTTP 201.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: BAD_REQUEST(400), UNSUPPORTED_MEDIA(415)

    Example:
        ```python
        >>> await subscribe_on_mechunit_mode_change(client)
        <Response [201]>
        ```
    """
    return await client.post(
        "/subscription",
        data={
            k: v
            for k, v in {
                "identifier": identifier,
                "identifier_p": identifier_p
            }.items()
            if v is not None
        },
    )

async def get_joints_from_position(
    client: RWSClient,
    mechunit: str,
    action: str | None = None,
    curr_position: str | None = None,
    curr_ext_joints: str | None = None,
    tool_frame_position: str | None = None,
    curr_orientation: str | None = None,
    tool_frame_orientation: str | None = None,
    old_rob_joints: str | None = None,
    old_ext_joints: str | None = None,
    robot_fixed_object: str | None = None,
    robot_configuration: str | None = None,
    elog_at_error: str | None = None,
) -> httpx.Response:
    """
    Get Joints From Position.

    Route: ``POST /rw/motionsystem/mechunits/{mechunit}``

    Args:
        client: Open RWSClient instance.
        mechunit: URL path parameter.
        action: Query parameter. Optional.
        curr_position: Body parameter. Optional.
        curr_ext_joints: Body parameter. Optional.
        tool_frame_position: Body parameter. Optional.
        curr_orientation: Body parameter. Optional.
        tool_frame_orientation: Body parameter. Optional.
        old_rob_joints: Body parameter. Optional.
        old_ext_joints: Body parameter. Optional.
        robot_fixed_object: Body parameter. Optional.
        robot_configuration: Body parameter. Optional.
        elog_at_error: Body parameter. Optional.

    Returns:
        Raw HTTP response. Expected success: HTTP 200.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: BAD_REQUEST (400)

    Example:
        ```python
        >>> await get_joints_from_position(client, "mechunit_value")
        <Response [200]>
        ```
    """
    return await client.post(
        f"/rw/motionsystem/mechunits/{mechunit}",
        params={k: v for k, v in {"action": action}.items() if v is not None},
        data={
            k: v
            for k, v in {
                "curr_position": curr_position,
                "curr_ext_joints": curr_ext_joints,
                "tool_frame_position": tool_frame_position,
                "curr_orientation": curr_orientation,
                "tool_frame_orientation": tool_frame_orientation,
                "old_rob_joints": old_rob_joints,
                "old_ext_joints": old_ext_joints,
                "robot_fixed_object": robot_fixed_object,
                "robot_configuration": robot_configuration,
                "elog_at_error": elog_at_error
            }.items()
            if v is not None
        },
    )

async def get_position_from_joints(
    client: RWSClient,
    mechunit: str,
    action: str | None = None,
    tool_frame_position: str | None = None,
    tool_frame_orientation: str | None = None,
    rob_joints: str | None = None,
    ext_joints: str | None = None,
    robot_fixed_object: str | None = None,
    elog_at_error: str | None = None,
) -> httpx.Response:
    """
    Get Position From Joints.

    Route: ``POST /rw/motionsystem/mechunits/{mechunit}``

    Args:
        client: Open RWSClient instance.
        mechunit: URL path parameter.
        action: Query parameter. Optional.
        tool_frame_position: Body parameter. Optional.
        tool_frame_orientation: Body parameter. Optional.
        rob_joints: Body parameter. Optional.
        ext_joints: Body parameter. Optional.
        robot_fixed_object: Body parameter. Optional.
        elog_at_error: Body parameter. Optional.

    Returns:
        Raw HTTP response. Expected success: HTTP 200.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: BAD_REQUEST (400)

    Example:
        ```python
        >>> await get_position_from_joints(client, "mechunit_value")
        <Response [200]>
        ```
    """
    return await client.post(
        f"/rw/motionsystem/mechunits/{mechunit}",
        params={k: v for k, v in {"action": action}.items() if v is not None},
        data={
            k: v
            for k, v in {
                "tool_frame_position": tool_frame_position,
                "tool_frame_orientation": tool_frame_orientation,
                "rob_joints": rob_joints,
                "ext_joints": ext_joints,
                "robot_fixed_object": robot_fixed_object,
                "elog_at_error": elog_at_error
            }.items()
            if v is not None
        },
    )

async def get_all_joint_solution(
    client: RWSClient,
    mechunit: str,
    action: str | None = None,
    curr_position: str | None = None,
    curr_ext_joints: str | None = None,
    tool_frame_position: str | None = None,
    curr_orientation: str | None = None,
    tool_frame_orientation: str | None = None,
    robot_fixed_object: str | None = None,
    robot_configuration: str | None = None,
) -> httpx.Response:
    """
    Get All Joint Solution.

    Route: ``POST /rw/motionsystem/mechunits/{mechunit}``

    Args:
        client: Open RWSClient instance.
        mechunit: URL path parameter.
        action: Query parameter. Optional.
        curr_position: Body parameter. Optional.
        curr_ext_joints: Body parameter. Optional.
        tool_frame_position: Body parameter. Optional.
        curr_orientation: Body parameter. Optional.
        tool_frame_orientation: Body parameter. Optional.
        robot_fixed_object: Body parameter. Optional.
        robot_configuration: Body parameter. Optional.

    Returns:
        Raw HTTP response. Expected success: HTTP 200.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: BAD_REQUEST (400)

    Example:
        ```python
        >>> await get_all_joint_solution(client, "mechunit_value")
        <Response [200]>
        ```
    """
    return await client.post(
        f"/rw/motionsystem/mechunits/{mechunit}",
        params={k: v for k, v in {"action": action}.items() if v is not None},
        data={
            k: v
            for k, v in {
                "curr_position": curr_position,
                "curr_ext_joints": curr_ext_joints,
                "tool_frame_position": tool_frame_position,
                "curr_orientation": curr_orientation,
                "tool_frame_orientation": tool_frame_orientation,
                "robot_fixed_object": robot_fixed_object,
                "robot_configuration": robot_configuration
            }.items()
            if v is not None
        },
    )

async def get_joints_from_cartesian(
    client: RWSClient,
    mechunit: str,
    action: str | None = None,
    curr_position: str | None = None,
    curr_ext_joints: str | None = None,
    tool_frame_position: str | None = None,
    curr_orientation: str | None = None,
    tool_frame_orientation: str | None = None,
    old_rob_joints: str | None = None,
    old_ext_joints: str | None = None,
    robot_fixed_object: str | None = None,
    robot_configuration: str | None = None,
    elog_at_error: str | None = None,
) -> httpx.Response:
    """
    Get Joints From Cartesian.

    Route: ``POST /rw/motionsystem/mechunits/{mechunit}``

    Args:
        client: Open RWSClient instance.
        mechunit: URL path parameter.
        action: Query parameter. Optional.
        curr_position: Body parameter. Optional.
        curr_ext_joints: Body parameter. Optional.
        tool_frame_position: Body parameter. Optional.
        curr_orientation: Body parameter. Optional.
        tool_frame_orientation: Body parameter. Optional.
        old_rob_joints: Body parameter. Optional.
        old_ext_joints: Body parameter. Optional.
        robot_fixed_object: Body parameter. Optional.
        robot_configuration: Body parameter. Optional.
        elog_at_error: Body parameter. Optional.

    Returns:
        Raw HTTP response. Expected success: HTTP 200.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: BAD_REQUEST (400)

    Example:
        ```python
        >>> await get_joints_from_cartesian(client, "mechunit_value")
        <Response [200]>
        ```
    """
    return await client.post(
        f"/rw/motionsystem/mechunits/{mechunit}",
        params={k: v for k, v in {"action": action}.items() if v is not None},
        data={
            k: v
            for k, v in {
                "curr_position": curr_position,
                "curr_ext_joints": curr_ext_joints,
                "tool_frame_position": tool_frame_position,
                "curr_orientation": curr_orientation,
                "tool_frame_orientation": tool_frame_orientation,
                "old_rob_joints": old_rob_joints,
                "old_ext_joints": old_ext_joints,
                "robot_fixed_object": robot_fixed_object,
                "robot_configuration": robot_configuration,
                "elog_at_error": elog_at_error
            }.items()
            if v is not None
        },
    )

async def get_calibration_info(
    client: RWSClient,
    mechunit: str,
) -> httpx.Response:
    """
    Get Calibration Info.

    Route: ``GET /rw/motionsystem/mechunits/{mechunit}/calibrationinfo``

    Args:
        client: Open RWSClient instance.
        mechunit: URL path parameter.

    Returns:
        Raw HTTP response. Expected success: HTTP 200.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: BAD_REQUEST(400)

    Example:
        ```python
        >>> await get_calibration_info(client, "mechunit_value")
        <Response [200]>
        ```
    """
    return await client.get(f"/rw/motionsystem/mechunits/{mechunit}/calibrationinfo")

async def post_calibration_for_baseframe(
    client: RWSClient,
    mechunit: str,
    method: str | None = None,
    type: str | None = None,
    reference: str | None = None,
    point1: str | None = None,
    point2: str | None = None,
    point3: str | None = None,
    point4: str | None = None,
    point5: str | None = None,
    point6: str | None = None,
    point7: str | None = None,
) -> httpx.Response:
    """
    Calibration for BaseFrame.

    Route: ``POST /rw/motionsystem/mechunits/{mechunit}/calib``

    Args:
        client: Open RWSClient instance.
        mechunit: URL path parameter.
        method: Body parameter. Optional.
        type: Body parameter. Optional.
        reference: Body parameter. Optional.
        point1: Body parameter. Optional.
        point2: Body parameter. Optional.
        point3: Body parameter. Optional.
        point4: Body parameter. Optional.
        point5: Body parameter. Optional.
        point6: Body parameter. Optional.
        point7: Body parameter. Optional.

    Returns:
        Raw HTTP response. Expected success: HTTP 200.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: BAD_REQUEST (400)

    Example:
        ```python
        >>> await post_calibration_for_baseframe(client, "mechunit_value")
        <Response [200]>
        ```
    """
    return await client.post(
        f"/rw/motionsystem/mechunits/{mechunit}/calib",
        data={
            k: v
            for k, v in {
                "method": method,
                "type": type,
                "reference": reference,
                "point1": point1,
                "point2": point2,
                "point3": point3,
                "point4": point4,
                "point5": point5,
                "point6": point6,
                "point7": point7
            }.items()
            if v is not None
        },
    )

async def post_calibration_for_baseframemoving(
    client: RWSClient,
    mechunit: str,
    method: str | None = None,
    type: str | None = None,
    point1: str | None = None,
    point2: str | None = None,
    point3: str | None = None,
    point4: str | None = None,
    point5: str | None = None,
    point6: str | None = None,
    point7: str | None = None,
    point8: str | None = None,
) -> httpx.Response:
    """
    Calibration for BaseFrameMoving.

    Route: ``POST /rw/motionsystem/mechunits/{mechunit}/calib``

    Args:
        client: Open RWSClient instance.
        mechunit: URL path parameter.
        method: Body parameter. Optional.
        type: Body parameter. Optional.
        point1: Body parameter. Optional.
        point2: Body parameter. Optional.
        point3: Body parameter. Optional.
        point4: Body parameter. Optional.
        point5: Body parameter. Optional.
        point6: Body parameter. Optional.
        point7: Body parameter. Optional.
        point8: Body parameter. Optional.

    Returns:
        Raw HTTP response. Expected success: HTTP 200.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: BAD_REQUEST (400) ,See

    Example:
        ```python
        >>> await post_calibration_for_baseframemoving(client, "mechunit_value")
        <Response [200]>
        ```
    """
    return await client.post(
        f"/rw/motionsystem/mechunits/{mechunit}/calib",
        data={
            k: v
            for k, v in {
                "method": method,
                "type": type,
                "point1": point1,
                "point2": point2,
                "point3": point3,
                "point4": point4,
                "point5": point5,
                "point6": point6,
                "point7": point7,
                "point8": point8
            }.items()
            if v is not None
        },
    )

async def post_calibration_for_externalrobotnombasenew(
    client: RWSClient,
    mechunit: str,
    method: str | None = None,
    type: str | None = None,
    point1: str | None = None,
    point2: str | None = None,
    point3: str | None = None,
    point4: str | None = None,
    point5: str | None = None,
    point6: str | None = None,
    point7: str | None = None,
    point8: str | None = None,
) -> httpx.Response:
    """
    Calibration for ExternalRobotNomBaseNew.

    Route: ``POST /rw/motionsystem/mechunits/{mechunit}/calib``

    Args:
        client: Open RWSClient instance.
        mechunit: URL path parameter.
        method: Body parameter. Optional.
        type: Body parameter. Optional.
        point1: Body parameter. Optional.
        point2: Body parameter. Optional.
        point3: Body parameter. Optional.
        point4: Body parameter. Optional.
        point5: Body parameter. Optional.
        point6: Body parameter. Optional.
        point7: Body parameter. Optional.
        point8: Body parameter. Optional.

    Returns:
        Raw HTTP response. Expected success: HTTP 200.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: BAD_REQUEST (400)

    Example:
        ```python
        >>> await post_calibration_for_externalrobotnombasenew(client, "mechunit_value")
        <Response [200]>
        ```
    """
    return await client.post(
        f"/rw/motionsystem/mechunits/{mechunit}/calib",
        data={
            k: v
            for k, v in {
                "method": method,
                "type": type,
                "point1": point1,
                "point2": point2,
                "point3": point3,
                "point4": point4,
                "point5": point5,
                "point6": point6,
                "point7": point7,
                "point8": point8
            }.items()
            if v is not None
        },
    )

async def post_calibration_for_robotaxisrot(
    client: RWSClient,
    mechunit: str,
    method: str | None = None,
    type: str | None = None,
    tolerance: str | None = None,
    axis: str | None = None,
    point1: str | None = None,
    point2: str | None = None,
    point3: str | None = None,
    point4: str | None = None,
    point5: str | None = None,
    point6: str | None = None,
) -> httpx.Response:
    """
    Calibration for RobotAxisRot.

    Route: ``POST /rw/motionsystem/mechunits/{mechunit}/calib``

    Args:
        client: Open RWSClient instance.
        mechunit: URL path parameter.
        method: Body parameter. Optional.
        type: Body parameter. Optional.
        tolerance: Body parameter. Optional.
        axis: Body parameter. Optional.
        point1: Body parameter. Optional.
        point2: Body parameter. Optional.
        point3: Body parameter. Optional.
        point4: Body parameter. Optional.
        point5: Body parameter. Optional.
        point6: Body parameter. Optional.

    Returns:
        Raw HTTP response. Expected success: HTTP 200.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: BAD_REQUEST (400)

    Example:
        ```python
        >>> await post_calibration_for_robotaxisrot(client, "mechunit_value")
        <Response [200]>
        ```
    """
    return await client.post(
        f"/rw/motionsystem/mechunits/{mechunit}/calib",
        data={
            k: v
            for k, v in {
                "method": method,
                "type": type,
                "tolerance": tolerance,
                "axis": axis,
                "point1": point1,
                "point2": point2,
                "point3": point3,
                "point4": point4,
                "point5": point5,
                "point6": point6
            }.items()
            if v is not None
        },
    )

async def post_calibration_for_singleuserrotnew(
    client: RWSClient,
    mechunit: str,
    method: str | None = None,
    type: str | None = None,
    tolerance: str | None = None,
    point1: str | None = None,
    point2: str | None = None,
    point3: str | None = None,
    point4: str | None = None,
    point5: str | None = None,
    point6: str | None = None,
    point7: str | None = None,
) -> httpx.Response:
    """
    Calibration for SingleUserRotNew.

    Route: ``POST /rw/motionsystem/mechunits/{mechunit}/calib``

    Args:
        client: Open RWSClient instance.
        mechunit: URL path parameter.
        method: Body parameter. Optional.
        type: Body parameter. Optional.
        tolerance: Body parameter. Optional.
        point1: Body parameter. Optional.
        point2: Body parameter. Optional.
        point3: Body parameter. Optional.
        point4: Body parameter. Optional.
        point5: Body parameter. Optional.
        point6: Body parameter. Optional.
        point7: Body parameter. Optional.

    Returns:
        Raw HTTP response. Expected success: HTTP 200.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: BAD_REQUEST (400) ,See

    Example:
        ```python
        >>> await post_calibration_for_singleuserrotnew(client, "mechunit_value")
        <Response [200]>
        ```
    """
    return await client.post(
        f"/rw/motionsystem/mechunits/{mechunit}/calib",
        data={
            k: v
            for k, v in {
                "method": method,
                "type": type,
                "tolerance": tolerance,
                "point1": point1,
                "point2": point2,
                "point3": point3,
                "point4": point4,
                "point5": point5,
                "point6": point6,
                "point7": point7
            }.items()
            if v is not None
        },
    )

async def post_calibration_for_rotextctrlzdef(
    client: RWSClient,
    mechunit: str,
    method: str | None = None,
    type: str | None = None,
    tolerance: str | None = None,
    pose: str | None = None,
    point1: str | None = None,
    point2: str | None = None,
    point3: str | None = None,
    point4: str | None = None,
    point5: str | None = None,
    point6: str | None = None,
) -> httpx.Response:
    """
    Calibration for RotExtCtrlZdef.

    Route: ``POST /rw/motionsystem/mechunits/{mechunit}/calib``

    Args:
        client: Open RWSClient instance.
        mechunit: URL path parameter.
        method: Body parameter. Optional.
        type: Body parameter. Optional.
        tolerance: Body parameter. Optional.
        pose: Body parameter. Optional.
        point1: Body parameter. Optional.
        point2: Body parameter. Optional.
        point3: Body parameter. Optional.
        point4: Body parameter. Optional.
        point5: Body parameter. Optional.
        point6: Body parameter. Optional.

    Returns:
        Raw HTTP response. Expected success: HTTP 200.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: BAD_REQUEST(400)

    Example:
        ```python
        >>> await post_calibration_for_rotextctrlzdef(client, "mechunit_value")
        <Response [200]>
        ```
    """
    return await client.post(
        f"/rw/motionsystem/mechunits/{mechunit}/calib",
        data={
            k: v
            for k, v in {
                "method": method,
                "type": type,
                "tolerance": tolerance,
                "pose": pose,
                "point1": point1,
                "point2": point2,
                "point3": point3,
                "point4": point4,
                "point5": point5,
                "point6": point6
            }.items()
            if v is not None
        },
    )

async def post_calibration_for_singleuserlin(
    client: RWSClient,
    mechunit: str,
    method: str | None = None,
    type: str | None = None,
    tolerance: str | None = None,
    point1: str | None = None,
    point2: str | None = None,
    point3: str | None = None,
    point4: str | None = None,
    point5: str | None = None,
    point6: str | None = None,
    point7: str | None = None,
) -> httpx.Response:
    """
    Calibration for SingleUserLin.

    Route: ``POST /rw/motionsystem/mechunits/{mechunit}/calib``

    Args:
        client: Open RWSClient instance.
        mechunit: URL path parameter.
        method: Body parameter. Optional.
        type: Body parameter. Optional.
        tolerance: Body parameter. Optional.
        point1: Body parameter. Optional.
        point2: Body parameter. Optional.
        point3: Body parameter. Optional.
        point4: Body parameter. Optional.
        point5: Body parameter. Optional.
        point6: Body parameter. Optional.
        point7: Body parameter. Optional.

    Returns:
        Raw HTTP response. Expected success: HTTP 200.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: BAD_REQUEST (400) ,See

    Example:
        ```python
        >>> await post_calibration_for_singleuserlin(client, "mechunit_value")
        <Response [200]>
        ```
    """
    return await client.post(
        f"/rw/motionsystem/mechunits/{mechunit}/calib",
        data={
            k: v
            for k, v in {
                "method": method,
                "type": type,
                "tolerance": tolerance,
                "point1": point1,
                "point2": point2,
                "point3": point3,
                "point4": point4,
                "point5": point5,
                "point6": point6,
                "point7": point7
            }.items()
            if v is not None
        },
    )

async def post_calibration_for_singletrack(
    client: RWSClient,
    mechunit: str,
    method: str | None = None,
    type: str | None = None,
    point1: str | None = None,
    point2: str | None = None,
    point3: str | None = None,
    point4: str | None = None,
    point5: str | None = None,
    point6: str | None = None,
    point7: str | None = None,
    point8: str | None = None,
) -> httpx.Response:
    """
    Calibration for SingleTrack.

    Route: ``POST /rw/motionsystem/mechunits/{mechunit}/calib``

    Args:
        client: Open RWSClient instance.
        mechunit: URL path parameter.
        method: Body parameter. Optional.
        type: Body parameter. Optional.
        point1: Body parameter. Optional.
        point2: Body parameter. Optional.
        point3: Body parameter. Optional.
        point4: Body parameter. Optional.
        point5: Body parameter. Optional.
        point6: Body parameter. Optional.
        point7: Body parameter. Optional.
        point8: Body parameter. Optional.

    Returns:
        Raw HTTP response. Expected success: HTTP 200.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: BAD_REQUEST (400) ,See

    Example:
        ```python
        >>> await post_calibration_for_singletrack(client, "mechunit_value")
        <Response [200]>
        ```
    """
    return await client.post(
        f"/rw/motionsystem/mechunits/{mechunit}/calib",
        data={
            k: v
            for k, v in {
                "method": method,
                "type": type,
                "point1": point1,
                "point2": point2,
                "point3": point3,
                "point4": point4,
                "point5": point5,
                "point6": point6,
                "point7": point7,
                "point8": point8
            }.items()
            if v is not None
        },
    )

async def post_calibration_for_robotaxisrot2(
    client: RWSClient,
    mechunit: str,
    method: str | None = None,
    type: str | None = None,
    tolerance: str | None = None,
    axis: str | None = None,
    point1: str | None = None,
    point2: str | None = None,
    point3: str | None = None,
    point4: str | None = None,
    point5: str | None = None,
    point6: str | None = None,
) -> httpx.Response:
    """
    Calibration for RobotAxisRot2.

    Route: ``POST /rw/motionsystem/mechunits/{mechunit}/calib``

    Args:
        client: Open RWSClient instance.
        mechunit: URL path parameter.
        method: Body parameter. Optional.
        type: Body parameter. Optional.
        tolerance: Body parameter. Optional.
        axis: Body parameter. Optional.
        point1: Body parameter. Optional.
        point2: Body parameter. Optional.
        point3: Body parameter. Optional.
        point4: Body parameter. Optional.
        point5: Body parameter. Optional.
        point6: Body parameter. Optional.

    Returns:
        Raw HTTP response. Expected success: HTTP 200.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: BAD_REQUEST(400)

    Example:
        ```python
        >>> await post_calibration_for_robotaxisrot2(client, "mechunit_value")
        <Response [200]>
        ```
    """
    return await client.post(
        f"/rw/motionsystem/mechunits/{mechunit}/calib",
        data={
            k: v
            for k, v in {
                "method": method,
                "type": type,
                "tolerance": tolerance,
                "axis": axis,
                "point1": point1,
                "point2": point2,
                "point3": point3,
                "point4": point4,
                "point5": point5,
                "point6": point6
            }.items()
            if v is not None
        },
    )

async def get_base_frame(
    client: RWSClient,
    mechunit: str,
) -> httpx.Response:
    """
    Get Base Frame.

    Route: ``GET /rw/motionsystem/mechunits/{mechunit}/baseframe``

    Args:
        client: Open RWSClient instance.
        mechunit: URL path parameter.

    Returns:
        Raw HTTP response. Expected success: HTTP 200.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: BAD_REQUEST(400), UNSUPPORTED_MEDIA(415)

    Example:
        ```python
        >>> await get_base_frame(client, "mechunit_value")
        <Response [200]>
        ```
    """
    return await client.get(f"/rw/motionsystem/mechunits/{mechunit}/baseframe")

async def get_base_frame_actions(
    client: RWSClient,
    mechunit: str,
    action: str | None = None,
) -> httpx.Response:
    """
    Get Base Frame actions.

    Route: ``GET /rw/motionsystem/mechunits/{mechunit}/baseframe``

    Args:
        client: Open RWSClient instance.
        mechunit: URL path parameter.
        action: Query parameter. Optional.

    Returns:
        Raw HTTP response. Expected success: HTTP 200.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: BAD_REQUEST(400)

    Example:
        ```python
        >>> await get_base_frame_actions(client, "mechunit_value")
        <Response [200]>
        ```
    """
    return await client.get(
        f"/rw/motionsystem/mechunits/{mechunit}/baseframe",
        params={k: v for k, v in {"action": action}.items() if v is not None},
    )

async def set_base_frame(
    client: RWSClient,
    mechunit: str,
    action: str | None = None,
    x: str | None = None,
    y: str | None = None,
    z: str | None = None,
    q1: str | None = None,
    q2: str | None = None,
    q3: str | None = None,
    q4: str | None = None,
) -> httpx.Response:
    """
    Set Base Frame.

    Route: ``POST /rw/motionsystem/mechunits/{mechunit}/baseframe``

    Args:
        client: Open RWSClient instance.
        mechunit: URL path parameter.
        action: Query parameter. Optional.
        x: Body parameter. Optional.
        y: Body parameter. Optional.
        z: Body parameter. Optional.
        q1: Body parameter. Optional.
        q2: Body parameter. Optional.
        q3: Body parameter. Optional.
        q4: Body parameter. Optional.

    Returns:
        Raw HTTP response. Expected success: HTTP 202.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: BAD_REQUEST(400), Forbidden(403)

    Example:
        ```python
        >>> await set_base_frame(client, "mechunit_value")
        <Response [202]>
        ```
    """
    return await client.post(
        f"/rw/motionsystem/mechunits/{mechunit}/baseframe",
        params={k: v for k, v in {"action": action}.items() if v is not None},
        data={
            k: v
            for k, v in {
                "x": x,
                "y": y,
                "z": z,
                "q1": q1,
                "q2": q2,
                "q3": q3,
                "q4": q4
            }.items()
            if v is not None
        },
    )

async def get_axes(
    client: RWSClient,
    mechunit: str,
) -> httpx.Response:
    """
    Get Axes.

    Route: ``GET /rw/motionsystem/mechunits/{mechunit}/axes``

    Args:
        client: Open RWSClient instance.
        mechunit: URL path parameter.

    Returns:
        Raw HTTP response. Expected success: HTTP 200.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: BAD_REQUEST(400)

    Example:
        ```python
        >>> await get_axes(client, "mechunit_value")
        <Response [200]>
        ```
    """
    return await client.get(f"/rw/motionsystem/mechunits/{mechunit}/axes")

async def get_axis(
    client: RWSClient,
    mechunit: str,
    axis_num: str,
) -> httpx.Response:
    """
    Get Axis.

    Route: ``GET /rw/motionsystem/mechunits/{mechunit}/axes/{axis_num}``

    Args:
        client: Open RWSClient instance.
        mechunit: URL path parameter.
        axis_num: URL path parameter.

    Returns:
        Raw HTTP response. Expected success: HTTP 200.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: BAD_REQUEST(400)

    Example:
        ```python
        >>> await get_axis(client, "mechunit_value", "axis_num_value")
        <Response [200]>
        ```
    """
    return await client.get(f"/rw/motionsystem/mechunits/{mechunit}/axes/{axis_num}")

async def get_axis_actions(
    client: RWSClient,
    mechunit: str,
    axis_num: str,
    action: str | None = None,
) -> httpx.Response:
    """
    Get Axis actions.

    Route: ``GET /rw/motionsystem/mechunits/{mechunit}/axes/{axis_num}``

    Args:
        client: Open RWSClient instance.
        mechunit: URL path parameter.
        axis_num: URL path parameter.
        action: Query parameter. Optional.

    Returns:
        Raw HTTP response. Expected success: HTTP 200.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: BAD_REQUEST(400)

    Example:
        ```python
        >>> await get_axis_actions(client, "mechunit_value", "axis_num_value")
        <Response [200]>
        ```
    """
    return await client.get(
        f"/rw/motionsystem/mechunits/{mechunit}/axes/{axis_num}",
        params={k: v for k, v in {"action": action}.items() if v is not None},
    )

async def get_axis_pose(
    client: RWSClient,
    mechunit: str,
    axis_num: str,
    resource: str | None = None,
) -> httpx.Response:
    """
    Get Axis pose.

    Route: ``GET /rw/motionsystem/mechunits/{mechunit}/axes/{axis_num}``

    Args:
        client: Open RWSClient instance.
        mechunit: URL path parameter.
        axis_num: URL path parameter.
        resource: Query parameter. Optional.

    Returns:
        Raw HTTP response. Expected success: HTTP 200.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: BAD_REQUEST(400)

    Example:
        ```python
        >>> await get_axis_pose(client, "mechunit_value", "axis_num_value")
        <Response [200]>
        ```
    """
    return await client.get(
        f"/rw/motionsystem/mechunits/{mechunit}/axes/{axis_num}",
        params={k: v for k, v in {"resource": resource}.items() if v is not None},
    )

async def set_axis_pose(
    client: RWSClient,
    mechunit: str,
    axis_num: str,
    action: str | None = None,
    x: str | None = None,
    y: str | None = None,
    z: str | None = None,
    q1: str | None = None,
    q2: str | None = None,
    q3: str | None = None,
    q4: str | None = None,
) -> httpx.Response:
    """
    Set Axis pose.

    Route: ``POST /rw/motionsystem/mechunits/{mechunit}/axes/{axis_num}``

    Args:
        client: Open RWSClient instance.
        mechunit: URL path parameter.
        axis_num: URL path parameter.
        action: Query parameter. Optional.
        x: Body parameter. Optional.
        y: Body parameter. Optional.
        z: Body parameter. Optional.
        q1: Body parameter. Optional.
        q2: Body parameter. Optional.
        q3: Body parameter. Optional.
        q4: Body parameter. Optional.

    Returns:
        Raw HTTP response. Expected success: HTTP 204.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: BAD_REQUEST(400), NOT_FOUND(404), FORBIDDEN(403)

    Example:
        ```python
        >>> await set_axis_pose(client, "mechunit_value", "axis_num_value")
        <Response [204]>
        ```
    """
    return await client.post(
        f"/rw/motionsystem/mechunits/{mechunit}/axes/{axis_num}",
        params={k: v for k, v in {"action": action}.items() if v is not None},
        data={
            k: v
            for k, v in {
                "x": x,
                "y": y,
                "z": z,
                "q1": q1,
                "q2": q2,
                "q3": q3,
                "q4": q4
            }.items()
            if v is not None
        },
    )

async def update_commutate(
    client: RWSClient,
    mechunit: str,
    axis_num: str,
    action: str | None = None,
) -> httpx.Response:
    """
    Update Commutate.

    Route: ``POST /rw/motionsystem/mechunits/{mechunit}/axes/{axis_num}``

    Args:
        client: Open RWSClient instance.
        mechunit: URL path parameter.
        axis_num: URL path parameter.
        action: Query parameter. Optional.

    Returns:
        Raw HTTP response. Expected success: HTTP 204.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: BAD_REQUEST(400)

    Example:
        ```python
        >>> await update_commutate(client, "mechunit_value", "axis_num_value")
        <Response [204]>
        ```
    """
    return await client.post(
        f"/rw/motionsystem/mechunits/{mechunit}/axes/{axis_num}",
        params={k: v for k, v in {"action": action}.items() if v is not None},
        data={},
    )

async def update_sync_revolution_counter(
    client: RWSClient,
    mechunit: str,
    axis_num: str,
    action: str | None = None,
    synctype: str | None = None,
) -> httpx.Response:
    """
    Update Sync Revolution Counter.

    Route: ``POST /rw/motionsystem/mechunits/{mechunit}/axes/{axis_num}``

    Args:
        client: Open RWSClient instance.
        mechunit: URL path parameter.
        axis_num: URL path parameter.
        action: Query parameter. Optional.
        synctype: Body parameter. Optional.

    Returns:
        Raw HTTP response. Expected success: HTTP 204.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: BAD_REQUEST(400)

    Example:
        ```python
        >>> await update_sync_revolution_counter(client, "mechunit_value", "axis_num_value")
        <Response [204]>
        ```
    """
    return await client.post(
        f"/rw/motionsystem/mechunits/{mechunit}/axes/{axis_num}",
        params={k: v for k, v in {"action": action}.items() if v is not None},
        data={k: v for k, v in {"synctype": synctype}.items() if v is not None},
    )

async def set_smb_data(
    client: RWSClient,
    mechunit: str,
    action: str | None = None,
    type: str | None = None,
) -> httpx.Response:
    """
    Set SMB Data.

    Route: ``POST /rw/motionsystem/mechunits/{mechunit}/smbdata``

    Args:
        client: Open RWSClient instance.
        mechunit: URL path parameter.
        action: Query parameter. Optional.
        type: Body parameter. Optional.

    Returns:
        Raw HTTP response. Expected success: HTTP 204.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.

    Example:
        ```python
        >>> await set_smb_data(client, "mechunit_value")
        <Response [204]>
        ```
    """
    return await client.post(
        f"/rw/motionsystem/mechunits/{mechunit}/smbdata",
        params={k: v for k, v in {"action": action}.items() if v is not None},
        data={k: v for k, v in {"type": type}.items() if v is not None},
    )

async def post_clear_smb_data(
    client: RWSClient,
    mechunit: str,
    action: str | None = None,
    type: str | None = None,
) -> httpx.Response:
    """
    Clear SMB Data.

    Route: ``POST /rw/motionsystem/mechunits/{mechunit}/smbdata``

    Args:
        client: Open RWSClient instance.
        mechunit: URL path parameter.
        action: Query parameter. Optional.
        type: Body parameter. Optional.

    Returns:
        Raw HTTP response. Expected success: HTTP 204.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.

    Example:
        ```python
        >>> await post_clear_smb_data(client, "mechunit_value")
        <Response [204]>
        ```
    """
    return await client.post(
        f"/rw/motionsystem/mechunits/{mechunit}/smbdata",
        params={k: v for k, v in {"action": action}.items() if v is not None},
        data={k: v for k, v in {"type": type}.items() if v is not None},
    )

async def get_smb_data(
    client: RWSClient,
    mechunit: str,
) -> httpx.Response:
    """
    Get SMB Data.

    Route: ``GET /rw/motionsystem/mechunits/{mechunit}/smbdata``

    Args:
        client: Open RWSClient instance.
        mechunit: URL path parameter.

    Returns:
        Raw HTTP response. Expected success: HTTP 200.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.

    Example:
        ```python
        >>> await get_smb_data(client, "mechunit_value")
        <Response [200]>
        ```
    """
    return await client.get(f"/rw/motionsystem/mechunits/{mechunit}/smbdata")

async def get_smb_data_actions(
    client: RWSClient,
    mechunit: str,
    action: str | None = None,
) -> httpx.Response:
    """
    Get SMB Data Actions.

    Route: ``GET /rw/motionsystem/mechunits/{mechunit}/smbdata``

    Args:
        client: Open RWSClient instance.
        mechunit: URL path parameter.
        action: Query parameter. Optional.

    Returns:
        Raw HTTP response. Expected success: HTTP 200.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: BAD_REQUEST(400)

    Example:
        ```python
        >>> await get_smb_data_actions(client, "mechunit_value")
        <Response [200]>
        ```
    """
    return await client.get(
        f"/rw/motionsystem/mechunits/{mechunit}/smbdata",
        params={k: v for k, v in {"action": action}.items() if v is not None},
    )

async def get_motor_calib_names(
    client: RWSClient,
    mechunit: str,
) -> httpx.Response:
    """
    Get Motor Calib Names.

    Route: ``GET /rw/motionsystem/mechunits/{mechunit}/motorcalib``

    Args:
        client: Open RWSClient instance.
        mechunit: URL path parameter.

    Returns:
        Raw HTTP response. Expected success: HTTP 200.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: NOT_FOUND(404), BAD_REQUEST(400)

    Example:
        ```python
        >>> await get_motor_calib_names(client, "mechunit_value")
        <Response [200]>
        ```
    """
    return await client.get(f"/rw/motionsystem/mechunits/{mechunit}/motorcalib")
