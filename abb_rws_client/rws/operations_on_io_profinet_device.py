# This file is AUTO-GENERATED by contrib/generator/main.py
# DO NOT EDIT MANUALLY — run the generator to regenerate.
"""
RWS module : Operations on IO Profinet Device → Get profinet I/O device read record implicit data from device in profinet network

Miroir 1:1 de l'API REST ABB RobotWare 6.
Chaque fonction correspond à exactement un endpoint HTTP.
Aucune logique composée — voir highlevel/ pour les wrappers.
"""
from __future__ import annotations

import httpx

from abb_rws_client._core.client import RWSClient


async def get_profinet_device_read_record_implicit_data(
    client: RWSClient,
    network: str,
    device: str,
    slot: str | None = None,
    subslot: str | None = None,
    index: str | None = None,
    datalength: str | None = None,
    vendorid: str | None = None,
    deviceid: str | None = None,
) -> httpx.Response:
    """
    Get profinet I/O device read record implicit data from device in profinet network.

    Route: ``GET rw/iosystem/devices/{network}/{device}/implicitdata``
    Contraintes ABB: Not supported in bootserver mode.

    Args:
        client: Instance RWSClient ouverte.
        network: Paramètre de chemin URL.
        device: Paramètre de chemin URL.
        slot: Paramètre query. Optionnel.
        subslot: Paramètre query. Optionnel.
        index: Paramètre query. Optionnel.
        datalength: Paramètre query. Optionnel.
        vendorid: Paramètre query. Optionnel.
        deviceid: Paramètre query. Optionnel.

    Returns:
        Réponse HTTP brute. Succès attendu : HTTP 200.

    Raises:
        RWSAuthenticationError: Sur HTTP 401.
        RWSNotFoundError: Sur HTTP 404.
        RWSHTTPError: Sur tout autre HTTP >= 400.
        # Codes ABB: Bad Request(406)

    Example:
        # curl --digest -u "Default User":robotics "http://localhost/rw/iosystem/devices/{profinet}/{pnet}/implicitdata"
    """
    return await client.get(f"rw/iosystem/devices/{network}/{device}/implicitdata", params={k: v for k, v in {"slot": slot, "subslot": subslot, "index": index, "datalength": datalength, "vendorid": vendorid, "deviceid": deviceid}.items() if v is not None})

async def post_read_record_implicit_data_from_device_in(
    client: RWSClient,
    network: str,
    device: str,
    slot: str | None = None,
    subslot: str | None = None,
    index: str | None = None,
    datalength: str | None = None,
    vendorid: str | None = None,
    deviceid: str | None = None,
    ip: str | None = None,
) -> httpx.Response:
    """
    Read record implicit data from device in profinet network.

    Route: ``POST rw/iosystem/devices/{network}/{device}/implicitdata``
    Contraintes ABB: Not supported in bootserver mode.

    Args:
        client: Instance RWSClient ouverte.
        network: Paramètre de chemin URL.
        device: Paramètre de chemin URL.
        slot: Paramètre body. Optionnel.
        subslot: Paramètre body. Optionnel.
        index: Paramètre body. Optionnel.
        datalength: Paramètre body. Optionnel.
        vendorid: Paramètre body. Optionnel.
        deviceid: Paramètre body. Optionnel.
        ip: Paramètre body. Optionnel.

    Returns:
        Réponse HTTP brute. Succès attendu : HTTP 200.

    Raises:
        RWSAuthenticationError: Sur HTTP 401.
        RWSNotFoundError: Sur HTTP 404.
        RWSHTTPError: Sur tout autre HTTP >= 400.
        # Codes ABB: NOT_ACCEPTABLE(406)

    Example:
        # curl --digest -u "Default User":robotics -d "slot=1&subslot=2&index=2&datalength=60&vendorid=42&deviceid=787&ip=127.1.1.
    """
    return await client.post(f"rw/iosystem/devices/{network}/{device}/implicitdata", data={k: v for k, v in {"slot": slot, "subslot": subslot, "index": index, "datalength": datalength, "vendorid": vendorid, "deviceid": deviceid, "ip": ip}.items() if v is not None})

async def get_forms(
    client: RWSClient,
    network: str,
    device: str,
) -> httpx.Response:
    """
    Get forms.

    Route: ``OPTIONS rw/iosystem/devices/{network}/{device}/implicitdata``
    Contraintes ABB: Not supported in bootserver mode.

    Args:
        client: Instance RWSClient ouverte.
        network: Paramètre de chemin URL.
        device: Paramètre de chemin URL.

    Returns:
        Réponse HTTP brute. Succès attendu : HTTP 200.

    Raises:
        RWSAuthenticationError: Sur HTTP 401.
        RWSNotFoundError: Sur HTTP 404.
        RWSHTTPError: Sur tout autre HTTP >= 400.
        # Codes ABB: NOT_ACCEPTABLE(406) See

    Example:
        # curl --digest -u "Default User":robotics -X OPTIONS "http://localhost/rw/iosystem/devices/PROFINET/PN_Internal_Device/im
    """
    return await client.options(f"rw/iosystem/devices/{network}/{device}/implicitdata")

async def get_profinet_device_read_record_data(
    client: RWSClient,
    network: str,
    device: str,
    slot: str | None = None,
    subslot: str | None = None,
    index: str | None = None,
    datalength: str | None = None,
) -> httpx.Response:
    """
    Get profinet I/O device read record data.

    Route: ``GET rw/iosystem/devices/{network}/{device}/explicitdata``
    Contraintes ABB: Not supported in bootserver mode

    Args:
        client: Instance RWSClient ouverte.
        network: Paramètre de chemin URL.
        device: Paramètre de chemin URL.
        slot: Paramètre query. Optionnel.
        subslot: Paramètre query. Optionnel.
        index: Paramètre query. Optionnel.
        datalength: Paramètre query. Optionnel.

    Returns:
        Réponse HTTP brute. Succès attendu : HTTP 200.

    Raises:
        RWSAuthenticationError: Sur HTTP 401.
        RWSNotFoundError: Sur HTTP 404.
        RWSHTTPError: Sur tout autre HTTP >= 400.
        # Codes ABB: NOT_ACCEPTABLE(406)

    Example:
        # curl --digest -u "Default User":robotics "http://localhost/rw/iosystem/devices/PROFINET/PN_Internal_Device/explicitdata"
    """
    return await client.get(f"rw/iosystem/devices/{network}/{device}/explicitdata", params={k: v for k, v in {"slot": slot, "subslot": subslot, "index": index, "datalength": datalength}.items() if v is not None})

async def get_profinet_device_read_record_data(
    client: RWSClient,
    network: str,
    device: str,
    slot: str | None = None,
    subslot: str | None = None,
    index: str | None = None,
    datalength: str | None = None,
) -> httpx.Response:
    """
    Get profinet I/O device read record data.

    Route: ``POST rw/iosystem/devices/{network}/{device}/explicitdata``
    Contraintes ABB: Not supported in bootserver mode.

    Args:
        client: Instance RWSClient ouverte.
        network: Paramètre de chemin URL.
        device: Paramètre de chemin URL.
        slot: Paramètre body. Optionnel.
        subslot: Paramètre body. Optionnel.
        index: Paramètre body. Optionnel.
        datalength: Paramètre body. Optionnel.

    Returns:
        Réponse HTTP brute. Succès attendu : HTTP 200.

    Raises:
        RWSAuthenticationError: Sur HTTP 401.
        RWSNotFoundError: Sur HTTP 404.
        RWSHTTPError: Sur tout autre HTTP >= 400.
        # Codes ABB: NOT_ACCEPTABLE(406)

    Example:
        # curl --digest -u "Default User":robotics -d "slot=1&subslot=2&index=2&datalength=60" -X POST "http://localhost/rw/iosyst
    """
    return await client.post(f"rw/iosystem/devices/{network}/{device}/explicitdata", data={k: v for k, v in {"slot": slot, "subslot": subslot, "index": index, "datalength": datalength}.items() if v is not None})

async def get_forms(
    client: RWSClient,
    network: str,
    device: str,
) -> httpx.Response:
    """
    Get Forms.

    Route: ``OPTIONS rw/iosystem/devices/{network}/{device}/explicitdata``
    Contraintes ABB: Not supported in bootserver mode.

    Args:
        client: Instance RWSClient ouverte.
        network: Paramètre de chemin URL.
        device: Paramètre de chemin URL.

    Returns:
        Réponse HTTP brute. Succès attendu : HTTP 200.

    Raises:
        RWSAuthenticationError: Sur HTTP 401.
        RWSNotFoundError: Sur HTTP 404.
        RWSHTTPError: Sur tout autre HTTP >= 400.
        # Codes ABB: See

    Example:
        # curl --digest -u "Default User":robotics -X OPTIONS "http://localhost/rw/iosystem/devices/PROFINET/PN_Internal_Device/ex
    """
    return await client.options(f"rw/iosystem/devices/{network}/{device}/explicitdata")

async def get_profinet_device_alarms_xml_response(
    client: RWSClient,
    network: str,
    device: str,
) -> httpx.Response:
    """
    Get profinet device alarms xml response.

    Route: ``GET rw/iosystem/devices/{network}/{device}/alarms``
    Contraintes ABB: Not supported in bootserver mode

    Args:
        client: Instance RWSClient ouverte.
        network: Paramètre de chemin URL.
        device: Paramètre de chemin URL.

    Returns:
        Réponse HTTP brute. Succès attendu : HTTP 200.

    Raises:
        RWSAuthenticationError: Sur HTTP 401.
        RWSNotFoundError: Sur HTTP 404.
        RWSHTTPError: Sur tout autre HTTP >= 400.
        # Codes ABB: NOT_ACCEPTABLE(406)

    Example:
        # curl --digest -u "Default User":robotics "http://localhost/rw/iosystem/devices/PROFINET/PN_Internal_Device/alarms"
    """
    return await client.get(f"rw/iosystem/devices/{network}/{device}/alarms")

async def post_clear_the_alarms(
    client: RWSClient,
    network: str,
    device: str,
) -> httpx.Response:
    """
    Clear the alarms.

    Route: ``POST rw/iosystem/devices/{network}/{device}/alarms/clear``
    Contraintes ABB: Not supported in bootserver mode.

    Args:
        client: Instance RWSClient ouverte.
        network: Paramètre de chemin URL.
        device: Paramètre de chemin URL.

    Returns:
        Réponse HTTP brute. Succès attendu : HTTP 200.

    Raises:
        RWSAuthenticationError: Sur HTTP 401.
        RWSNotFoundError: Sur HTTP 404.
        RWSHTTPError: Sur tout autre HTTP >= 400.
        # Codes ABB: NOT_ACCEPTABLE(406)

    Example:
        # curl --digest -u "Default User":robotics -d POST "http://localhost/rw/iosystem/devices/PROFINET/PN_Internal_Device/alarm
    """
    return await client.post(f"rw/iosystem/devices/{network}/{device}/alarms/clear")

async def get_forms(
    client: RWSClient,
    network: str,
    device: str,
) -> httpx.Response:
    """
    Get Forms.

    Route: ``OPTIONS rw/iosystem/devices/{network}/{device}/alarms/clear``
    Contraintes ABB: Not supported in bootserver mode.

    Args:
        client: Instance RWSClient ouverte.
        network: Paramètre de chemin URL.
        device: Paramètre de chemin URL.

    Returns:
        Réponse HTTP brute. Succès attendu : HTTP 200.

    Raises:
        RWSAuthenticationError: Sur HTTP 401.
        RWSNotFoundError: Sur HTTP 404.
        RWSHTTPError: Sur tout autre HTTP >= 400.
        # Codes ABB: NOT_ACCEPTABLE(406) See

    Example:
        # curl --digest -u "Default User":robotics -X OPTIONS "http://localhost/rw/iosystem/devices/PROFINET/PN_Internal_Device/al
    """
    return await client.options(f"rw/iosystem/devices/{network}/{device}/alarms/clear")
