# This file is AUTO-GENERATED by contrib/generator/main.py
# DO NOT EDIT MANUALLY — run the generator to regenerate.
#  Generator author: Clement RACINET
"""
RWS module: File Service → Get File Service Resources

1:1 mirror of the ABB RobotWare 6 REST API.
Each function maps to exactly one HTTP endpoint.
No composed logic — see highlevel/ for wrappers.
"""
from __future__ import annotations

import httpx

from abb_rws_client._core.client import RWSClient


async def get_file_service_resources(client: RWSClient) -> httpx.Response:
    """
    Get File Service Resources.

    Route: ``GET /fileservice``
    ABB constraints: Though root supports environment variables these are not listed in the
        returned response. Environment variables are only allowed directly under the root URI
        i.e. /fileservice/$home Supported in bootserver mode

    Args:
        client: Open RWSClient instance.

    Returns:
        Raw HTTP response. Expected success: HTTP 200.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: UNAUTHORIZED(401),NOT_FOUND(404)

    Example:
        ```python
        >>> await get_file_service_resources(client)
        <Response [200]>
        ```
    """
    return await client.get("/fileservice")

async def get_directory_listing_of_resources(
    client: RWSClient,
    environment_variable_device: str,
    directory: str,
) -> httpx.Response:
    """
    Get Directory listing of resources.

    Route: ``GET /fileservice/{environment_variable|device}/{directory}``
    ABB constraints: Supported in bootserver mode

    Args:
        client: Open RWSClient instance.
        environment_variable_device: URL path parameter.
        directory: URL path parameter.

    Returns:
        Raw HTTP response. Expected success: HTTP 200.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: UNAUTHORIZED(401), NOT_FOUND(404), BAD_REQUEST(400) See

    Example:
        ```python
        >>> await get_directory_listing_of_resources(
        ...     client,
        ...     "environment_variable_device_value",
        ...     "directory_value"
        ... )
        <Response [200]>
        ```
    """
    return await client.get(f"/fileservice/{environment_variable_device}/{directory}")

async def get_directory_actions(
    client: RWSClient,
    device: str,
    directory: str,
    action: str | None = None,
) -> httpx.Response:
    """
    Get Directory Actions.

    Route: ``GET /fileservice/{device}|{directory}``
    ABB constraints: Supported in bootserver mode

    Args:
        client: Open RWSClient instance.
        device: URL path parameter.
        directory: URL path parameter.
        action: Query parameter. Optional.

    Returns:
        Raw HTTP response. Expected success: HTTP 200.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: BAD_REQUEST(400) See

    Example:
        ```python
        >>> await get_directory_actions(client, "device_value", "directory_value")
        <Response [200]>
        ```
    """
    return await client.get(
        f"/fileservice/{device}|{directory}",
        params={k: v for k, v in {"action": action}.items() if v is not None},
    )

async def create_directory(
    client: RWSClient,
    device_environment_variable: str,
    directory: str,
) -> httpx.Response:
    """
    Create a directory.

    Route: ``POST /fileservice/{device|environment_variable}/{directory}``
    ABB constraints: Only relative path are allowed for "*fs-newname*". Absolute paths are not
        supported The fs-newname can take nested directory structure e.g.
        fs-newname=parentdir/subdir will create both the directories if they don't exist i.e.
        subdir under parentdir Supported in bootserver mode

    Args:
        client: Open RWSClient instance.
        device_environment_variable: URL path parameter.
        directory: URL path parameter.

    Returns:
        Raw HTTP response. Expected success: HTTP 201.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: UNAUTHORIZED(401), BAD_REQUEST(400), UNSUPPORTED_MEDIA(415),

    Example:
        ```python
        >>> await create_directory(
        ...     client,
        ...     "device_environment_variable_value",
        ...     "directory_value"
        ... )
        <Response [201]>
        ```
    """
    return await client.post(f"/fileservice/{device_environment_variable}/{directory}")

async def post_rename_directory(
    client: RWSClient,
    device_environment_variable: str,
    directory: str,
) -> httpx.Response:
    """
    Rename a directory.

    Route: ``POST /fileservice/{device|environment_variable}/{directory}``
    ABB constraints: Supported in bootserver mode

    Args:
        client: Open RWSClient instance.
        device_environment_variable: URL path parameter.
        directory: URL path parameter.

    Returns:
        Raw HTTP response. Expected success: HTTP 200.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: UNAUTHORIZED(401), NOT_FOUND(404), BAD_REQUEST(400), UNSUPPORTED_MEDIA(415),
            METHOD_NOT_ALLOWED(405)

    Example:
        ```python
        >>> await post_rename_directory(
        ...     client,
        ...     "device_environment_variable_value",
        ...     "directory_value"
        ... )
        <Response [200]>
        ```
    """
    return await client.post(f"/fileservice/{device_environment_variable}/{directory}")

async def post_copy_directory(
    client: RWSClient,
    device_environment_variable: str,
    directory: str,
) -> httpx.Response:
    """
    Copy a directory.

    Route: ``POST /fileservice/{device|environment_variable}/{directory}``
    ABB constraints: The value for fs-newname can either be absolute or relative. It is
        differentiated based on whether the value has a leading slash (absolute) or not
        (relative). Note: when absolute paths are used, it should start from /fileservice e.g.
        Absolute path : fs-newname=/fileservice/$home/copydir2 Relative...

    Args:
        client: Open RWSClient instance.
        device_environment_variable: URL path parameter.
        directory: URL path parameter.

    Returns:
        Raw HTTP response. Expected success: HTTP 204.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: UNAUTHORIZED(401), NOT_FOUND(404), BAD_REQUEST(400), UNSUPPORTED_MEDIA(415),
            CONFLICT(409)

    Example:
        ```python
        >>> await post_copy_directory(
        ...     client,
        ...     "device_environment_variable_value",
        ...     "directory_value"
        ... )
        <Response [204]>
        ```
    """
    return await client.post(f"/fileservice/{device_environment_variable}/{directory}")

async def delete_directory(
    client: RWSClient,
    device_environment_variable: str,
    directory: str,
) -> httpx.Response:
    """
    Delete a directory.

    Route: ``DELETE /fileservice/{device|environment_variable}/{directory}``
    ABB constraints: Supported in bootserver mode

    Args:
        client: Open RWSClient instance.
        device_environment_variable: URL path parameter.
        directory: URL path parameter.

    Returns:
        Raw HTTP response. Expected success: HTTP 204.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: UNAUTHORIZED(401), NOT_FOUND(404), BAD_REQUEST(400), METHOD_NOT_ALLOWED(405)

    Example:
        ```python
        >>> await delete_directory(
        ...     client,
        ...     "device_environment_variable_value",
        ...     "directory_value"
        ... )
        <Response [204]>
        ```
    """
    return await client.delete(f"/fileservice/{device_environment_variable}/{directory}")

async def get_file(
    client: RWSClient,
    device: str,
    directory: str,
    file: str,
) -> httpx.Response:
    """
    Get a file.

    Route: ``GET /fileservice/{device}|{directory}/{file}``
    ABB constraints: Supported in bootserver mode

    Args:
        client: Open RWSClient instance.
        device: URL path parameter.
        directory: URL path parameter.
        file: URL path parameter.

    Returns:
        Raw HTTP response. Expected success: HTTP 200.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: UNAUTHORIZED(401), NOT_FOUND(404),

    Example:
        ```python
        >>> await get_file(client, "device_value", "directory_value", "file_value")
        <Response [200]>
        ```
    """
    return await client.get(f"/fileservice/{device}|{directory}/{file}")

async def get_file_actions(
    client: RWSClient,
    device: str,
    directory: str,
    file: str,
    action: str | None = None,
) -> httpx.Response:
    """
    Get File Actions.

    Route: ``GET /fileservice/{device}|{directory}/{file}``
    ABB constraints: Supported in bootserver mode

    Args:
        client: Open RWSClient instance.
        device: URL path parameter.
        directory: URL path parameter.
        file: URL path parameter.
        action: Query parameter. Optional.

    Returns:
        Raw HTTP response. Expected success: HTTP 200.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: UNAUTHORIZED(401), NOT_FOUND(404), See

    Example:
        ```python
        >>> await get_file_actions(client, "device_value", "directory_value", "file_value")
        <Response [200]>
        ```
    """
    return await client.get(
        f"/fileservice/{device}|{directory}/{file}",
        params={k: v for k, v in {"action": action}.items() if v is not None},
    )

async def post_rename_file(
    client: RWSClient,
    device_environment_variable_directory: str,
    file: str,
) -> httpx.Response:
    """
    Rename a file.

    Route: ``POST /fileservice/{device|environment_variable|directory}/{file}``
    ABB constraints: Supported in bootserver mode

    Args:
        client: Open RWSClient instance.
        device_environment_variable_directory: URL path parameter.
        file: URL path parameter.

    Returns:
        Raw HTTP response. Expected success: HTTP 204.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: UNAUTHORIZED(401), NOT_FOUND(404), BAD_REQUEST(400), UNSUPPORTED_MEDIA(415)

    Example:
        ```python
        >>> await post_rename_file(
        ...     client,
        ...     "device_environment_variable_directory_value",
        ...     "file_value"
        ... )
        <Response [204]>
        ```
    """
    return await client.post(f"/fileservice/{device_environment_variable_directory}/{file}")

async def create_copy_of_file(
    client: RWSClient,
    device_environment_variable: str,
    filename: str,
) -> httpx.Response:
    """
    Create a copy of a file.

    Route: ``POST /fileservice/{device|environment_variable}/{filename}``
    ABB constraints: The value for fs-newname can either be absolute or relative. It is
        differentiated based on the whether the value has a leading slash (absolute) or not
        (relative). Note: when absolute paths are used, it should start from /fileservice e.g.
        Absolute path : fs-newname=/fileservice/$home/copyfile2.txt...

    Args:
        client: Open RWSClient instance.
        device_environment_variable: URL path parameter.
        filename: URL path parameter.

    Returns:
        Raw HTTP response. Expected success: HTTP 204.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: UNAUTHORIZED(401), NOT_FOUND(404), BAD_REQUEST(400), UNSUPPORTED_MEDIA(415),
            CONFLICT(409)

    Example:
        ```python
        >>> await create_copy_of_file(
        ...     client,
        ...     "device_environment_variable_value",
        ...     "filename_value"
        ... )
        <Response [204]>
        ```
    """
    return await client.post(f"/fileservice/{device_environment_variable}/{filename}")

async def put_upload_file(
    client: RWSClient,
    device_environment_variable_directory: str,
    file: str,
) -> httpx.Response:
    """
    Upload a file.

    Route: ``PUT /fileservice/{device|environment_variable|directory}/{file}``
    ABB constraints: If the file exists, the file is overwritten with the specified content else
        a new file with the given name is created. Supported in bootserver mode Maximum supported
        file size is less than 800MB.

    Args:
        client: Open RWSClient instance.
        device_environment_variable_directory: URL path parameter.
        file: URL path parameter.

    Returns:
        Raw HTTP response. Expected success: HTTP 200.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: UNAUTHORIZED(401), NOT_FOUND(404), FORBIDDEN(403), BAD_REQUEST(400)

    Example:
        ```python
        >>> await put_upload_file(
        ...     client,
        ...     "device_environment_variable_directory_value",
        ...     "file_value"
        ... )
        <Response [200]>
        ```
    """
    return await client.put(f"/fileservice/{device_environment_variable_directory}/{file}")

async def delete_file(
    client: RWSClient,
    device_environment_variable_directory: str,
    file: str,
) -> httpx.Response:
    """
    Delete a file.

    Route: ``DELETE /fileservice/{device|environment_variable|directory}/{file}``
    ABB constraints: Supported in bootserver mode

    Args:
        client: Open RWSClient instance.
        device_environment_variable_directory: URL path parameter.
        file: URL path parameter.

    Returns:
        Raw HTTP response. Expected success: HTTP 204.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: UNAUTHORIZED(401), NOT_FOUND(404), BAD_REQUEST(400),METHOD_NOT_ALLOWED(405)

    Example:
        ```python
        >>> await delete_file(
        ...     client,
        ...     "device_environment_variable_directory_value",
        ...     "file_value"
        ... )
        <Response [204]>
        ```
    """
    return await client.delete(f"/fileservice/{device_environment_variable_directory}/{file}")

async def get_file_meta_data(
    client: RWSClient,
    device_environment_variable_directory: str,
    file: str,
) -> httpx.Response:
    """
    Get file Meta data.

    Route: ``HEAD /fileservice/{device|environment_variable|directory}/{file}``
    ABB constraints: Supported in bootserver mode

    Args:
        client: Open RWSClient instance.
        device_environment_variable_directory: URL path parameter.
        file: URL path parameter.

    Returns:
        Raw HTTP response. Expected success: HTTP 204.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: UNAUTHORIZED(401), NOT_FOUND(404), See

    Example:
        ```python
        >>> await get_file_meta_data(
        ...     client,
        ...     "device_environment_variable_directory_value",
        ...     "file_value"
        ... )
        <Response [204]>
        ```
    """
    return await client.head(f"/fileservice/{device_environment_variable_directory}/{file}")
