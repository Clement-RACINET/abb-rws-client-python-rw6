# This file is AUTO-GENERATED by contrib/generator/main.py
# DO NOT EDIT MANUALLY — run the generator to regenerate.
#  Generator author: Clement RACINET
"""
RWS module: Controller Service → Operations on Identity Resource → Get Identity Resource

1:1 mirror of the ABB RobotWare 6 REST API.
Each function maps to exactly one HTTP endpoint.
No composed logic — see highlevel/ for wrappers.
"""
from __future__ import annotations

import httpx

from abb_rws_client._core.client import RWSClient


async def get_identity_resource(client: RWSClient) -> httpx.Response:
    """
    Get Identity Resource.

    Route: ``GET /ctrl/identity``
    ABB constraints: Supported in bootserver mode

    Args:
        client: Open RWSClient instance.

    Returns:
        Raw HTTP response. Expected success: HTTP 200.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: NOT_FOUND(404) See

    Example:
        ```python
        >>> await get_identity_resource(client)
        <Response [200]>
        ```
    """
    return await client.get("/ctrl/identity")

async def get_identity_actions(
    client: RWSClient,
    action: str | None = None,
) -> httpx.Response:
    """
    Get Identity Actions.

    Route: ``GET /ctrl/identity``
    ABB constraints: Supported in bootserver mode

    Args:
        client: Open RWSClient instance.
        action: Query parameter. Optional.

    Returns:
        Raw HTTP response. Expected success: HTTP 200.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: NOT_FOUND(404) See

    Example:
        ```python
        >>> await get_identity_actions(client)
        <Response [200]>
        ```
    """
    return await client.get(
        "/ctrl/identity",
        params={k: v for k, v in {"action": action}.items() if v is not None},
    )

async def set_the_identity_of_the_controller(client: RWSClient) -> httpx.Response:
    """
    Set the Identity of the controller.

    Route: ``PUT /ctrl/identity``
    ABB constraints: Supported in bootserver mode

    Args:
        client: Open RWSClient instance.

    Returns:
        Raw HTTP response. Expected success: HTTP 204.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: UNAUTHORIZED(401), BAD_REQUEST(400), UNSUPPORTED_MEDIA(415)

    Example:
        ```python
        >>> await set_the_identity_of_the_controller(client)
        <Response [204]>
        ```
    """
    return await client.put(
        "/ctrl/identity",
        data={},
    )
