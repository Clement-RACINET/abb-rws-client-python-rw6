# This file is AUTO-GENERATED by contrib/generator/main.py
# DO NOT EDIT MANUALLY — run the generator to regenerate.
#  Generator author: Clement RACINET
"""
RWS module: RobotWare Services → IO Service → Operations on IO Signals

1:1 mirror of the ABB RobotWare 6 REST API.
Each function maps to exactly one HTTP endpoint.
No composed logic — see highlevel/ for wrappers.
"""
from __future__ import annotations

import httpx

from abb_rws_client._core.client import RWSClient


async def get_io_signals(client: RWSClient) -> httpx.Response:
    """
    Get IO Signals.

    Route: ``GET /rw/iosystem/signals``
    ABB constraints: Not supported in bootserver mode

    Args:
        client: Open RWSClient instance.

    Returns:
        Raw HTTP response. Expected success: HTTP 200.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: BAD REQUEST(400)

    Example:
        ```python
        >>> await get_io_signals(client)
        <Response [200]>
        ```
    """
    return await client.get("/rw/iosystem/signals")

async def post_signal_search(
    client: RWSClient,
    action: str | None = None,
    start: str | None = None,
    limit: str | None = None,
    name: str | None = None,
    device: str | None = None,
    network: str | None = None,
    category: str | None = None,
    category_pon: str | None = None,
    type: str | None = None,
    invert: str | None = None,
    blocked: str | None = None,
    name2: str | None = None,
    device2: str | None = None,
) -> httpx.Response:
    """
    Signal Search.

    Route: ``POST /rw/iosystem/signals``
    ABB constraints: Not supported in bootserver mode

    Args:
        client: Open RWSClient instance.
        action: Query parameter. Optional.
        start: Query parameter. Optional.
        limit: Query parameter. Optional.
        name: Body parameter. Optional.
        device: Body parameter. Optional.
        network: Body parameter. Optional.
        category: Body parameter. Optional.
        category_pon: Body parameter. Optional.
        type: Body parameter. Optional.
        invert: Body parameter. Optional.
        blocked: Body parameter. Optional.
        name2: Body parameter. Optional.
        device2: Body parameter. Optional.

    Returns:
        Raw HTTP response. Expected success: HTTP 200.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: Bad Request(400)

    Example:
        ```python
        >>> await post_signal_search(client)
        <Response [200]>
        ```
    """
    return await client.post(
        "/rw/iosystem/signals",
        params={
            k: v
            for k, v in {
                "action": action,
                "start": start,
                "limit": limit
            }.items()
            if v is not None
        },
        data={
            k: v
            for k, v in {
                "name": name,
                "device": device,
                "network": network,
                "category": category,
                "category_pon": category_pon,
                "type": type,
                "invert": invert,
                "blocked": blocked,
                "name2": name2,
                "device2": device2
            }.items()
            if v is not None
        },
    )

async def post_signal_search_extended(
    client: RWSClient,
    action: str | None = None,
    start: str | None = None,
    limit: str | None = None,
    name: str | None = None,
    device: str | None = None,
    network: str | None = None,
    category: str | None = None,
    category_pon: str | None = None,
    type: str | None = None,
    invert: str | None = None,
    blocked: str | None = None,
    name2: str | None = None,
    device2: str | None = None,
) -> httpx.Response:
    """
    Signal Search Extended.

    Route: ``POST /rw/iosystem/signals``
    ABB constraints: Not supported in bootserver mode

    Args:
        client: Open RWSClient instance.
        action: Query parameter. Optional.
        start: Query parameter. Optional.
        limit: Query parameter. Optional.
        name: Body parameter. Optional.
        device: Body parameter. Optional.
        network: Body parameter. Optional.
        category: Body parameter. Optional.
        category_pon: Body parameter. Optional.
        type: Body parameter. Optional.
        invert: Body parameter. Optional.
        blocked: Body parameter. Optional.
        name2: Body parameter. Optional.
        device2: Body parameter. Optional.

    Returns:
        Raw HTTP response. Expected success: HTTP 200.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: Bad Request(400)

    Example:
        ```python
        >>> await post_signal_search_extended(client)
        <Response [200]>
        ```
    """
    return await client.post(
        "/rw/iosystem/signals",
        params={
            k: v
            for k, v in {
                "action": action,
                "start": start,
                "limit": limit
            }.items()
            if v is not None
        },
        data={
            k: v
            for k, v in {
                "name": name,
                "device": device,
                "network": network,
                "category": category,
                "category_pon": category_pon,
                "type": type,
                "invert": invert,
                "blocked": blocked,
                "name2": name2,
                "device2": device2
            }.items()
            if v is not None
        },
    )

async def post_unblock_signals(
    client: RWSClient,
    action: str | None = None,
) -> httpx.Response:
    """
    Unblock Signals.

    Route: ``POST /rw/iosystem/signals``
    ABB constraints: Not supported in bootserver mode

    Args:
        client: Open RWSClient instance.
        action: Query parameter. Optional.

    Returns:
        Raw HTTP response. Expected success: HTTP 204.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: Bad Request(400), FORBIDDEN(403), UNAUTHORIZED(401)

    Example:
        ```python
        >>> await post_unblock_signals(client)
        <Response [204]>
        ```
    """
    return await client.post(
        "/rw/iosystem/signals",
        params={k: v for k, v in {"action": action}.items() if v is not None},
    )

async def get_io_signals_actions(
    client: RWSClient,
    action: str | None = None,
) -> httpx.Response:
    """
    Get IO Signals actions.

    Route: ``GET /rw/iosystem/signals``
    ABB constraints: Not supported in bootserver mode

    Args:
        client: Open RWSClient instance.
        action: Query parameter. Optional.

    Returns:
        Raw HTTP response. Expected success: HTTP 200.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: Bad Request(400)

    Example:
        ```python
        >>> await get_io_signals_actions(client)
        <Response [200]>
        ```
    """
    return await client.get(
        "/rw/iosystem/signals",
        params={k: v for k, v in {"action": action}.items() if v is not None},
    )
