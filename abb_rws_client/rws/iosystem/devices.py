# This file is AUTO-GENERATED by contrib/generator/main.py
# DO NOT EDIT MANUALLY — run the generator to regenerate.
#  Generator author: Clément RACINET
"""
RWS module: RobotWare Services → IO Service → Operations on IO Devices

1:1 mirror of the ABB RobotWare 6 REST API.
Each function maps to exactly one HTTP endpoint.
No composed logic — see highlevel/ for wrappers.
"""
from __future__ import annotations

import httpx

from abb_rws_client._core.client import RWSClient


async def get_io_devices(client: RWSClient) -> httpx.Response:
    """
    Get IO Devices.

    Route: ``GET /rw/iosystem/devices``
    ABB constraints: Not supported in bootserver mode

    Args:
        client: Open RWSClient instance.

    Returns:
        Raw HTTP response. Expected success: HTTP 200.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: BAD_REQUEST(400)

    Example:
        # curl --digest -u "Default User":robotics "http://localhost/rw/iosystem/devices"
    """
    return await client.get("/rw/iosystem/devices")

async def get_actions_io_devices(
    client: RWSClient,
    action: str | None = None,
) -> httpx.Response:
    """
    Get Actions IO Devices.

    Route: ``GET /rw/iosystem/devices``
    ABB constraints: Not supported in bootserver mode

    Args:
        client: Open RWSClient instance.
        action: Query parameter. Optional.

    Returns:
        Raw HTTP response. Expected success: HTTP 200.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: BAD_REQUEST(400)

    Example:
        # curl --digest -u "Default User":robotics
            "http://localhost/rw/iosystem/devices?action=show"
    """
    return await client.get(
        "/rw/iosystem/devices",
        params={k: v for k, v in {"action": action}.items() if v is not None},
    )

async def post_search_io_devices(
    client: RWSClient,
    action: str | None = None,
    name: str | None = None,
    lstate: str | None = None,
    network: str | None = None,
    example_name: str | None = None,
) -> httpx.Response:
    """
    Search IO Devices.

    Route: ``POST /rw/iosystem/devices``
    ABB constraints: Not supported in bootserver mode

    Args:
        client: Open RWSClient instance.
        action: Query parameter. Optional.
        name: Body parameter. Optional.
        lstate: Body parameter. Optional.
        network: Body parameter. Optional.
        example_name: Body parameter. Optional.

    Returns:
        Raw HTTP response. Expected success: HTTP 200.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: BAD_REQUEST(400)

    Example:
        # Search IO Devices
    """
    return await client.post(
        "/rw/iosystem/devices",
        params={k: v for k, v in {"action": action}.items() if v is not None},
        data={
            k: v
            for k, v in {
                "name": name,
                "lstate": lstate,
                "network": network,
                "example_name": example_name
            }.items()
            if v is not None
        },
    )
