# This file is AUTO-GENERATED by contrib/generator/main.py
# DO NOT EDIT MANUALLY — run the generator to regenerate.
"""
RWS module : RobotWare Services → System service → System Information

Miroir 1:1 de l'API REST ABB RobotWare 6.
Chaque fonction correspond à exactement un endpoint HTTP.
Aucune logique composée — voir highlevel/ pour les wrappers.
"""
from __future__ import annotations

import httpx

from abb_rws_client._core.client import RWSClient


async def get_system_information(client: RWSClient) -> httpx.Response:
    """
    System Information.

    Route: ``GET /rw/system``
    Contraintes ABB: Not supported in bootserver mode

    Args:
        client: Instance RWSClient ouverte.

    Returns:
        Réponse HTTP brute. Succès attendu : HTTP 200.

    Raises:
        RWSAuthenticationError: Sur HTTP 401.
        RWSNotFoundError: Sur HTTP 404.
        RWSHTTPError: Sur tout autre HTTP >= 400.
        # Codes ABB: BAD_REQUEST(400)

    Example:
        # curl --digest -u "Default User":robotics "http://localhost/rw/system"
    """
    return await client.get("/rw/system")

async def get_robot_type(client: RWSClient) -> httpx.Response:
    """
    Get robot type.

    Route: ``GET /rw/system/robottype``
    Contraintes ABB: Not supported in bootserver mode. The API supports only ABB standard robots. Positioners, track motion etc. are not supported. In case, there is no ABB standard robots, NO_CONTENT will be returned.

    Args:
        client: Instance RWSClient ouverte.

    Returns:
        Réponse HTTP brute. Succès attendu : HTTP 200.

    Raises:
        RWSAuthenticationError: Sur HTTP 401.
        RWSNotFoundError: Sur HTTP 404.
        RWSHTTPError: Sur tout autre HTTP >= 400.
        # Codes ABB: Robot controller errors, see

    Example:
        # curl --digest -u "Default User":robotics "http://localhost/rw/system/robottype"
    """
    return await client.get("/rw/system/robottype")

async def get_system_options(client: RWSClient) -> httpx.Response:
    """
    System Options.

    Route: ``GET /rw/system/options``
    Contraintes ABB: Not supported in bootserver mode

    Args:
        client: Instance RWSClient ouverte.

    Returns:
        Réponse HTTP brute. Succès attendu : HTTP 200.

    Raises:
        RWSAuthenticationError: Sur HTTP 401.
        RWSNotFoundError: Sur HTTP 404.
        RWSHTTPError: Sur tout autre HTTP >= 400.
        # Codes ABB: BAD_REQUEST(400)

    Example:
        # curl --digest -u "Default User":robotics "http://localhost/rw/system/options"
    """
    return await client.get("/rw/system/options")

async def get_system_energy_actions(
    client: RWSClient,
    action: str | None = None,
) -> httpx.Response:
    """
    Get System Energy Actions.

    Route: ``GET /rw/system/energy``
    Contraintes ABB: Not supported in bootserver mode

    Args:
        client: Instance RWSClient ouverte.
        action: Paramètre query. Optionnel.

    Returns:
        Réponse HTTP brute. Succès attendu : HTTP 200.

    Raises:
        RWSAuthenticationError: Sur HTTP 401.
        RWSNotFoundError: Sur HTTP 404.
        RWSHTTPError: Sur tout autre HTTP >= 400.
        # Codes ABB: BAD_REQUEST(400)

    Example:
        # curl --digest -u "Default User":robotics "http://localhost/rw/system/energy?action=show"
    """
    return await client.get("/rw/system/energy", params={k: v for k, v in {"action": action}.items() if v is not None})

async def get_system_energy_info_change_count(
    client: RWSClient,
    resource: str | None = None,
) -> httpx.Response:
    """
    System Energy Info Change Count.

    Route: ``GET /rw/system/energy``
    Contraintes ABB: Not supported in bootserver mode

    Args:
        client: Instance RWSClient ouverte.
        resource: Paramètre query. Optionnel.

    Returns:
        Réponse HTTP brute. Succès attendu : HTTP 200.

    Raises:
        RWSAuthenticationError: Sur HTTP 401.
        RWSNotFoundError: Sur HTTP 404.
        RWSHTTPError: Sur tout autre HTTP >= 400.
        # Codes ABB: BAD_REQUEST(400)

    Example:
        # curl --digest -u "Default User":robotics "http://localhost/rw/system/energy?resource=change-count"
    """
    return await client.get("/rw/system/energy", params={k: v for k, v in {"resource": resource}.items() if v is not None})

async def reset_accumulated_energy(
    client: RWSClient,
    action: str | None = None,
) -> httpx.Response:
    """
    Reset Accumulated Energy.

    Route: ``POST /rw/system/energy``
    Contraintes ABB: Not supported in bootserver mode

    Args:
        client: Instance RWSClient ouverte.
        action: Paramètre query. Optionnel.

    Returns:
        Réponse HTTP brute. Succès attendu : HTTP 204.

    Raises:
        RWSAuthenticationError: Sur HTTP 401.
        RWSNotFoundError: Sur HTTP 404.
        RWSHTTPError: Sur tout autre HTTP >= 400.
        # Codes ABB: BAD_REQUEST(400),NOT_FOUND(404)

    Example:
        # curl --digest -u "Default User":robotics "http://localhost/rw/system/energy?action=reset"
    """
    return await client.post("/rw/system/energy", params={k: v for k, v in {"action": action}.items() if v is not None})

async def get_system_energy(client: RWSClient) -> httpx.Response:
    """
    System Energy.

    Route: ``GET /rw/system/energy``
    Contraintes ABB: Not supported in bootserver mode

    Args:
        client: Instance RWSClient ouverte.

    Returns:
        Réponse HTTP brute. Succès attendu : HTTP 200.

    Raises:
        RWSAuthenticationError: Sur HTTP 401.
        RWSNotFoundError: Sur HTTP 404.
        RWSHTTPError: Sur tout autre HTTP >= 400.
        # Codes ABB: BAD_REQUEST(400)

    Example:
        # curl --digest -u "Default User":robotics "http://localhost/rw/system/energy"
    """
    return await client.get("/rw/system/energy")

async def subscribe_on_system_energy_changes(
    client: RWSClient,
    identifier: str | None = None,
    identifier_p: str | None = None,
) -> httpx.Response:
    """
    Subscribe on System Energy Changes..

    Route: ``POST /subscription``
    Contraintes ABB: Not supported in bootserver mode

    Args:
        client: Instance RWSClient ouverte.
        identifier: Paramètre body. Optionnel.
        identifier_p: Paramètre body. Optionnel.

    Returns:
        Réponse HTTP brute. Succès attendu : HTTP 201.

    Raises:
        RWSAuthenticationError: Sur HTTP 401.
        RWSNotFoundError: Sur HTTP 404.
        RWSHTTPError: Sur tout autre HTTP >= 400.
        # Codes ABB: UNSUPPORTED_MEDIA(415),BAD_REQUEST(400)

    Example:
        # only low priority subscription(-p=0) and medium priority subscription(-p=1) are allowed on this resource
    """
    return await client.post("/subscription", data={k: v for k, v in {"identifier": identifier, "identifier_p": identifier_p}.items() if v is not None})

async def get_system_robotware_license(client: RWSClient) -> httpx.Response:
    """
    Get System Robotware License.

    Route: ``GET /rw/system/license``
    Contraintes ABB: Not supported in bootserver mode.

    Args:
        client: Instance RWSClient ouverte.

    Returns:
        Réponse HTTP brute. Succès attendu : HTTP 200.

    Raises:
        RWSAuthenticationError: Sur HTTP 401.
        RWSNotFoundError: Sur HTTP 404.
        RWSHTTPError: Sur tout autre HTTP >= 400.
        # Codes ABB: BAD_REQUEST(400)

    Example:
        # curl --digest -u "Default User":robotics "http://localhost/rw/system/license"
    """
    return await client.get("/rw/system/license")

async def get_installed_products(
    client: RWSClient,
    name: str | None = None,
) -> httpx.Response:
    """
    Get Installed Products.

    Route: ``GET /rw/system/products``
    Contraintes ABB: In case, there is no products, NO_CONTENT(204) will be returned.

    Args:
        client: Instance RWSClient ouverte.
        name: Paramètre query. Optionnel.

    Returns:
        Réponse HTTP brute. Succès attendu : HTTP 200.

    Raises:
        RWSAuthenticationError: Sur HTTP 401.
        RWSNotFoundError: Sur HTTP 404.
        RWSHTTPError: Sur tout autre HTTP >= 400.
        # Codes ABB: BAD_REQUEST(400)

    Example:
        # `curl –digest -u "Default User":robotics " "
    """
    return await client.get("/rw/system/products", params={k: v for k, v in {"name": name}.items() if v is not None})
