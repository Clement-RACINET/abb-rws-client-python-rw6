# This file is AUTO-GENERATED by contrib/generator/main.py
# DO NOT EDIT MANUALLY — run the generator to regenerate.
"""
RWS module : RobotWare Services → RAPID Service → Operations on RAPID symbols properties

Miroir 1:1 de l'API REST ABB RobotWare 6.
Chaque fonction correspond à exactement un endpoint HTTP.
Aucune logique composée — voir highlevel/ pour les wrappers.
"""
from __future__ import annotations

import httpx

from abb_rws_client._core.client import RWSClient


async def get_rapid_symbols_resources(client: RWSClient) -> httpx.Response:
    """
    Get RAPID symbols resources.

    Route: ``GET /rw/rapid/symbols``
    Contraintes ABB: Not supported in bootserver mode

    Args:
        client: Instance RWSClient ouverte.

    Returns:
        Réponse HTTP brute. Succès attendu : HTTP 200.

    Raises:
        RWSAuthenticationError: Sur HTTP 401.
        RWSNotFoundError: Sur HTTP 404.
        RWSHTTPError: Sur tout autre HTTP >= 400.

    Example:
        # curl --digest -u "Default User":robotics" "
    """
    return await client.get("/rw/rapid/symbols")

async def get_rapid_symbols_actions(
    client: RWSClient,
    action: str | None = None,
) -> httpx.Response:
    """
    Get rapid symbols actions.

    Route: ``GET /rw/rapid/symbols``
    Contraintes ABB: Not supported in bootserver mode

    Args:
        client: Instance RWSClient ouverte.
        action: Paramètre query. Optionnel.

    Returns:
        Réponse HTTP brute. Succès attendu : HTTP 200.

    Raises:
        RWSAuthenticationError: Sur HTTP 401.
        RWSNotFoundError: Sur HTTP 404.
        RWSHTTPError: Sur tout autre HTTP >= 400.
        # Codes ABB: BAD_REQUEST(400)

    Example:
        # curl --digest -u "Default User":robotics" "
    """
    return await client.get("/rw/rapid/symbols", params={k: v for k, v in {"action": action}.items() if v is not None})

async def post_search_rapid_symbols(
    client: RWSClient,
    action: str | None = None,
    view: str | None = None,
    vartyp: str | None = None,
    blockurl: str | None = None,
    recursive: str | None = None,
    posl: str | None = None,
    posc: str | None = None,
    stack: str | None = None,
    onlyused: str | None = None,
    skipshared: str | None = None,
    regexp: str | None = None,
) -> httpx.Response:
    """
    Search RAPID symbols.

    Route: ``POST /rw/rapid/symbols``
    Contraintes ABB: Not supported in bootserver mode

    Args:
        client: Instance RWSClient ouverte.
        action: Paramètre query. Optionnel.
        view: Paramètre body. Optionnel.
        vartyp: Paramètre body. Optionnel.
        blockurl: Paramètre body. Optionnel.
        recursive: Paramètre body. Optionnel.
        posl: Paramètre body. Optionnel.
        posc: Paramètre body. Optionnel.
        stack: Paramètre body. Optionnel.
        onlyused: Paramètre body. Optionnel.
        skipshared: Paramètre body. Optionnel.
        regexp: Paramètre body. Optionnel.

    Returns:
        Réponse HTTP brute. Succès attendu : HTTP 200.

    Raises:
        RWSAuthenticationError: Sur HTTP 401.
        RWSNotFoundError: Sur HTTP 404.
        RWSHTTPError: Sur tout autre HTTP >= 400.
        # Codes ABB: BAD REQUEST(400) see

    Example:
        # curl --digest -u "Default User":robotics -d "view=block&vartyp=any&blockurl=RAPID/T_ROB1&symtyp=var&recursive=true&datty
    """
    return await client.post("/rw/rapid/symbols", params={k: v for k, v in {"action": action}.items() if v is not None}, data={k: v for k, v in {"view": view, "vartyp": vartyp, "blockurl": blockurl, "recursive": recursive, "posl": posl, "posc": posc, "stack": stack, "onlyused": onlyused, "skipshared": skipshared, "regexp": regexp}.items() if v is not None})

async def get_object_extension_list(
    client: RWSClient,
    symbol_url: str,
    info: str | None = None,
    type: str | None = None,
    statements: str | None = None,
    backwardstmts: str | None = None,
    undostmts: str | None = None,
    typedecls: str | None = None,
) -> httpx.Response:
    """
    Get Object Extension List.

    Route: ``GET /rw/rapid/symbols/{symbol URL}``
    Contraintes ABB: Not supported in bootserver mode

    Args:
        client: Instance RWSClient ouverte.
        symbol_url: Paramètre de chemin URL.
        info: Paramètre query. Optionnel.
        type: Paramètre query. Optionnel.
        statements: Paramètre query. Optionnel.
        backwardstmts: Paramètre query. Optionnel.
        undostmts: Paramètre query. Optionnel.
        typedecls: Paramètre query. Optionnel.

    Returns:
        Réponse HTTP brute. Succès attendu : HTTP 200.

    Raises:
        RWSAuthenticationError: Sur HTTP 401.
        RWSNotFoundError: Sur HTTP 404.
        RWSHTTPError: Sur tout autre HTTP >= 400.
        # Codes ABB: BAD_REQUEST (400)

    Example:
        # curl --digest -u "Default User":robotics" "
    """
    return await client.get(f"/rw/rapid/symbols/{symbol_url}", params={k: v for k, v in {"info": info, "type": type, "statements": statements, "backwardstmts": backwardstmts, "undostmts": undostmts, "typedecls": typedecls}.items() if v is not None})
