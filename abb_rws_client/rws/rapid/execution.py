# This file is AUTO-GENERATED by utils/generator/main.py
# DO NOT EDIT MANUALLY — run the generator to regenerate.
#  Generator author: Clement RACINET
"""
RWS module: RobotWare Services → RAPID Service → Operations on RAPID execution

1:1 mirror of the ABB RobotWare 6 REST API.
Each function maps to exactly one HTTP endpoint.
No composed logic — see highlevel/ for wrappers.
"""
from __future__ import annotations

import httpx

from abb_rws_client.core.client import RWSClient


async def get_rapid_execution_state(
    client: RWSClient,
    continue_on_err: str | None = None,
) -> httpx.Response:
    """
    Get RAPID Execution state.

    Route: ``GET /rw/rapid/execution``
    ABB constraints: Not supported in bootserver mode. Use /rw/rapid/execution;ctrlexecstate to
        filter ctrlexecstate value.

    Args:
        client: Open RWSClient instance.
        continue_on_err: Query parameter. Optional.

    Returns:
        Raw HTTP response. Expected success: HTTP 200.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: BAD_REQUEST(400)

    Example:
        ```python
        >>> await get_rapid_execution_state(client)
        <Response [200]>
        ```
    """
    return await client.get(
        "/rw/rapid/execution",
        params={k: v for k, v in {"continue_on_err": continue_on_err}.items() if v is not None},
    )

async def get_rapid_execution_actions(
    client: RWSClient,
    action: str | None = None,
) -> httpx.Response:
    """
    Get RAPID Execution actions.

    Route: ``GET /rw/rapid/execution``
    ABB constraints: Not supported in bootserver mode

    Args:
        client: Open RWSClient instance.
        action: Query parameter. Optional.

    Returns:
        Raw HTTP response. Expected success: HTTP 200.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: BAD_REQUEST(400)

    Example:
        ```python
        >>> await get_rapid_execution_actions(client)
        <Response [200]>
        ```
    """
    return await client.get(
        "/rw/rapid/execution",
        params={k: v for k, v in {"action": action}.items() if v is not None},
    )

async def start_rapid_execution(
    client: RWSClient,
    action: str | None = None,
    regain: str | None = None,
    execmode: str | None = None,
    cycle: str | None = None,
    condition: str | None = None,
    stopatbp: str | None = None,
    alltaskbytsp: str | None = None,
) -> httpx.Response:
    """
    Start RAPID Execution.

    Route: ``POST /rw/rapid/execution``
    ABB constraints: Not supported in bootserver mode.

    Args:
        client: Open RWSClient instance.
        action: Query parameter. Optional.
        regain: Body parameter. Optional.
        execmode: Body parameter. Optional.
        cycle: Body parameter. Optional.
        condition: Body parameter. Optional.
        stopatbp: Body parameter. Optional.
        alltaskbytsp: Body parameter. Optional.

    Returns:
        Raw HTTP response. Expected success: HTTP 204.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: BAD_REQUEST(400)

    Example:
        ```python
        >>> await start_rapid_execution(client)
        <Response [204]>
        ```
    """
    return await client.post(
        "/rw/rapid/execution",
        params={k: v for k, v in {"action": action}.items() if v is not None},
        data={
            k: v
            for k, v in {
                "regain": regain,
                "execmode": execmode,
                "cycle": cycle,
                "condition": condition,
                "stopatbp": stopatbp,
                "alltaskbytsp": alltaskbytsp
            }.items()
            if v is not None
        },
    )

async def stop_rapid_execution(
    client: RWSClient,
    action: str | None = None,
    stopmode: str | None = None,
    usetsp: str | None = None,
) -> httpx.Response:
    """
    Stop RAPID Execution.

    Route: ``POST /rw/rapid/execution``
    ABB constraints: Not supported in bootserver mode

    Args:
        client: Open RWSClient instance.
        action: Query parameter. Optional.
        stopmode: Body parameter. Optional.
        usetsp: Body parameter. Optional.

    Returns:
        Raw HTTP response. Expected success: HTTP 204.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: BAD_REQUEST(400)

    Example:
        ```python
        >>> await stop_rapid_execution(client)
        <Response [204]>
        ```
    """
    return await client.post(
        "/rw/rapid/execution",
        params={k: v for k, v in {"action": action}.items() if v is not None},
        data={
            k: v
            for k, v in {
                "stopmode": stopmode,
                "usetsp": usetsp
            }.items()
            if v is not None
        },
    )

async def start_rapid_execution_from_production_entry(
    client: RWSClient,
    action: str | None = None,
) -> httpx.Response:
    """
    Start RAPID Execution from production entry.

    Route: ``POST /rw/rapid/execution``
    ABB constraints: Not supported in bootserver mode

    Args:
        client: Open RWSClient instance.
        action: Query parameter. Optional.

    Returns:
        Raw HTTP response. Expected success: HTTP 204.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: BAD_REQUEST(400)

    Example:
        ```python
        >>> await start_rapid_execution_from_production_entry(client)
        <Response [204]>
        ```
    """
    return await client.post(
        "/rw/rapid/execution",
        params={k: v for k, v in {"action": action}.items() if v is not None},
        data={},
    )

async def reset_rapid_program_pointer_to_main(
    client: RWSClient,
    action: str | None = None,
) -> httpx.Response:
    """
    Reset RAPID program pointer to main.

    Route: ``POST /rw/rapid/execution``
    ABB constraints: Not supported in bootserver mode

    Args:
        client: Open RWSClient instance.
        action: Query parameter. Optional.

    Returns:
        Raw HTTP response. Expected success: HTTP 204.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: BAD_REQUEST(400)

    Example:
        ```python
        >>> await reset_rapid_program_pointer_to_main(client)
        <Response [204]>
        ```
    """
    return await client.post(
        "/rw/rapid/execution",
        params={k: v for k, v in {"action": action}.items() if v is not None},
        data={},
    )

async def set_number_of_execution_cycles(
    client: RWSClient,
    action: str | None = None,
    cycle: str | None = None,
) -> httpx.Response:
    """
    Set number of execution cycles.

    Route: ``POST /rw/rapid/execution``
    ABB constraints: Not supported in bootserver mode. Mastership is required to set cycle.

    Args:
        client: Open RWSClient instance.
        action: Query parameter. Optional.
        cycle: Body parameter. Optional.

    Returns:
        Raw HTTP response. Expected success: HTTP 204.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: BAD_REQUEST(400)

    Example:
        ```python
        >>> await set_number_of_execution_cycles(client)
        <Response [204]>
        ```
    """
    return await client.post(
        "/rw/rapid/execution",
        params={k: v for k, v in {"action": action}.items() if v is not None},
        data={k: v for k, v in {"cycle": cycle}.items() if v is not None},
    )

async def subscribe_rapid_execution(
    client: RWSClient,
    resources: str | None = None,
    identifier: str | None = None,
    identifier_p: str | None = None,
) -> httpx.Response:
    """
    Subscribe RAPID Execution.

    Route: ``POST /subscription``
    ABB constraints: Not supported in bootserver mode

    Args:
        client: Open RWSClient instance.
        resources: Body parameter. Optional.
        identifier: Body parameter. Optional.
        identifier_p: Body parameter. Optional.

    Returns:
        Raw HTTP response. Expected success: HTTP 201.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: BAD_REQUEST(400), UNSUPPORTED_MEDIA(415)

    Example:
        ```python
        >>> await subscribe_rapid_execution(client)
        <Response [201]>
        ```
    """
    return await client.post(
        "/subscription",
        data={
            k: v
            for k, v in {
                "resources": resources,
                "identifier": identifier,
                "identifier_p": identifier_p
            }.items()
            if v is not None
        },
    )

async def subscribe_rapid_execution_cycle(
    client: RWSClient,
    resources: str | None = None,
    identifier: str | None = None,
    identifier_p: str | None = None,
) -> httpx.Response:
    """
    Subscribe RAPID Execution Cycle.

    Route: ``POST /subscription``
    ABB constraints: Not supported in bootserver mode

    Args:
        client: Open RWSClient instance.
        resources: Body parameter. Optional.
        identifier: Body parameter. Optional.
        identifier_p: Body parameter. Optional.

    Returns:
        Raw HTTP response. Expected success: HTTP 201.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: BAD_REQUEST(400), UNSUPPORTED_MEDIA(415)

    Example:
        ```python
        >>> await subscribe_rapid_execution_cycle(client)
        <Response [201]>
        ```
    """
    return await client.post(
        "/subscription",
        data={
            k: v
            for k, v in {
                "resources": resources,
                "identifier": identifier,
                "identifier_p": identifier_p
            }.items()
            if v is not None
        },
    )

async def subscribe_on_hold_to_run(
    client: RWSClient,
    identifier: str | None = None,
    identifier_p: str | None = None,
) -> httpx.Response:
    """
    Subscribe on Hold to Run.

    Route: ``POST /subscription``
    ABB constraints: Not supported in bootserver mode

    Args:
        client: Open RWSClient instance.
        identifier: Body parameter. Optional.
        identifier_p: Body parameter. Optional.

    Returns:
        Raw HTTP response. Expected success: HTTP 201.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: BAD_REQUEST(400), UNSUPPORTED_MEDIA(415)

    Example:
        ```python
        >>> await subscribe_on_hold_to_run(client)
        <Response [201]>
        ```
    """
    return await client.post(
        "/subscription",
        data={
            k: v
            for k, v in {
                "identifier": identifier,
                "identifier_p": identifier_p
            }.items()
            if v is not None
        },
    )

async def set_hold_to_run_cmd(
    client: RWSClient,
    action: str | None = None,
    state: str | None = None,
) -> httpx.Response:
    """
    Set Hold to Run Cmd.

    Route: ``POST /rw/rapid/execution``
    ABB constraints: -Not supported in bootserver mode -Supported in VC only -Login as Local
        Client -The Hold-To-Run control prevents RAPID-program execution to start until the
        holdtorun state is changed to Press. -In the state Pressed the client must poll the
        Hold-To-Run control every 2 second, otherwise the program...

    Args:
        client: Open RWSClient instance.
        action: Query parameter. Optional.
        state: Body parameter. Optional.

    Returns:
        Raw HTTP response. Expected success: HTTP 204.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: BAD_REQUEST(400),FORBIDDEN(403)

    Example:
        ```python
        >>> await set_hold_to_run_cmd(client)
        <Response [204]>
        ```
    """
    return await client.post(
        "/rw/rapid/execution",
        params={k: v for k, v in {"action": action}.items() if v is not None},
        data={k: v for k, v in {"state": state}.items() if v is not None},
    )
