# This file is AUTO-GENERATED by utils/generator/main.py
# DO NOT EDIT MANUALLY — run the generator to regenerate.
#  Generator author: Clement RACINET
"""
RWS module: Controller Service → Get Controller Resources

1:1 mirror of the ABB RobotWare 6 REST API.
Each function maps to exactly one HTTP endpoint.
No composed logic — see highlevel/ for wrappers.
"""

from __future__ import annotations

import httpx

from abb_rws_client_python_rw6.core.client import RWSClient


async def get_controller_resources(client: RWSClient) -> httpx.Response:
    """
    Get Controller Resources.

    Route: ``GET /ctrl``
    ABB constraints: Supported in bootserver mode

    Args:
        client: Open RWSClient instance.

    Returns:
        Raw HTTP response. Expected success: HTTP 200.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: NOT_FOUND(404) See

    Example:
        ```python
        >>> await get_controller_resources(client)
        <Response [200]>
        ```
    """
    return await client.get("/ctrl")


async def get_controller_actions(
    client: RWSClient,
    action: str | None = None,
) -> httpx.Response:
    """
    Get Controller Actions.

    Route: ``GET /ctrl``
    ABB constraints: Supported in bootserver mode

    Args:
        client: Open RWSClient instance.
        action: Query parameter. Optional.

    Returns:
        Raw HTTP response. Expected success: HTTP 200.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: NOT_FOUND(404) See

    Example:
        ```python
        >>> await get_controller_actions(client)
        <Response [200]>
        ```
    """
    return await client.get(
        "/ctrl",
        params={k: v for k, v in {"action": action}.items() if v is not None},
    )


async def get_controller_environment_variable(
    client: RWSClient,
    envname: str,
) -> httpx.Response:
    """
    Get Controller environment variable.

    Route: ``GET /ctrl/${ENVNAME}``

    Args:
        client: Open RWSClient instance.
        envname: URL path parameter.

    Returns:
        Raw HTTP response. Expected success: HTTP 200.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: NOT_FOUND(404)

    Example:
        ```python
        >>> await get_controller_environment_variable(client, "envname_value")
        <Response [200]>
        ```
    """
    return await client.get(f"/ctrl/${envname}")


async def restart_or_shutdown_controller(client: RWSClient) -> httpx.Response:
    """
    Restart or Shutdown controller.

    Route: ``POST /ctrl``
    ABB constraints: Supported in bootserver mode

    Args:
        client: Open RWSClient instance.

    Returns:
        Raw HTTP response. Expected success: HTTP 200.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: NOT_FOUND(404), BAD_REQUEST(400), UNSUPPORTED_MEDIA(415), CONFLICT(409)

    Example:
        ```python
        >>> await restart_or_shutdown_controller(client)
        <Response [200]>
        ```
    """
    return await client.post(
        "/ctrl",
        data={},
    )


async def set_controller_language(
    client: RWSClient,
    action: str | None = None,
) -> httpx.Response:
    """
    Set Controller language.

    Route: ``POST /ctrl``
    ABB constraints: Not Supported in bootserver mode

    Args:
        client: Open RWSClient instance.
        action: Query parameter. Optional.

    Returns:
        Raw HTTP response. Expected success: HTTP 204.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: BAD_REQUEST(400)

    Example:
        ```python
        >>> await set_controller_language(client)
        <Response [204]>
        ```
    """
    return await client.post(
        "/ctrl",
        params={k: v for k, v in {"action": action}.items() if v is not None},
        data={},
    )


async def get_list_of_installed_systems(client: RWSClient) -> httpx.Response:
    """
    Get list of installed systems.

    Route: ``GET /ctrl/system``
    ABB constraints: Supported in bootserver mode

    Args:
        client: Open RWSClient instance.

    Returns:
        Raw HTTP response. Expected success: HTTP 200.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: NOT_FOUND(404) See

    Example:
        ```python
        >>> await get_list_of_installed_systems(client)
        <Response [200]>
        ```
    """
    return await client.get("/ctrl/system")


async def get_actions_on_system(
    client: RWSClient,
    action: str | None = None,
) -> httpx.Response:
    """
    Get actions on system.

    Route: ``GET /ctrl/system``
    ABB constraints: Supported in bootserver mode

    Args:
        client: Open RWSClient instance.
        action: Query parameter. Optional.

    Returns:
        Raw HTTP response. Expected success: HTTP 200.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: NOT_FOUND(404) See

    Example:
        ```python
        >>> await get_actions_on_system(client)
        <Response [200]>
        ```
    """
    return await client.get(
        "/ctrl/system",
        params={k: v for k, v in {"action": action}.items() if v is not None},
    )


async def set_boot_device(
    client: RWSClient,
    action: str | None = None,
) -> httpx.Response:
    """
    Set Boot Device.

    Route: ``POST /ctrl/system``
    ABB constraints: Supported in bootserver mode only

    Args:
        client: Open RWSClient instance.
        action: Query parameter. Optional.

    Returns:
        Raw HTTP response. Expected success: HTTP 204.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: BAD_REQUEST(400) See

    Example:
        ```python
        >>> await set_boot_device(client)
        <Response [204]>
        ```
    """
    return await client.post(
        "/ctrl/system",
        params={k: v for k, v in {"action": action}.items() if v is not None},
        data={},
    )


async def get_boot_device(
    client: RWSClient,
    resource: str | None = None,
) -> httpx.Response:
    """
    Get Boot Device.

    Route: ``GET /ctrl/system``
    ABB constraints: Supported in bootserver mode only

    Args:
        client: Open RWSClient instance.
        resource: Query parameter. Optional.

    Returns:
        Raw HTTP response. Expected success: HTTP 200.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: BAD_REQUEST(400) See

    Example:
        ```python
        >>> await get_boot_device(client)
        <Response [200]>
        ```
    """
    return await client.get(
        "/ctrl/system",
        params={k: v for k, v in {"resource": resource}.items() if v is not None},
    )


async def load_boot_image(
    client: RWSClient,
    action: str | None = None,
) -> httpx.Response:
    """
    Load Boot Image.

    Route: ``POST /ctrl/system``
    ABB constraints: Supported in bootserver mode only Controller must be restarted to reflect
        the changes Not supported by VC

    Args:
        client: Open RWSClient instance.
        action: Query parameter. Optional.

    Returns:
        Raw HTTP response. Expected success: HTTP 202.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: BAD_REQUEST(400) See

    Example:
        ```python
        >>> await load_boot_image(client)
        <Response [202]>
        ```
    """
    return await client.post(
        "/ctrl/system",
        params={k: v for k, v in {"action": action}.items() if v is not None},
        data={},
    )


async def post_unload_boot_image(
    client: RWSClient,
    action: str | None = None,
) -> httpx.Response:
    """
    Unload Boot Image.

    Route: ``POST /ctrl/system``
    ABB constraints: Supported in bootserver mode only Not supported by VC

    Args:
        client: Open RWSClient instance.
        action: Query parameter. Optional.

    Returns:
        Raw HTTP response. Expected success: HTTP 204.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: BAD_REQUEST(400) See

    Example:
        ```python
        >>> await post_unload_boot_image(client)
        <Response [204]>
        ```
    """
    return await client.post(
        "/ctrl/system",
        params={k: v for k, v in {"action": action}.items() if v is not None},
        data={},
    )


async def get_selected_system_name(
    client: RWSClient,
    type: str | None = None,
) -> httpx.Response:
    """
    Get selected system name.

    Route: ``GET /ctrl/system``
    ABB constraints: Not supported in VC Supported in Bootserver In bootserver, system-name will
        show as BOOTSERVER, as active system is not applicable in bootserver mode

    Args:
        client: Open RWSClient instance.
        type: Query parameter. Optional.

    Returns:
        Raw HTTP response. Expected success: HTTP 200.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: BAD_REQUEST(400) See

    Example:
        ```python
        >>> await get_selected_system_name(client)
        <Response [200]>
        ```
    """
    return await client.get(
        "/ctrl/system",
        params={k: v for k, v in {"type": type}.items() if v is not None},
    )


async def post_install_deployment_package(client: RWSClient) -> httpx.Response:
    """
    Install deployment package.

    Route: ``POST /ctrl/system/installdpkg``
    ABB constraints: Supported in bootserver mode only. Will validate the deployment package
        internally. To get status of the installation, see location header. System will be
        restarted after unpacking the files. dp-pkg-path should be inside inbox folder.The
        installation package and all associated files should be pla...

    Args:
        client: Open RWSClient instance.

    Returns:
        Raw HTTP response. Expected success: HTTP 202.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: UNSUPPORTED_MEDIA(415), BAD_REQUEST(400)

    Example:
        ```python
        >>> await post_install_deployment_package(client)
        <Response [202]>
        ```
    """
    return await client.post(
        "/ctrl/system/installdpkg",
        data={},
    )


async def validate_deployment_package(client: RWSClient) -> httpx.Response:
    """
    Validate deployment package.

    Route: ``POST /ctrl/system/validatedpkg``
    ABB constraints: Supported in bootserver mode only. path should be inside inbox folder.

    Args:
        client: Open RWSClient instance.

    Returns:
        Raw HTTP response. Expected success: HTTP 204.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: UNSUPPORTED_MEDIA(415), BAD_REQUEST(400)

    Example:
        ```python
        >>> await validate_deployment_package(client)
        <Response [204]>
        ```
    """
    return await client.post(
        "/ctrl/system/validatedpkg",
        data={},
    )


async def get_system_resource(
    client: RWSClient,
    system_name: str,
) -> httpx.Response:
    """
    Get system resource.

    Route: ``GET /ctrl/system/{system-name}``
    ABB constraints: Supported in bootserver mode

    Args:
        client: Open RWSClient instance.
        system_name: URL path parameter.

    Returns:
        Raw HTTP response. Expected success: HTTP 200.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: NOT_FOUND(404) See

    Example:
        ```python
        >>> await get_system_resource(client, "system_name_value")
        <Response [200]>
        ```
    """
    return await client.get(f"/ctrl/system/{system_name}")


async def get_actions_on_system_resource(
    client: RWSClient,
    system_name: str,
    action: str | None = None,
) -> httpx.Response:
    """
    Get actions on a system resource.

    Route: ``GET /ctrl/system/{system-name}``
    ABB constraints: Supported in bootserver mode

    Args:
        client: Open RWSClient instance.
        system_name: URL path parameter.
        action: Query parameter. Optional.

    Returns:
        Raw HTTP response. Expected success: HTTP 200.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: NOT_FOUND(404) See

    Example:
        ```python
        >>> await get_actions_on_system_resource(client, "system_name_value")
        <Response [200]>
        ```
    """
    return await client.get(
        f"/ctrl/system/{system_name}",
        params={k: v for k, v in {"action": action}.items() if v is not None},
    )


async def post_rename_system(
    client: RWSClient,
    system_name: str,
    action: str | None = None,
    newname: str | None = None,
) -> httpx.Response:
    """
    Rename a system.

    Route: ``POST /ctrl/system/{system-name}``
    ABB constraints: Only available for VxWorks in "System-Mode". Not available for VC or
        Boot-Server.

    Args:
        client: Open RWSClient instance.
        system_name: URL path parameter.
        action: Query parameter. Optional.
        newname: Body parameter. Optional.

    Returns:
        Raw HTTP response. Expected success: HTTP 201.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: BAD_REQUEST(400), UNAUTHORIZED(401)

    Example:
        ```python
        >>> await post_rename_system(client, "system_name_value")
        <Response [201]>
        ```
    """
    return await client.post(
        f"/ctrl/system/{system_name}",
        params={k: v for k, v in {"action": action}.items() if v is not None},
        data={k: v for k, v in {"newname": newname}.items() if v is not None},
    )


async def post_select_system(
    client: RWSClient,
    system_name: str,
    action: str | None = None,
) -> httpx.Response:
    """
    Select a system.

    Route: ``POST /ctrl/system/{system-name}``
    ABB constraints: status code CONFLICT is returned if trying to select a system that is
        already active.

    Args:
        client: Open RWSClient instance.
        system_name: URL path parameter.
        action: Query parameter. Optional.

    Returns:
        Raw HTTP response. Expected success: HTTP 200.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: UNAUTHORIZED(401), BAD_REQUEST(400), CONFLICT(409)

    Example:
        ```python
        >>> await post_select_system(client, "system_name_value")
        <Response [200]>
        ```
    """
    return await client.post(
        f"/ctrl/system/{system_name}",
        params={k: v for k, v in {"action": action}.items() if v is not None},
        data={},
    )


async def delete_system(
    client: RWSClient,
    system_name: str,
) -> httpx.Response:
    """
    Delete a System.

    Route: ``DELETE /ctrl/system/{system-name}``
    ABB constraints: Supported in bootserver mode not supported in VC

    Args:
        client: Open RWSClient instance.
        system_name: URL path parameter.

    Returns:
        Raw HTTP response. Expected success: HTTP 204.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: BAD_REQUEST(400), UNAUTHORIZED(401)

    Example:
        ```python
        >>> await delete_system(client, "system_name_value")
        <Response [204]>
        ```
    """
    return await client.delete(f"/ctrl/system/{system_name}")


async def post_deselect_system(
    client: RWSClient,
    action: str | None = None,
) -> httpx.Response:
    """
    Deselect a System.

    Route: ``POST /ctrl/system``
    ABB constraints: Not supported in Virtual Controller

    Args:
        client: Open RWSClient instance.
        action: Query parameter. Optional.

    Returns:
        Raw HTTP response. Expected success: HTTP 204.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: BAD_REQUEST(400) See

    Example:
        ```python
        >>> await post_deselect_system(client)
        <Response [204]>
        ```
    """
    return await client.post(
        "/ctrl/system",
        params={k: v for k, v in {"action": action}.items() if v is not None},
        data={},
    )


async def get_network_resource(client: RWSClient) -> httpx.Response:
    """
    Get Network Resource.

    Route: ``GET /ctrl/network``
    ABB constraints: Supported in bootserver mode

    Args:
        client: Open RWSClient instance.

    Returns:
        Raw HTTP response. Expected success: HTTP 200.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.

    Example:
        ```python
        >>> await get_network_resource(client)
        <Response [200]>
        ```
    """
    return await client.get("/ctrl/network")


async def get_network_setting_actions(
    client: RWSClient,
    action: str | None = None,
) -> httpx.Response:
    """
    Get Network setting actions.

    Route: ``GET /ctrl/network``
    ABB constraints: Supported in bootserver mode. Not supported by VC.

    Args:
        client: Open RWSClient instance.
        action: Query parameter. Optional.

    Returns:
        Raw HTTP response. Expected success: HTTP 200.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: BAD_REQUEST(400)

    Example:
        ```python
        >>> await get_network_setting_actions(client)
        <Response [200]>
        ```
    """
    return await client.get(
        "/ctrl/network",
        params={k: v for k, v in {"action": action}.items() if v is not None},
    )


async def set_network_configuration(
    client: RWSClient,
    action: str | None = None,
) -> httpx.Response:
    """
    Set Network configuration.

    Route: ``POST /ctrl/network``
    ABB constraints: Supported in bootserver mode. Not supported by VC. Controller must be
        restarted to reflect the changes. Requires the UAS grant UAS_CONTROLLER_PROPERTIES_WRITE

    Args:
        client: Open RWSClient instance.
        action: Query parameter. Optional.

    Returns:
        Raw HTTP response. Expected success: HTTP 202.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: UNSUPPORTED_MEDIA(415), BAD_REQUEST(400), UNAUTHORIZED(401)

    Example:
        ```python
        >>> await set_network_configuration(client)
        <Response [202]>
        ```
    """
    return await client.post(
        "/ctrl/network",
        params={k: v for k, v in {"action": action}.items() if v is not None},
        data={},
    )


async def get_dns_resource(client: RWSClient) -> httpx.Response:
    """
    Get DNS Resource.

    Route: ``GET /ctrl/network/dns``
    ABB constraints: Supported in bootserver mode Not supported by VC.

    Args:
        client: Open RWSClient instance.

    Returns:
        Raw HTTP response. Expected success: HTTP 200.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.

    Example:
        ```python
        >>> await get_dns_resource(client)
        <Response [200]>
        ```
    """
    return await client.get("/ctrl/network/dns")


async def add_route_table_entry(client: RWSClient) -> httpx.Response:
    """
    Add a route table entry.

    Route: ``POST /ctrl/network/route/add``
    ABB constraints: Supported in bootserver mode. Not supported by VC.

    Args:
        client: Open RWSClient instance.

    Returns:
        Raw HTTP response. Expected success: HTTP 204.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: UNSUPPORTED_MEDIA(415), BAD_REQUEST(400)

    Example:
        ```python
        >>> await add_route_table_entry(client)
        <Response [204]>
        ```
    """
    return await client.post(
        "/ctrl/network/route/add",
        data={},
    )


async def options_options_to_add_route_table_entry(client: RWSClient) -> httpx.Response:
    """
    Options to add a route table entry.

    Route: ``OPTIONS /ctrl/network/route/add``
    ABB constraints: Supported in bootserver mode. Not supported by VC.

    Args:
        client: Open RWSClient instance.

    Returns:
        Raw HTTP response. Expected success: HTTP 200.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: See

    Example:
        ```python
        >>> await options_options_to_add_route_table_entry(client)
        <Response [200]>
        ```
    """
    return await client.options("/ctrl/network/route/add")


async def remove_route_table_entry(client: RWSClient) -> httpx.Response:
    """
    Remove a route table entry.

    Route: ``POST /ctrl/network/route/remove``
    ABB constraints: The routing entry will be removed from the network stack routing table after
        reboot. Supported in bootserver mode. Not supported by VC.

    Args:
        client: Open RWSClient instance.

    Returns:
        Raw HTTP response. Expected success: HTTP 202.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: UNSUPPORTED_MEDIA(415), BAD_REQUEST(400)

    Example:
        ```python
        >>> await remove_route_table_entry(client)
        <Response [202]>
        ```
    """
    return await client.post(
        "/ctrl/network/route/remove",
        data={},
    )


async def options_options_to_remove_route_table_entry(client: RWSClient) -> httpx.Response:
    """
    Options to remove a route table entry.

    Route: ``OPTIONS /ctrl/network/route/remove``
    ABB constraints: Supported in bootserver mode. Not supported by VC.

    Args:
        client: Open RWSClient instance.

    Returns:
        Raw HTTP response. Expected success: HTTP 200.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: See

    Example:
        ```python
        >>> await options_options_to_remove_route_table_entry(client)
        <Response [200]>
        ```
    """
    return await client.options("/ctrl/network/route/remove")


async def get_compress_resources(client: RWSClient) -> httpx.Response:
    """
    Get compress resources.

    Route: ``GET /ctrl/compress``
    ABB constraints: Not supported in bootserver mode

    Args:
        client: Open RWSClient instance.

    Returns:
        Raw HTTP response. Expected success: HTTP 200.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: BAD_REQUEST(400), See

    Example:
        ```python
        >>> await get_compress_resources(client)
        <Response [200]>
        ```
    """
    return await client.get("/ctrl/compress")


async def get_compress_actions(
    client: RWSClient,
    action: str | None = None,
) -> httpx.Response:
    """
    Get compress actions.

    Route: ``GET /ctrl/compress``
    ABB constraints: Not supported in bootserver mode

    Args:
        client: Open RWSClient instance.
        action: Query parameter. Optional.

    Returns:
        Raw HTTP response. Expected success: HTTP 200.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: BAD_REQUEST(400), See

    Example:
        ```python
        >>> await get_compress_actions(client)
        <Response [200]>
        ```
    """
    return await client.get(
        "/ctrl/compress",
        params={k: v for k, v in {"action": action}.items() if v is not None},
    )


async def post_compress_decompress_resource(
    client: RWSClient,
    action: str | None = None,
) -> httpx.Response:
    """
    Compress/Decompress Resource.

    Route: ``POST /ctrl/compress``
    ABB constraints: Compression of HOME folder, its sub directories, and SYSTEM folder is not
        allowed. While decompressing a compressed file or a folder to a destination path, if a
        file or a folder with the same name as the compressed file name is already existing, it
        will be overwritten by the contents of the decom...

    Args:
        client: Open RWSClient instance.
        action: Query parameter. Optional.

    Returns:
        Raw HTTP response. Expected success: HTTP 202.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: BAD_REQUEST(400), See

    Example:
        ```python
        >>> await post_compress_decompress_resource(client)
        <Response [202]>
        ```
    """
    return await client.post(
        "/ctrl/compress",
        params={k: v for k, v in {"action": action}.items() if v is not None},
        data={},
    )


async def get_diagnostics_resources(client: RWSClient) -> httpx.Response:
    """
    Get diagnostics resources.

    Route: ``GET /ctrl/diagnostics``
    ABB constraints: Not supported in bootserver mode. Not supported on Virtual Controller.

    Args:
        client: Open RWSClient instance.

    Returns:
        Raw HTTP response. Expected success: HTTP 200.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: BAD_REQUEST(400), See

    Example:
        ```python
        >>> await get_diagnostics_resources(client)
        <Response [200]>
        ```
    """
    return await client.get("/ctrl/diagnostics")


async def get_diagnostics_actions(
    client: RWSClient,
    action: str | None = None,
) -> httpx.Response:
    """
    Get diagnostics actions.

    Route: ``GET /ctrl/diagnostics``
    ABB constraints: Not supported in bootserver mode. Not supported on Virtual Controller.

    Args:
        client: Open RWSClient instance.
        action: Query parameter. Optional.

    Returns:
        Raw HTTP response. Expected success: HTTP 200.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: BAD_REQUEST(400), See

    Example:
        ```python
        >>> await get_diagnostics_actions(client)
        <Response [200]>
        ```
    """
    return await client.get(
        "/ctrl/diagnostics",
        params={k: v for k, v in {"action": action}.items() if v is not None},
    )


async def save_system_diagnostics(
    client: RWSClient,
    action: str | None = None,
) -> httpx.Response:
    """
    Save system diagnostics.

    Route: ``POST /ctrl/diagnostics``
    ABB constraints: Since saving a diagnostic log is an asynchronous task, the location header
        can be subscribed on to get information about the status of the task. Not supported in
        bootserver mode. Not supported on Virtual Controller.

    Args:
        client: Open RWSClient instance.
        action: Query parameter. Optional.

    Returns:
        Raw HTTP response. Expected success: HTTP 202.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: BAD_REQUEST(400), CONFLICT(409), UNSUPPORTED_MEDIA(415) See

    Example:
        ```python
        >>> await save_system_diagnostics(client)
        <Response [202]>
        ```
    """
    return await client.post(
        "/ctrl/diagnostics",
        params={k: v for k, v in {"action": action}.items() if v is not None},
        data={},
    )


async def subscribe_on_system_dump(
    client: RWSClient,
    identifier: str | None = None,
    identifier_p: str | None = None,
) -> httpx.Response:
    """
    Subscribe on system dump.

    Route: ``POST /subscription``
    ABB constraints: If there is no system dump available in the controller, the initial response
        will be empty. Also, if the subscription is created when a system dump is in progress,
        the initial response will be empty. if an client has already subscribed on the system
        dump resource(/ctrl/diagnostics), and the exist...

    Args:
        client: Open RWSClient instance.
        identifier: Body parameter. Optional.
        identifier_p: Body parameter. Optional.

    Returns:
        Raw HTTP response. Expected success: HTTP 201.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: BAD_REQUEST(400), See

    Example:
        ```python
        >>> await subscribe_on_system_dump(client)
        <Response [201]>
        ```
    """
    return await client.post(
        "/subscription",
        data={
            k: v
            for k, v in {"identifier": identifier, "identifier_p": identifier_p}.items()
            if v is not None
        },
    )


async def subscribe_on_diagnostics_states_get_system_diagnostics(
    client: RWSClient,
    identifier: str | None = None,
    identifier_p: str | None = None,
) -> httpx.Response:
    """
    Subscribe on Diagnostics States (Get System Diagnostics).

    Route: ``POST /subscription``
    ABB constraints: if the subscription is created when save System Diagnostics(diagnostics.log
        is in progress),the state will be in "pending". Not supported in bootserver mode. Not
        supported on virtual controller.

    Args:
        client: Open RWSClient instance.
        identifier: Body parameter. Optional.
        identifier_p: Body parameter. Optional.

    Returns:
        Raw HTTP response. Expected success: HTTP 201.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: BAD_REQUEST(400) See

    Example:
        ```python
        >>> await subscribe_on_diagnostics_states_get_system_diagnostics(client)
        <Response [201]>
        ```
    """
    return await client.post(
        "/subscription",
        data={
            k: v
            for k, v in {"identifier": identifier, "identifier_p": identifier_p}.items()
            if v is not None
        },
    )


async def get_safety_resources(client: RWSClient) -> httpx.Response:
    """
    Get safety resources.

    Route: ``GET /ctrl/safety``
    ABB constraints: Supported in bootserver mode

    Args:
        client: Open RWSClient instance.

    Returns:
        Raw HTTP response. Expected success: HTTP 200.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: NOT_FOUND(404) See

    Example:
        ```python
        >>> await get_safety_resources(client)
        <Response [200]>
        ```
    """
    return await client.get("/ctrl/safety")


async def get_safety_actions(
    client: RWSClient,
    action: str | None = None,
) -> httpx.Response:
    """
    Get Safety actions.

    Route: ``GET /ctrl/safety``
    ABB constraints: Not supported in bootserver mode

    Args:
        client: Open RWSClient instance.
        action: Query parameter. Optional.

    Returns:
        Raw HTTP response. Expected success: HTTP 200.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: BAD_REQUEST(400), see

    Example:
        ```python
        >>> await get_safety_actions(client)
        <Response [200]>
        ```
    """
    return await client.get(
        "/ctrl/safety",
        params={k: v for k, v in {"action": action}.items() if v is not None},
    )


async def load_safety_configuration_file_to_controller(
    client: RWSClient,
    action: str | None = None,
) -> httpx.Response:
    """
    Load Safety configuration file to controller.

    Route: ``POST /ctrl/safety``
    ABB constraints: Not supported in bootserver mode

    Args:
        client: Open RWSClient instance.
        action: Query parameter. Optional.

    Returns:
        Raw HTTP response. Expected success: HTTP 200.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: BAD_REQUEST(400), NOT_FOUND(404), FORBIDDEN(403)

    Example:
        ```python
        >>> await load_safety_configuration_file_to_controller(client)
        <Response [200]>
        ```
    """
    return await client.post(
        "/ctrl/safety",
        params={k: v for k, v in {"action": action}.items() if v is not None},
        data={},
    )


async def set_safety_mode_of_the_controller(
    client: RWSClient,
    action: str | None = None,
    mode: str | None = None,
) -> httpx.Response:
    """
    Set Safety Mode of the controller.

    Route: ``POST /ctrl/safety``
    ABB constraints: Not supported in bootserver mode Controller shoul be in manual mode

    Args:
        client: Open RWSClient instance.
        action: Query parameter. Optional.
        mode: Body parameter. Optional.

    Returns:
        Raw HTTP response. Expected success: HTTP 204.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: BAD_REQUEST(400), FORBIDDEN(403)

    Example:
        ```python
        >>> await set_safety_mode_of_the_controller(client)
        <Response [204]>
        ```
    """
    return await client.post(
        "/ctrl/safety",
        params={k: v for k, v in {"action": action}.items() if v is not None},
        data={k: v for k, v in {"mode": mode}.items() if v is not None},
    )


async def get_config_status(
    client: RWSClient,
    resource: str | None = None,
) -> httpx.Response:
    """
    Get Config status.

    Route: ``GET /ctrl/safety``
    ABB constraints: Not supported in bootserver mode

    Args:
        client: Open RWSClient instance.
        resource: Query parameter. Optional.

    Returns:
        Raw HTTP response. Expected success: HTTP 200.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: BAD_REQUEST(400), see

    Example:
        ```python
        >>> await get_config_status(client)
        <Response [200]>
        ```
    """
    return await client.get(
        "/ctrl/safety",
        params={k: v for k, v in {"resource": resource}.items() if v is not None},
    )


async def get_safety_mode_status(
    client: RWSClient,
    resource: str | None = None,
) -> httpx.Response:
    """
    Get Safety Mode status.

    Route: ``GET /ctrl/safety``
    ABB constraints: Not supported in bootserver mode

    Args:
        client: Open RWSClient instance.
        resource: Query parameter. Optional.

    Returns:
        Raw HTTP response. Expected success: HTTP 200.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: BAD_REQUEST(400), see

    Example:
        ```python
        >>> await get_safety_mode_status(client)
        <Response [200]>
        ```
    """
    return await client.get(
        "/ctrl/safety",
        params={k: v for k, v in {"resource": resource}.items() if v is not None},
    )


async def get_safety_cyclic_brake_check_status(
    client: RWSClient,
    resource: str | None = None,
    drivenum: str | None = None,
) -> httpx.Response:
    """
    Get Safety Cyclic Brake Check status.

    Route: ``GET /ctrl/safety``
    ABB constraints: Not supported in bootserver mode

    Args:
        client: Open RWSClient instance.
        resource: Query parameter. Optional.
        drivenum: Query parameter. Optional.

    Returns:
        Raw HTTP response. Expected success: HTTP 200.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: BAD_REQUEST(400), see

    Example:
        ```python
        >>> await get_safety_cyclic_brake_check_status(client)
        <Response [200]>
        ```
    """
    return await client.get(
        "/ctrl/safety",
        params={
            k: v for k, v in {"resource": resource, "drivenum": drivenum}.items() if v is not None
        },
    )


async def get_loadoperation_status(
    client: RWSClient,
    resource: str | None = None,
) -> httpx.Response:
    """
    Get LoadOperation status.

    Route: ``GET /ctrl/safety``
    ABB constraints: Not supported in bootserver mode User should have Safety services privileges

    Args:
        client: Open RWSClient instance.
        resource: Query parameter. Optional.

    Returns:
        Raw HTTP response. Expected success: HTTP 200.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: BAD_REQUEST(400), FORBIDDEN(403)

    Example:
        ```python
        >>> await get_loadoperation_status(client)
        <Response [200]>
        ```
    """
    return await client.get(
        "/ctrl/safety",
        params={k: v for k, v in {"resource": resource}.items() if v is not None},
    )


async def get_safety_configurations(
    client: RWSClient,
    resource: str | None = None,
) -> httpx.Response:
    """
    Get Safety Configurations.

    Route: ``GET /ctrl/safety``
    ABB constraints: Not supported in bootserver mode

    Args:
        client: Open RWSClient instance.
        resource: Query parameter. Optional.

    Returns:
        Raw HTTP response. Expected success: HTTP 200.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.

    Example:
        ```python
        >>> await get_safety_configurations(client)
        <Response [200]>
        ```
    """
    return await client.get(
        "/ctrl/safety",
        params={k: v for k, v in {"resource": resource}.items() if v is not None},
    )


async def get_safety_violation_info(
    client: RWSClient,
    resource: str | None = None,
) -> httpx.Response:
    """
    Get Safety Violation Info.

    Route: ``GET /ctrl/safety``
    ABB constraints: Not supported in bootserver mode User should have Safety services privileges

    Args:
        client: Open RWSClient instance.
        resource: Query parameter. Optional.

    Returns:
        Raw HTTP response. Expected success: HTTP 200.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: BAD_REQUEST(400), FORBIDDEN(403)

    Example:
        ```python
        >>> await get_safety_violation_info(client)
        <Response [200]>
        ```
    """
    return await client.get(
        "/ctrl/safety",
        params={k: v for k, v in {"resource": resource}.items() if v is not None},
    )


async def post_unlock_the_safety_configuration(
    client: RWSClient,
    action: str | None = None,
) -> httpx.Response:
    """
    Unlock the safety configuration.

    Route: ``POST /ctrl/safety``
    ABB constraints: Not supported in bootserver mode

    Args:
        client: Open RWSClient instance.
        action: Query parameter. Optional.

    Returns:
        Raw HTTP response. Expected success: HTTP 204.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: BAD_REQUEST(400), NOT_FOUND(404), FORBIDDEN(403)

    Example:
        ```python
        >>> await post_unlock_the_safety_configuration(client)
        <Response [204]>
        ```
    """
    return await client.post(
        "/ctrl/safety",
        params={k: v for k, v in {"action": action}.items() if v is not None},
        data={},
    )


async def post_software_sync_acknowledgement(
    client: RWSClient,
    action: str | None = None,
    index: str | None = None,
) -> httpx.Response:
    """
    Software Sync Acknowledgement.

    Route: ``POST /ctrl/safety``
    ABB constraints: Not supported in bootserver mode

    Args:
        client: Open RWSClient instance.
        action: Query parameter. Optional.
        index: Body parameter. Optional.

    Returns:
        Raw HTTP response. Expected success: HTTP 204.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: BAD_REQUEST(400), NOT_FOUND(404), FORBIDDEN(403)

    Example:
        ```python
        >>> await post_software_sync_acknowledgement(client)
        <Response [204]>
        ```
    """
    return await client.post(
        "/ctrl/safety",
        params={k: v for k, v in {"action": action}.items() if v is not None},
        data={k: v for k, v in {"index": index}.items() if v is not None},
    )


async def add_validation_info(
    client: RWSClient,
    action: str | None = None,
    validated_by: str | None = None,
) -> httpx.Response:
    """
    Add Validation info.

    Route: ``POST /ctrl/safety``
    ABB constraints: Must have grant UAS_SAFETY_SERVICES for method to succeed Not supported in
        bootserver mode

    Args:
        client: Open RWSClient instance.
        action: Query parameter. Optional.
        validated_by: Body parameter. Optional.

    Returns:
        Raw HTTP response. Expected success: HTTP 204.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: BAD_REQUEST(400),FORBIDDEN(403)

    Example:
        ```python
        >>> await add_validation_info(client)
        <Response [204]>
        ```
    """
    return await client.post(
        "/ctrl/safety",
        params={k: v for k, v in {"action": action}.items() if v is not None},
        data={k: v for k, v in {"validated_by": validated_by}.items() if v is not None},
    )


async def remove_validation_info(
    client: RWSClient,
    action: str | None = None,
) -> httpx.Response:
    """
    Remove Validation info.

    Route: ``POST /ctrl/safety``
    ABB constraints: Must have grant UAS_SAFETY_SERVICES for method to succeed Not supported in
        bootserver mode

    Args:
        client: Open RWSClient instance.
        action: Query parameter. Optional.

    Returns:
        Raw HTTP response. Expected success: HTTP 204.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: BAD_REQUEST(400), FORBIDDEN(403)

    Example:
        ```python
        >>> await remove_validation_info(client)
        <Response [204]>
        ```
    """
    return await client.post(
        "/ctrl/safety",
        params={k: v for k, v in {"action": action}.items() if v is not None},
        data={},
    )


async def set_reset_safety_controller(
    client: RWSClient,
    action: str | None = None,
) -> httpx.Response:
    """
    Set Reset Safety Controller.

    Route: ``POST /ctrl/safety``
    ABB constraints: Not supported in bootserver mode User should have Safety services privileges

    Args:
        client: Open RWSClient instance.
        action: Query parameter. Optional.

    Returns:
        Raw HTTP response. Expected success: HTTP 204.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: BAD_REQUEST(400), FORBIDDEN(403)

    Example:
        ```python
        >>> await set_reset_safety_controller(client)
        <Response [204]>
        ```
    """
    return await client.post(
        "/ctrl/safety",
        params={k: v for k, v in {"action": action}.items() if v is not None},
        data={},
    )


async def get_options_resource(
    client: RWSClient,
    option_to_verify: str,
) -> httpx.Response:
    """
    Get options resource.

    Route: ``GET /ctrl/options/{option to verify}``
    ABB constraints: The option to be verified is to provided as part of the request url. Also,
        the url is case sensitive. see example above. Not Supported in bootserver mode

    Args:
        client: Open RWSClient instance.
        option_to_verify: URL path parameter.

    Returns:
        Raw HTTP response. Expected success: HTTP 200.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: NOT_FOUND(404)

    Example:
        ```python
        >>> await get_options_resource(client, "option_to_verify_value")
        <Response [200]>
        ```
    """
    return await client.get(f"/ctrl/options/{option_to_verify}")


async def get_check_robotware_version_compatibility_with_contorller_hardware(
    client: RWSClient,
    robotware_version: str,
) -> httpx.Response:
    """
    Check Robotware version compatibility with contorller hardware.

    Route: ``GET /ctrl/compatibility/{robotware version}``
    ABB constraints: Supported only in RC. Supported in bootserver mode.

    Args:
        client: Open RWSClient instance.
        robotware_version: URL path parameter.

    Returns:
        Raw HTTP response. Expected success: HTTP 200.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: NOT FOUND(404), BAD REQUEST(400)

    Example:
        ```python
        >>> await get_check_robotware_version_compatibility_with_contorller_hardware(
        ...     client,
        ...     "robotware_version_value"
        ... )
        <Response [200]>
        ```
    """
    return await client.get(f"/ctrl/compatibility/{robotware_version}")


async def get_virtual_time_resources(client: RWSClient) -> httpx.Response:
    """
    Get Virtual Time resources.

    Route: ``GET /ctrl/virtualtime``
    ABB constraints: Not supported in bootserver mode Supported only in VC.

    Args:
        client: Open RWSClient instance.

    Returns:
        Raw HTTP response. Expected success: HTTP 200.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: NOT_FOUND(404), see

    Example:
        ```python
        >>> await get_virtual_time_resources(client)
        <Response [200]>
        ```
    """
    return await client.get("/ctrl/virtualtime")


async def get_virtualtime(client: RWSClient) -> httpx.Response:
    """
    Get Virtualtime.

    Route: ``GET /ctrl/virtualtime/vttime``
    ABB constraints: Supported only in VC mode

    Args:
        client: Open RWSClient instance.

    Returns:
        Raw HTTP response. Expected success: HTTP 200.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: Not Found(404)

    Example:
        ```python
        >>> await get_virtualtime(client)
        <Response [200]>
        ```
    """
    return await client.get("/ctrl/virtualtime/vttime")


async def get_vttimeslice_value(client: RWSClient) -> httpx.Response:
    """
    Get VTTimeslice Value.

    Route: ``GET /ctrl/virtualtime/vttimeslice``
    ABB constraints: Supported only in VC. vttimeslice value is in ms. Not supported in
        bootserver mode.

    Args:
        client: Open RWSClient instance.

    Returns:
        Raw HTTP response. Expected success: HTTP 200.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: NOT FOUND(404),

    Example:
        ```python
        >>> await get_vttimeslice_value(client)
        <Response [200]>
        ```
    """
    return await client.get("/ctrl/virtualtime/vttimeslice")


async def get_actions_on_vttimeslice(
    client: RWSClient,
    action: str | None = None,
) -> httpx.Response:
    """
    Actions on VTTimeslice.

    Route: ``GET /ctrl/virtualtime/vttimeslice``
    ABB constraints: Supported only in VC. vttimeslice value is in ms. Not supported in
        bootserver mode.

    Args:
        client: Open RWSClient instance.
        action: Query parameter. Optional.

    Returns:
        Raw HTTP response. Expected success: HTTP 200.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: BAD REQUEST(400)

    Example:
        ```python
        >>> await get_actions_on_vttimeslice(client)
        <Response [200]>
        ```
    """
    return await client.get(
        "/ctrl/virtualtime/vttimeslice",
        params={k: v for k, v in {"action": action}.items() if v is not None},
    )


async def set_vttimeslice_value(
    client: RWSClient,
    vttimeslice: str | None = None,
) -> httpx.Response:
    """
    Set VTTimeslice Value.

    Route: ``POST /ctrl/virtualtime/vttimeslice``
    ABB constraints: Supported only in VC. Value should be in ms. For values < 10 ms. vttimeslice
        will be set to a default value of 10 ms. Not supported in bootserver mode.

    Args:
        client: Open RWSClient instance.
        vttimeslice: Body parameter. Optional.

    Returns:
        Raw HTTP response. Expected success: HTTP 204.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: NOT FOUND(404)

    Example:
        ```python
        >>> await set_vttimeslice_value(client)
        <Response [204]>
        ```
    """
    return await client.post(
        "/ctrl/virtualtime/vttimeslice",
        data={k: v for k, v in {"vttimeslice": vttimeslice}.items() if v is not None},
    )


async def get_speed_of_virtual_time(client: RWSClient) -> httpx.Response:
    """
    Get Speed of Virtual Time.

    Route: ``GET /ctrl/virtualtime/vtspeed``
    ABB constraints: Not supported in bootserver mode Supported only in VC.

    Args:
        client: Open RWSClient instance.

    Returns:
        Raw HTTP response. Expected success: HTTP 200.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: BAD_REQUEST(400)

    Example:
        ```python
        >>> await get_speed_of_virtual_time(client)
        <Response [200]>
        ```
    """
    return await client.get("/ctrl/virtualtime/vtspeed")


async def get_actions_on_vtspeed(
    client: RWSClient,
    action: str | None = None,
) -> httpx.Response:
    """
    Get actions on a VTSpeed.

    Route: ``GET /ctrl/virtualtime/vtspeed``
    ABB constraints: Not supported in bootserver mode Supported only in VC.

    Args:
        client: Open RWSClient instance.
        action: Query parameter. Optional.

    Returns:
        Raw HTTP response. Expected success: HTTP 200.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: HTTP Errors, see

    Example:
        ```python
        >>> await get_actions_on_vtspeed(client)
        <Response [200]>
        ```
    """
    return await client.get(
        "/ctrl/virtualtime/vtspeed",
        params={k: v for k, v in {"action": action}.items() if v is not None},
    )


async def set_speed_of_virtualtime(
    client: RWSClient,
    vtspeed: str | None = None,
) -> httpx.Response:
    """
    Set Speed of Virtualtime.

    Route: ``POST /ctrl/virtualtime/vtspeed``
    ABB constraints: Supported only in VC. Not supported in bootserver mode.

    Args:
        client: Open RWSClient instance.
        vtspeed: Body parameter. Optional.

    Returns:
        Raw HTTP response. Expected success: HTTP 204.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: BAD REQUEST(400)

    Example:
        ```python
        >>> await set_speed_of_virtualtime(client)
        <Response [204]>
        ```
    """
    return await client.post(
        "/ctrl/virtualtime/vtspeed",
        data={k: v for k, v in {"vtspeed": vtspeed}.items() if v is not None},
    )


async def get_state_of_virtual_time(client: RWSClient) -> httpx.Response:
    """
    Get State of Virtual Time.

    Route: ``GET /ctrl/virtualtime/vtstate``
    ABB constraints: Not supported in bootserver mode Supported only in VC.

    Args:
        client: Open RWSClient instance.

    Returns:
        Raw HTTP response. Expected success: HTTP 200.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: BAD_REQUEST(400)

    Example:
        ```python
        >>> await get_state_of_virtual_time(client)
        <Response [200]>
        ```
    """
    return await client.get("/ctrl/virtualtime/vtstate")


async def get_actions_on_vtstate(
    client: RWSClient,
    action: str | None = None,
) -> httpx.Response:
    """
    Get actions on a VTState.

    Route: ``GET /ctrl/virtualtime/vtstate``
    ABB constraints: Not supported in bootserver mode Supported only in VC.

    Args:
        client: Open RWSClient instance.
        action: Query parameter. Optional.

    Returns:
        Raw HTTP response. Expected success: HTTP 200.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: HTTP Errors, see

    Example:
        ```python
        >>> await get_actions_on_vtstate(client)
        <Response [200]>
        ```
    """
    return await client.get(
        "/ctrl/virtualtime/vtstate",
        params={k: v for k, v in {"action": action}.items() if v is not None},
    )


async def set_state_of_virtualtime(
    client: RWSClient,
    vtstate: str | None = None,
) -> httpx.Response:
    """
    Set State of Virtualtime.

    Route: ``POST /ctrl/virtualtime/vtstate``
    ABB constraints: Supported only in VC. Not supported in bootserver mode.

    Args:
        client: Open RWSClient instance.
        vtstate: Body parameter. Optional.

    Returns:
        Raw HTTP response. Expected success: HTTP 204.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: BAD REQUEST(400)

    Example:
        ```python
        >>> await set_state_of_virtualtime(client)
        <Response [204]>
        ```
    """
    return await client.post(
        "/ctrl/virtualtime/vtstate",
        data={k: v for k, v in {"vtstate": vtstate}.items() if v is not None},
    )


async def post_vt_run(client: RWSClient) -> httpx.Response:
    """
    VT Run.

    Route: ``POST /ctrl/virtualtime/vtrun``
    ABB constraints: Supported only in VC. Not supported in bootserver mode.

    Args:
        client: Open RWSClient instance.

    Returns:
        Raw HTTP response. Expected success: HTTP 204.

    Raises:
        RWSAuthenticationError: On HTTP 401.
        RWSNotFoundError: On HTTP 404.
        RWSHTTPError: On any other HTTP >= 400.
        # ABB codes: BAD REQUEST(400)

    Example:
        ```python
        >>> await post_vt_run(client)
        <Response [204]>
        ```
    """
    return await client.post(
        "/ctrl/virtualtime/vtrun",
        data={},
    )
