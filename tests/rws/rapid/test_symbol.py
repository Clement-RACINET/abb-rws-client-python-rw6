# This file is AUTO-GENERATED by contrib/generator/main.py
# DO NOT EDIT MANUALLY — run the generator to regenerate.
"""Tests unitaires auto-générés pour rws/rapid/symbol."""
from __future__ import annotations

import pytest
import httpx

from abb_rws_client._core.client import RWSClient
from abb_rws_client.rws.rapid.symbol import (
    get_rapid_symbol_properties,
    get_rapid_symbol_data,
    get_rapid_symbol_data_actions,
    update_rapid_variable_current_value,
    validate_rapid_variable,
    subscribe_on_rapid_persistent_variable,
    update_rapid_variable_initial_value,
)


class _MockTransport(httpx.AsyncBaseTransport):
    """Transport mock retournant une réponse HTTP configurable."""

    def __init__(self, status_code: int = 200, content: bytes = b"") -> None:
        self.status_code = status_code
        self.content = content
        self.last_request: httpx.Request | None = None

    async def handle_async_request(self, request: httpx.Request) -> httpx.Response:
        self.last_request = request
        return httpx.Response(self.status_code, content=self.content, request=request)


def _make_client(transport: _MockTransport) -> RWSClient:
    """Construit un RWSClient branché sur le transport mock."""
    client = RWSClient.__new__(RWSClient)
    client._http = httpx.AsyncClient(
        transport=transport,
        base_url="http://localhost/",
    )
    client.host = "localhost"
    return client


@pytest.mark.asyncio
async def test_get_rapid_symbol_properties() -> None:
    """Vérifie que get_rapid_symbol_properties émet GET /rw/rapid/symbol/properties/{symbolurl}."""
    transport = _MockTransport(status_code=200)
    client = _make_client(transport)

    resp = await get_rapid_symbol_properties(client, "symbolurl_test")

    assert transport.last_request is not None
    assert transport.last_request.method == "GET"
    assert transport.last_request.url.path == "/rw/rapid/symbol/properties/symbolurl_test"
    assert resp.status_code == 200

@pytest.mark.asyncio
async def test_get_rapid_symbol_data() -> None:
    """Vérifie que get_rapid_symbol_data émet GET /rw/rapid/symbol/data/{symbolurl}."""
    transport = _MockTransport(status_code=200)
    client = _make_client(transport)

    resp = await get_rapid_symbol_data(client, "symbolurl_test")

    assert transport.last_request is not None
    assert transport.last_request.method == "GET"
    assert transport.last_request.url.path == "/rw/rapid/symbol/data/symbolurl_test"
    assert resp.status_code == 200

@pytest.mark.asyncio
async def test_get_rapid_symbol_data_actions() -> None:
    """Vérifie que get_rapid_symbol_data_actions émet GET /rw/rapid/symbols/{symbolurl}."""
    transport = _MockTransport(status_code=200)
    client = _make_client(transport)

    resp = await get_rapid_symbol_data_actions(client, "symbolurl_test")

    assert transport.last_request is not None
    assert transport.last_request.method == "GET"
    assert transport.last_request.url.path == "/rw/rapid/symbols/symbolurl_test"
    assert resp.status_code == 200

@pytest.mark.asyncio
async def test_update_rapid_variable_current_value() -> None:
    """Vérifie que update_rapid_variable_current_value émet POST /rw/rapid/symbol/data/{symbolurl}."""
    transport = _MockTransport(status_code=204)
    client = _make_client(transport)

    resp = await update_rapid_variable_current_value(client, "symbolurl_test")

    assert transport.last_request is not None
    assert transport.last_request.method == "POST"
    assert transport.last_request.url.path == "/rw/rapid/symbol/data/symbolurl_test"
    assert resp.status_code == 204

@pytest.mark.asyncio
async def test_validate_rapid_variable() -> None:
    """Vérifie que validate_rapid_variable émet POST /rw/rapid/symbol/data."""
    transport = _MockTransport(status_code=204)
    client = _make_client(transport)

    resp = await validate_rapid_variable(client)

    assert transport.last_request is not None
    assert transport.last_request.method == "POST"
    assert transport.last_request.url.path == "/rw/rapid/symbol/data"
    assert resp.status_code == 204

@pytest.mark.asyncio
async def test_subscribe_on_rapid_persistent_variable() -> None:
    """Vérifie que subscribe_on_rapid_persistent_variable émet POST /subscription."""
    transport = _MockTransport(status_code=201)
    client = _make_client(transport)

    resp = await subscribe_on_rapid_persistent_variable(client)

    assert transport.last_request is not None
    assert transport.last_request.method == "POST"
    assert transport.last_request.url.path == "/subscription"
    assert resp.status_code == 201

@pytest.mark.asyncio
async def test_update_rapid_variable_initial_value() -> None:
    """Vérifie que update_rapid_variable_initial_value émet POST /rw/rapid/symbol/data/{symbolurl}."""
    transport = _MockTransport(status_code=204)
    client = _make_client(transport)

    resp = await update_rapid_variable_initial_value(client, "symbolurl_test")

    assert transport.last_request is not None
    assert transport.last_request.method == "POST"
    assert transport.last_request.url.path == "/rw/rapid/symbol/data/symbolurl_test"
    assert resp.status_code == 204
