# This file is AUTO-GENERATED by contrib/generator/main.py
# DO NOT EDIT MANUALLY — run the generator to regenerate.
"""Tests unitaires auto-générés pour rws/ctrl/backup."""
from __future__ import annotations

import pytest
import httpx

from abb_rws_client._core.client import RWSClient
from abb_rws_client.rws.ctrl.backup import (
    get_backup_resources,
    get_backup_actions,
    create_backup,
    post_restore_backup,
    get_check_restore,
    get_backup_state,
    subscribe_on_backup_system_information,
    get_backup_system_information,
)


class _MockTransport(httpx.AsyncBaseTransport):
    """Transport mock retournant une réponse HTTP configurable."""

    def __init__(self, status_code: int = 200, content: bytes = b"") -> None:
        self.status_code = status_code
        self.content = content
        self.last_request: httpx.Request | None = None

    async def handle_async_request(self, request: httpx.Request) -> httpx.Response:
        self.last_request = request
        return httpx.Response(self.status_code, content=self.content, request=request)


def _make_client(transport: _MockTransport) -> RWSClient:
    """Construit un RWSClient branché sur le transport mock."""
    client = RWSClient.__new__(RWSClient)
    client._http = httpx.AsyncClient(
        transport=transport,
        base_url="http://localhost/",
    )
    client.host = "localhost"
    return client


@pytest.mark.asyncio
async def test_get_backup_resources() -> None:
    """Vérifie que get_backup_resources émet GET /ctrl/backup."""
    transport = _MockTransport(status_code=200)
    client = _make_client(transport)

    resp = await get_backup_resources(client)

    assert transport.last_request is not None
    assert transport.last_request.method == "GET"
    assert transport.last_request.url.path == "/ctrl/backup"
    assert resp.status_code == 200

@pytest.mark.asyncio
async def test_get_backup_actions() -> None:
    """Vérifie que get_backup_actions émet GET /ctrl/backup."""
    transport = _MockTransport(status_code=200)
    client = _make_client(transport)

    resp = await get_backup_actions(client)

    assert transport.last_request is not None
    assert transport.last_request.method == "GET"
    assert transport.last_request.url.path == "/ctrl/backup"
    assert resp.status_code == 200

@pytest.mark.asyncio
async def test_create_backup() -> None:
    """Vérifie que create_backup émet POST /ctrl/backup."""
    transport = _MockTransport(status_code=202)
    client = _make_client(transport)

    resp = await create_backup(client)

    assert transport.last_request is not None
    assert transport.last_request.method == "POST"
    assert transport.last_request.url.path == "/ctrl/backup"
    assert resp.status_code == 202

@pytest.mark.asyncio
async def test_post_restore_backup() -> None:
    """Vérifie que post_restore_backup émet POST /ctrl/backup."""
    transport = _MockTransport(status_code=204)
    client = _make_client(transport)

    resp = await post_restore_backup(client)

    assert transport.last_request is not None
    assert transport.last_request.method == "POST"
    assert transport.last_request.url.path == "/ctrl/backup"
    assert resp.status_code == 204

@pytest.mark.asyncio
async def test_get_check_restore() -> None:
    """Vérifie que get_check_restore émet GET /ctrl/backup."""
    transport = _MockTransport(status_code=200)
    client = _make_client(transport)

    resp = await get_check_restore(client)

    assert transport.last_request is not None
    assert transport.last_request.method == "GET"
    assert transport.last_request.url.path == "/ctrl/backup"
    assert resp.status_code == 200

@pytest.mark.asyncio
async def test_get_backup_state() -> None:
    """Vérifie que get_backup_state émet GET /ctrl/backup."""
    transport = _MockTransport(status_code=200)
    client = _make_client(transport)

    resp = await get_backup_state(client)

    assert transport.last_request is not None
    assert transport.last_request.method == "GET"
    assert transport.last_request.url.path == "/ctrl/backup"
    assert resp.status_code == 200

@pytest.mark.asyncio
async def test_subscribe_on_backup_system_information() -> None:
    """Vérifie que subscribe_on_backup_system_information émet POST /subscription."""
    transport = _MockTransport(status_code=201)
    client = _make_client(transport)

    resp = await subscribe_on_backup_system_information(client)

    assert transport.last_request is not None
    assert transport.last_request.method == "POST"
    assert transport.last_request.url.path == "/subscription"
    assert resp.status_code == 201

@pytest.mark.asyncio
async def test_get_backup_system_information() -> None:
    """Vérifie que get_backup_system_information émet GET /ctrl/backup/info/."""
    transport = _MockTransport(status_code=200)
    client = _make_client(transport)

    resp = await get_backup_system_information(client)

    assert transport.last_request is not None
    assert transport.last_request.method == "GET"
    assert transport.last_request.url.path == "/ctrl/backup/info/"
    assert resp.status_code == 200
