# This file is AUTO-GENERATED by contrib/generator/main.py
# DO NOT EDIT MANUALLY — run the generator to regenerate.
"""Tests unitaires auto-générés pour rws/ctrl/ctrl."""
from __future__ import annotations

import pytest
import httpx

from abb_rws_client._core.client import RWSClient
from abb_rws_client.rws.ctrl.ctrl import (
    get_controller_resources,
    get_controller_actions,
    get_controller_environment_variable,
    restart_or_shutdown_controller,
    set_controller_language,
    get_list_of_installed_systems,
    get_actions_on_system,
    set_boot_device,
    get_boot_device,
    load_boot_image,
    post_unload_boot_image,
    get_selected_system_name,
    post_install_deployment_package,
    validate_deployment_package,
    get_system_resource,
    get_actions_on_system_resource,
    post_rename_system,
    post_select_system,
    delete_system,
    post_deselect_system,
    get_network_resource,
    get_network_setting_actions,
    set_network_configuration,
    get_dns_resource,
    add_route_table_entry,
    options_options_to_add_route_table_entry,
    remove_route_table_entry,
    options_options_to_remove_route_table_entry,
    get_compress_resources,
    get_compress_actions,
    post_compress_decompress_resource,
    get_diagnostics_resources,
    get_diagnostics_actions,
    save_system_diagnostics,
    subscribe_on_system_dump,
    subscribe_on_diagnostics_states_get_system_diagnostics,
    get_safety_resources,
    get_safety_actions,
    load_safety_configuration_file_to_controller,
    set_safety_mode_of_the_controller,
    get_config_status,
    get_safety_mode_status,
    get_safety_cyclic_brake_check_status,
    get_loadoperation_status,
    get_safety_configurations,
    get_safety_violation_info,
    post_unlock_the_safety_configuration,
    post_software_sync_acknowledgement,
    add_validation_info,
    remove_validation_info,
    set_reset_safety_controller,
    get_options_resource,
    get_check_robotware_version_compatibility_with_contorller_hardware,
    get_virtual_time_resources,
    get_virtualtime,
    get_vttimeslice_value,
    get_actions_on_vttimeslice,
    set_vttimeslice_value,
    get_speed_of_virtual_time,
    get_actions_on_vtspeed,
    set_speed_of_virtualtime,
    get_state_of_virtual_time,
    get_actions_on_vtstate,
    set_state_of_virtualtime,
    post_vt_run,
)


class _MockTransport(httpx.AsyncBaseTransport):
    """Transport mock retournant une réponse HTTP configurable."""

    def __init__(self, status_code: int = 200, content: bytes = b"") -> None:
        self.status_code = status_code
        self.content = content
        self.last_request: httpx.Request | None = None

    async def handle_async_request(self, request: httpx.Request) -> httpx.Response:
        self.last_request = request
        return httpx.Response(self.status_code, content=self.content, request=request)


def _make_client(transport: _MockTransport) -> RWSClient:
    """Construit un RWSClient branché sur le transport mock."""
    client = RWSClient.__new__(RWSClient)
    client._http = httpx.AsyncClient(
        transport=transport,
        base_url="http://localhost/",
    )
    client.host = "localhost"
    return client


@pytest.mark.asyncio
async def test_get_controller_resources() -> None:
    """Vérifie que get_controller_resources émet GET /ctrl."""
    transport = _MockTransport(status_code=200)
    client = _make_client(transport)

    resp = await get_controller_resources(client)

    assert transport.last_request is not None
    assert transport.last_request.method == "GET"
    assert transport.last_request.url.path == "/ctrl"
    assert resp.status_code == 200

@pytest.mark.asyncio
async def test_get_controller_actions() -> None:
    """Vérifie que get_controller_actions émet GET /ctrl."""
    transport = _MockTransport(status_code=200)
    client = _make_client(transport)

    resp = await get_controller_actions(client)

    assert transport.last_request is not None
    assert transport.last_request.method == "GET"
    assert transport.last_request.url.path == "/ctrl"
    assert resp.status_code == 200

@pytest.mark.asyncio
async def test_get_controller_environment_variable() -> None:
    """Vérifie que get_controller_environment_variable émet GET /ctrl/${ENVNAME}."""
    transport = _MockTransport(status_code=200)
    client = _make_client(transport)

    resp = await get_controller_environment_variable(client, "envname_test")

    assert transport.last_request is not None
    assert transport.last_request.method == "GET"
    assert transport.last_request.url.path == "/ctrl/$envname_test"
    assert resp.status_code == 200

@pytest.mark.asyncio
async def test_restart_or_shutdown_controller() -> None:
    """Vérifie que restart_or_shutdown_controller émet POST /ctrl."""
    transport = _MockTransport(status_code=200)
    client = _make_client(transport)

    resp = await restart_or_shutdown_controller(client)

    assert transport.last_request is not None
    assert transport.last_request.method == "POST"
    assert transport.last_request.url.path == "/ctrl"
    assert resp.status_code == 200

@pytest.mark.asyncio
async def test_set_controller_language() -> None:
    """Vérifie que set_controller_language émet POST /ctrl."""
    transport = _MockTransport(status_code=204)
    client = _make_client(transport)

    resp = await set_controller_language(client)

    assert transport.last_request is not None
    assert transport.last_request.method == "POST"
    assert transport.last_request.url.path == "/ctrl"
    assert resp.status_code == 204

@pytest.mark.asyncio
async def test_get_list_of_installed_systems() -> None:
    """Vérifie que get_list_of_installed_systems émet GET /ctrl/system."""
    transport = _MockTransport(status_code=200)
    client = _make_client(transport)

    resp = await get_list_of_installed_systems(client)

    assert transport.last_request is not None
    assert transport.last_request.method == "GET"
    assert transport.last_request.url.path == "/ctrl/system"
    assert resp.status_code == 200

@pytest.mark.asyncio
async def test_get_actions_on_system() -> None:
    """Vérifie que get_actions_on_system émet GET /ctrl/system."""
    transport = _MockTransport(status_code=200)
    client = _make_client(transport)

    resp = await get_actions_on_system(client)

    assert transport.last_request is not None
    assert transport.last_request.method == "GET"
    assert transport.last_request.url.path == "/ctrl/system"
    assert resp.status_code == 200

@pytest.mark.asyncio
async def test_set_boot_device() -> None:
    """Vérifie que set_boot_device émet POST /ctrl/system."""
    transport = _MockTransport(status_code=204)
    client = _make_client(transport)

    resp = await set_boot_device(client)

    assert transport.last_request is not None
    assert transport.last_request.method == "POST"
    assert transport.last_request.url.path == "/ctrl/system"
    assert resp.status_code == 204

@pytest.mark.asyncio
async def test_get_boot_device() -> None:
    """Vérifie que get_boot_device émet GET /ctrl/system."""
    transport = _MockTransport(status_code=200)
    client = _make_client(transport)

    resp = await get_boot_device(client)

    assert transport.last_request is not None
    assert transport.last_request.method == "GET"
    assert transport.last_request.url.path == "/ctrl/system"
    assert resp.status_code == 200

@pytest.mark.asyncio
async def test_load_boot_image() -> None:
    """Vérifie que load_boot_image émet POST /ctrl/system."""
    transport = _MockTransport(status_code=202)
    client = _make_client(transport)

    resp = await load_boot_image(client)

    assert transport.last_request is not None
    assert transport.last_request.method == "POST"
    assert transport.last_request.url.path == "/ctrl/system"
    assert resp.status_code == 202

@pytest.mark.asyncio
async def test_post_unload_boot_image() -> None:
    """Vérifie que post_unload_boot_image émet POST /ctrl/system."""
    transport = _MockTransport(status_code=204)
    client = _make_client(transport)

    resp = await post_unload_boot_image(client)

    assert transport.last_request is not None
    assert transport.last_request.method == "POST"
    assert transport.last_request.url.path == "/ctrl/system"
    assert resp.status_code == 204

@pytest.mark.asyncio
async def test_get_selected_system_name() -> None:
    """Vérifie que get_selected_system_name émet GET /ctrl/system."""
    transport = _MockTransport(status_code=200)
    client = _make_client(transport)

    resp = await get_selected_system_name(client)

    assert transport.last_request is not None
    assert transport.last_request.method == "GET"
    assert transport.last_request.url.path == "/ctrl/system"
    assert resp.status_code == 200

@pytest.mark.asyncio
async def test_post_install_deployment_package() -> None:
    """Vérifie que post_install_deployment_package émet POST /ctrl/system/installdpkg."""
    transport = _MockTransport(status_code=202)
    client = _make_client(transport)

    resp = await post_install_deployment_package(client)

    assert transport.last_request is not None
    assert transport.last_request.method == "POST"
    assert transport.last_request.url.path == "/ctrl/system/installdpkg"
    assert resp.status_code == 202

@pytest.mark.asyncio
async def test_validate_deployment_package() -> None:
    """Vérifie que validate_deployment_package émet POST /ctrl/system/validatedpkg."""
    transport = _MockTransport(status_code=204)
    client = _make_client(transport)

    resp = await validate_deployment_package(client)

    assert transport.last_request is not None
    assert transport.last_request.method == "POST"
    assert transport.last_request.url.path == "/ctrl/system/validatedpkg"
    assert resp.status_code == 204

@pytest.mark.asyncio
async def test_get_system_resource() -> None:
    """Vérifie que get_system_resource émet GET /ctrl/system/{system-name}."""
    transport = _MockTransport(status_code=200)
    client = _make_client(transport)

    resp = await get_system_resource(client, "system_name_test")

    assert transport.last_request is not None
    assert transport.last_request.method == "GET"
    assert transport.last_request.url.path == "/ctrl/system/system_name_test"
    assert resp.status_code == 200

@pytest.mark.asyncio
async def test_get_actions_on_system_resource() -> None:
    """Vérifie que get_actions_on_system_resource émet GET /ctrl/system/{system-name}."""
    transport = _MockTransport(status_code=200)
    client = _make_client(transport)

    resp = await get_actions_on_system_resource(client, "system_name_test")

    assert transport.last_request is not None
    assert transport.last_request.method == "GET"
    assert transport.last_request.url.path == "/ctrl/system/system_name_test"
    assert resp.status_code == 200

@pytest.mark.asyncio
async def test_post_rename_system() -> None:
    """Vérifie que post_rename_system émet POST /ctrl/system/{system-name}."""
    transport = _MockTransport(status_code=201)
    client = _make_client(transport)

    resp = await post_rename_system(client, "system_name_test")

    assert transport.last_request is not None
    assert transport.last_request.method == "POST"
    assert transport.last_request.url.path == "/ctrl/system/system_name_test"
    assert resp.status_code == 201

@pytest.mark.asyncio
async def test_post_select_system() -> None:
    """Vérifie que post_select_system émet POST /ctrl/system/{system-name}."""
    transport = _MockTransport(status_code=200)
    client = _make_client(transport)

    resp = await post_select_system(client, "system_name_test")

    assert transport.last_request is not None
    assert transport.last_request.method == "POST"
    assert transport.last_request.url.path == "/ctrl/system/system_name_test"
    assert resp.status_code == 200

@pytest.mark.asyncio
async def test_delete_system() -> None:
    """Vérifie que delete_system émet DELETE /ctrl/system/{system-name}."""
    transport = _MockTransport(status_code=204)
    client = _make_client(transport)

    resp = await delete_system(client, "system_name_test")

    assert transport.last_request is not None
    assert transport.last_request.method == "DELETE"
    assert transport.last_request.url.path == "/ctrl/system/system_name_test"
    assert resp.status_code == 204

@pytest.mark.asyncio
async def test_post_deselect_system() -> None:
    """Vérifie que post_deselect_system émet POST /ctrl/system."""
    transport = _MockTransport(status_code=204)
    client = _make_client(transport)

    resp = await post_deselect_system(client)

    assert transport.last_request is not None
    assert transport.last_request.method == "POST"
    assert transport.last_request.url.path == "/ctrl/system"
    assert resp.status_code == 204

@pytest.mark.asyncio
async def test_get_network_resource() -> None:
    """Vérifie que get_network_resource émet GET /ctrl/network."""
    transport = _MockTransport(status_code=200)
    client = _make_client(transport)

    resp = await get_network_resource(client)

    assert transport.last_request is not None
    assert transport.last_request.method == "GET"
    assert transport.last_request.url.path == "/ctrl/network"
    assert resp.status_code == 200

@pytest.mark.asyncio
async def test_get_network_setting_actions() -> None:
    """Vérifie que get_network_setting_actions émet GET /ctrl/network."""
    transport = _MockTransport(status_code=200)
    client = _make_client(transport)

    resp = await get_network_setting_actions(client)

    assert transport.last_request is not None
    assert transport.last_request.method == "GET"
    assert transport.last_request.url.path == "/ctrl/network"
    assert resp.status_code == 200

@pytest.mark.asyncio
async def test_set_network_configuration() -> None:
    """Vérifie que set_network_configuration émet POST /ctrl/network."""
    transport = _MockTransport(status_code=202)
    client = _make_client(transport)

    resp = await set_network_configuration(client)

    assert transport.last_request is not None
    assert transport.last_request.method == "POST"
    assert transport.last_request.url.path == "/ctrl/network"
    assert resp.status_code == 202

@pytest.mark.asyncio
async def test_get_dns_resource() -> None:
    """Vérifie que get_dns_resource émet GET /ctrl/network/dns."""
    transport = _MockTransport(status_code=200)
    client = _make_client(transport)

    resp = await get_dns_resource(client)

    assert transport.last_request is not None
    assert transport.last_request.method == "GET"
    assert transport.last_request.url.path == "/ctrl/network/dns"
    assert resp.status_code == 200

@pytest.mark.asyncio
async def test_add_route_table_entry() -> None:
    """Vérifie que add_route_table_entry émet POST /ctrl/network/route/add."""
    transport = _MockTransport(status_code=204)
    client = _make_client(transport)

    resp = await add_route_table_entry(client)

    assert transport.last_request is not None
    assert transport.last_request.method == "POST"
    assert transport.last_request.url.path == "/ctrl/network/route/add"
    assert resp.status_code == 204

@pytest.mark.asyncio
async def test_options_options_to_add_route_table_entry() -> None:
    """Vérifie que options_options_to_add_route_table_entry émet OPTIONS /ctrl/network/route/add."""
    transport = _MockTransport(status_code=200)
    client = _make_client(transport)

    resp = await options_options_to_add_route_table_entry(client)

    assert transport.last_request is not None
    assert transport.last_request.method == "OPTIONS"
    assert transport.last_request.url.path == "/ctrl/network/route/add"
    assert resp.status_code == 200

@pytest.mark.asyncio
async def test_remove_route_table_entry() -> None:
    """Vérifie que remove_route_table_entry émet POST /ctrl/network/route/remove."""
    transport = _MockTransport(status_code=202)
    client = _make_client(transport)

    resp = await remove_route_table_entry(client)

    assert transport.last_request is not None
    assert transport.last_request.method == "POST"
    assert transport.last_request.url.path == "/ctrl/network/route/remove"
    assert resp.status_code == 202

@pytest.mark.asyncio
async def test_options_options_to_remove_route_table_entry() -> None:
    """Vérifie que options_options_to_remove_route_table_entry émet OPTIONS /ctrl/network/route/remove."""
    transport = _MockTransport(status_code=200)
    client = _make_client(transport)

    resp = await options_options_to_remove_route_table_entry(client)

    assert transport.last_request is not None
    assert transport.last_request.method == "OPTIONS"
    assert transport.last_request.url.path == "/ctrl/network/route/remove"
    assert resp.status_code == 200

@pytest.mark.asyncio
async def test_get_compress_resources() -> None:
    """Vérifie que get_compress_resources émet GET /ctrl/compress."""
    transport = _MockTransport(status_code=200)
    client = _make_client(transport)

    resp = await get_compress_resources(client)

    assert transport.last_request is not None
    assert transport.last_request.method == "GET"
    assert transport.last_request.url.path == "/ctrl/compress"
    assert resp.status_code == 200

@pytest.mark.asyncio
async def test_get_compress_actions() -> None:
    """Vérifie que get_compress_actions émet GET /ctrl/compress."""
    transport = _MockTransport(status_code=200)
    client = _make_client(transport)

    resp = await get_compress_actions(client)

    assert transport.last_request is not None
    assert transport.last_request.method == "GET"
    assert transport.last_request.url.path == "/ctrl/compress"
    assert resp.status_code == 200

@pytest.mark.asyncio
async def test_post_compress_decompress_resource() -> None:
    """Vérifie que post_compress_decompress_resource émet POST /ctrl/compress."""
    transport = _MockTransport(status_code=202)
    client = _make_client(transport)

    resp = await post_compress_decompress_resource(client)

    assert transport.last_request is not None
    assert transport.last_request.method == "POST"
    assert transport.last_request.url.path == "/ctrl/compress"
    assert resp.status_code == 202

@pytest.mark.asyncio
async def test_get_diagnostics_resources() -> None:
    """Vérifie que get_diagnostics_resources émet GET /ctrl/diagnostics."""
    transport = _MockTransport(status_code=200)
    client = _make_client(transport)

    resp = await get_diagnostics_resources(client)

    assert transport.last_request is not None
    assert transport.last_request.method == "GET"
    assert transport.last_request.url.path == "/ctrl/diagnostics"
    assert resp.status_code == 200

@pytest.mark.asyncio
async def test_get_diagnostics_actions() -> None:
    """Vérifie que get_diagnostics_actions émet GET /ctrl/diagnostics."""
    transport = _MockTransport(status_code=200)
    client = _make_client(transport)

    resp = await get_diagnostics_actions(client)

    assert transport.last_request is not None
    assert transport.last_request.method == "GET"
    assert transport.last_request.url.path == "/ctrl/diagnostics"
    assert resp.status_code == 200

@pytest.mark.asyncio
async def test_save_system_diagnostics() -> None:
    """Vérifie que save_system_diagnostics émet POST /ctrl/diagnostics."""
    transport = _MockTransport(status_code=202)
    client = _make_client(transport)

    resp = await save_system_diagnostics(client)

    assert transport.last_request is not None
    assert transport.last_request.method == "POST"
    assert transport.last_request.url.path == "/ctrl/diagnostics"
    assert resp.status_code == 202

@pytest.mark.asyncio
async def test_subscribe_on_system_dump() -> None:
    """Vérifie que subscribe_on_system_dump émet POST /subscription."""
    transport = _MockTransport(status_code=201)
    client = _make_client(transport)

    resp = await subscribe_on_system_dump(client)

    assert transport.last_request is not None
    assert transport.last_request.method == "POST"
    assert transport.last_request.url.path == "/subscription"
    assert resp.status_code == 201

@pytest.mark.asyncio
async def test_subscribe_on_diagnostics_states_get_system_diagnostics() -> None:
    """Vérifie que subscribe_on_diagnostics_states_get_system_diagnostics émet POST /subscription."""
    transport = _MockTransport(status_code=201)
    client = _make_client(transport)

    resp = await subscribe_on_diagnostics_states_get_system_diagnostics(client)

    assert transport.last_request is not None
    assert transport.last_request.method == "POST"
    assert transport.last_request.url.path == "/subscription"
    assert resp.status_code == 201

@pytest.mark.asyncio
async def test_get_safety_resources() -> None:
    """Vérifie que get_safety_resources émet GET /ctrl/safety."""
    transport = _MockTransport(status_code=200)
    client = _make_client(transport)

    resp = await get_safety_resources(client)

    assert transport.last_request is not None
    assert transport.last_request.method == "GET"
    assert transport.last_request.url.path == "/ctrl/safety"
    assert resp.status_code == 200

@pytest.mark.asyncio
async def test_get_safety_actions() -> None:
    """Vérifie que get_safety_actions émet GET /ctrl/safety."""
    transport = _MockTransport(status_code=200)
    client = _make_client(transport)

    resp = await get_safety_actions(client)

    assert transport.last_request is not None
    assert transport.last_request.method == "GET"
    assert transport.last_request.url.path == "/ctrl/safety"
    assert resp.status_code == 200

@pytest.mark.asyncio
async def test_load_safety_configuration_file_to_controller() -> None:
    """Vérifie que load_safety_configuration_file_to_controller émet POST /ctrl/safety."""
    transport = _MockTransport(status_code=200)
    client = _make_client(transport)

    resp = await load_safety_configuration_file_to_controller(client)

    assert transport.last_request is not None
    assert transport.last_request.method == "POST"
    assert transport.last_request.url.path == "/ctrl/safety"
    assert resp.status_code == 200

@pytest.mark.asyncio
async def test_set_safety_mode_of_the_controller() -> None:
    """Vérifie que set_safety_mode_of_the_controller émet POST /ctrl/safety."""
    transport = _MockTransport(status_code=204)
    client = _make_client(transport)

    resp = await set_safety_mode_of_the_controller(client)

    assert transport.last_request is not None
    assert transport.last_request.method == "POST"
    assert transport.last_request.url.path == "/ctrl/safety"
    assert resp.status_code == 204

@pytest.mark.asyncio
async def test_get_config_status() -> None:
    """Vérifie que get_config_status émet GET /ctrl/safety."""
    transport = _MockTransport(status_code=200)
    client = _make_client(transport)

    resp = await get_config_status(client)

    assert transport.last_request is not None
    assert transport.last_request.method == "GET"
    assert transport.last_request.url.path == "/ctrl/safety"
    assert resp.status_code == 200

@pytest.mark.asyncio
async def test_get_safety_mode_status() -> None:
    """Vérifie que get_safety_mode_status émet GET /ctrl/safety."""
    transport = _MockTransport(status_code=200)
    client = _make_client(transport)

    resp = await get_safety_mode_status(client)

    assert transport.last_request is not None
    assert transport.last_request.method == "GET"
    assert transport.last_request.url.path == "/ctrl/safety"
    assert resp.status_code == 200

@pytest.mark.asyncio
async def test_get_safety_cyclic_brake_check_status() -> None:
    """Vérifie que get_safety_cyclic_brake_check_status émet GET /ctrl/safety."""
    transport = _MockTransport(status_code=200)
    client = _make_client(transport)

    resp = await get_safety_cyclic_brake_check_status(client)

    assert transport.last_request is not None
    assert transport.last_request.method == "GET"
    assert transport.last_request.url.path == "/ctrl/safety"
    assert resp.status_code == 200

@pytest.mark.asyncio
async def test_get_loadoperation_status() -> None:
    """Vérifie que get_loadoperation_status émet GET /ctrl/safety."""
    transport = _MockTransport(status_code=200)
    client = _make_client(transport)

    resp = await get_loadoperation_status(client)

    assert transport.last_request is not None
    assert transport.last_request.method == "GET"
    assert transport.last_request.url.path == "/ctrl/safety"
    assert resp.status_code == 200

@pytest.mark.asyncio
async def test_get_safety_configurations() -> None:
    """Vérifie que get_safety_configurations émet GET /ctrl/safety."""
    transport = _MockTransport(status_code=200)
    client = _make_client(transport)

    resp = await get_safety_configurations(client)

    assert transport.last_request is not None
    assert transport.last_request.method == "GET"
    assert transport.last_request.url.path == "/ctrl/safety"
    assert resp.status_code == 200

@pytest.mark.asyncio
async def test_get_safety_violation_info() -> None:
    """Vérifie que get_safety_violation_info émet GET /ctrl/safety."""
    transport = _MockTransport(status_code=200)
    client = _make_client(transport)

    resp = await get_safety_violation_info(client)

    assert transport.last_request is not None
    assert transport.last_request.method == "GET"
    assert transport.last_request.url.path == "/ctrl/safety"
    assert resp.status_code == 200

@pytest.mark.asyncio
async def test_post_unlock_the_safety_configuration() -> None:
    """Vérifie que post_unlock_the_safety_configuration émet POST /ctrl/safety."""
    transport = _MockTransport(status_code=204)
    client = _make_client(transport)

    resp = await post_unlock_the_safety_configuration(client)

    assert transport.last_request is not None
    assert transport.last_request.method == "POST"
    assert transport.last_request.url.path == "/ctrl/safety"
    assert resp.status_code == 204

@pytest.mark.asyncio
async def test_post_software_sync_acknowledgement() -> None:
    """Vérifie que post_software_sync_acknowledgement émet POST /ctrl/safety."""
    transport = _MockTransport(status_code=204)
    client = _make_client(transport)

    resp = await post_software_sync_acknowledgement(client)

    assert transport.last_request is not None
    assert transport.last_request.method == "POST"
    assert transport.last_request.url.path == "/ctrl/safety"
    assert resp.status_code == 204

@pytest.mark.asyncio
async def test_add_validation_info() -> None:
    """Vérifie que add_validation_info émet POST /ctrl/safety."""
    transport = _MockTransport(status_code=204)
    client = _make_client(transport)

    resp = await add_validation_info(client)

    assert transport.last_request is not None
    assert transport.last_request.method == "POST"
    assert transport.last_request.url.path == "/ctrl/safety"
    assert resp.status_code == 204

@pytest.mark.asyncio
async def test_remove_validation_info() -> None:
    """Vérifie que remove_validation_info émet POST /ctrl/safety."""
    transport = _MockTransport(status_code=204)
    client = _make_client(transport)

    resp = await remove_validation_info(client)

    assert transport.last_request is not None
    assert transport.last_request.method == "POST"
    assert transport.last_request.url.path == "/ctrl/safety"
    assert resp.status_code == 204

@pytest.mark.asyncio
async def test_set_reset_safety_controller() -> None:
    """Vérifie que set_reset_safety_controller émet POST /ctrl/safety."""
    transport = _MockTransport(status_code=204)
    client = _make_client(transport)

    resp = await set_reset_safety_controller(client)

    assert transport.last_request is not None
    assert transport.last_request.method == "POST"
    assert transport.last_request.url.path == "/ctrl/safety"
    assert resp.status_code == 204

@pytest.mark.asyncio
async def test_get_options_resource() -> None:
    """Vérifie que get_options_resource émet GET /ctrl/options/{option to verify}."""
    transport = _MockTransport(status_code=200)
    client = _make_client(transport)

    resp = await get_options_resource(client, "option_to_verify_test")

    assert transport.last_request is not None
    assert transport.last_request.method == "GET"
    assert transport.last_request.url.path == "/ctrl/options/option_to_verify_test"
    assert resp.status_code == 200

@pytest.mark.asyncio
async def test_get_check_robotware_version_compatibility_with_contorller_hardware() -> None:
    """Vérifie que get_check_robotware_version_compatibility_with_contorller_hardware émet GET /ctrl/compatibility/{robotware version}."""
    transport = _MockTransport(status_code=200)
    client = _make_client(transport)

    resp = await get_check_robotware_version_compatibility_with_contorller_hardware(client, "robotware_version_test")

    assert transport.last_request is not None
    assert transport.last_request.method == "GET"
    assert transport.last_request.url.path == "/ctrl/compatibility/robotware_version_test"
    assert resp.status_code == 200

@pytest.mark.asyncio
async def test_get_virtual_time_resources() -> None:
    """Vérifie que get_virtual_time_resources émet GET /ctrl/virtualtime."""
    transport = _MockTransport(status_code=200)
    client = _make_client(transport)

    resp = await get_virtual_time_resources(client)

    assert transport.last_request is not None
    assert transport.last_request.method == "GET"
    assert transport.last_request.url.path == "/ctrl/virtualtime"
    assert resp.status_code == 200

@pytest.mark.asyncio
async def test_get_virtualtime() -> None:
    """Vérifie que get_virtualtime émet GET /ctrl/virtualtime/vttime."""
    transport = _MockTransport(status_code=200)
    client = _make_client(transport)

    resp = await get_virtualtime(client)

    assert transport.last_request is not None
    assert transport.last_request.method == "GET"
    assert transport.last_request.url.path == "/ctrl/virtualtime/vttime"
    assert resp.status_code == 200

@pytest.mark.asyncio
async def test_get_vttimeslice_value() -> None:
    """Vérifie que get_vttimeslice_value émet GET /ctrl/virtualtime/vttimeslice."""
    transport = _MockTransport(status_code=200)
    client = _make_client(transport)

    resp = await get_vttimeslice_value(client)

    assert transport.last_request is not None
    assert transport.last_request.method == "GET"
    assert transport.last_request.url.path == "/ctrl/virtualtime/vttimeslice"
    assert resp.status_code == 200

@pytest.mark.asyncio
async def test_get_actions_on_vttimeslice() -> None:
    """Vérifie que get_actions_on_vttimeslice émet GET /ctrl/virtualtime/vttimeslice."""
    transport = _MockTransport(status_code=200)
    client = _make_client(transport)

    resp = await get_actions_on_vttimeslice(client)

    assert transport.last_request is not None
    assert transport.last_request.method == "GET"
    assert transport.last_request.url.path == "/ctrl/virtualtime/vttimeslice"
    assert resp.status_code == 200

@pytest.mark.asyncio
async def test_set_vttimeslice_value() -> None:
    """Vérifie que set_vttimeslice_value émet POST /ctrl/virtualtime/vttimeslice."""
    transport = _MockTransport(status_code=204)
    client = _make_client(transport)

    resp = await set_vttimeslice_value(client)

    assert transport.last_request is not None
    assert transport.last_request.method == "POST"
    assert transport.last_request.url.path == "/ctrl/virtualtime/vttimeslice"
    assert resp.status_code == 204

@pytest.mark.asyncio
async def test_get_speed_of_virtual_time() -> None:
    """Vérifie que get_speed_of_virtual_time émet GET /ctrl/virtualtime/vtspeed."""
    transport = _MockTransport(status_code=200)
    client = _make_client(transport)

    resp = await get_speed_of_virtual_time(client)

    assert transport.last_request is not None
    assert transport.last_request.method == "GET"
    assert transport.last_request.url.path == "/ctrl/virtualtime/vtspeed"
    assert resp.status_code == 200

@pytest.mark.asyncio
async def test_get_actions_on_vtspeed() -> None:
    """Vérifie que get_actions_on_vtspeed émet GET /ctrl/virtualtime/vtspeed."""
    transport = _MockTransport(status_code=200)
    client = _make_client(transport)

    resp = await get_actions_on_vtspeed(client)

    assert transport.last_request is not None
    assert transport.last_request.method == "GET"
    assert transport.last_request.url.path == "/ctrl/virtualtime/vtspeed"
    assert resp.status_code == 200

@pytest.mark.asyncio
async def test_set_speed_of_virtualtime() -> None:
    """Vérifie que set_speed_of_virtualtime émet POST /ctrl/virtualtime/vtspeed."""
    transport = _MockTransport(status_code=204)
    client = _make_client(transport)

    resp = await set_speed_of_virtualtime(client)

    assert transport.last_request is not None
    assert transport.last_request.method == "POST"
    assert transport.last_request.url.path == "/ctrl/virtualtime/vtspeed"
    assert resp.status_code == 204

@pytest.mark.asyncio
async def test_get_state_of_virtual_time() -> None:
    """Vérifie que get_state_of_virtual_time émet GET /ctrl/virtualtime/vtstate."""
    transport = _MockTransport(status_code=200)
    client = _make_client(transport)

    resp = await get_state_of_virtual_time(client)

    assert transport.last_request is not None
    assert transport.last_request.method == "GET"
    assert transport.last_request.url.path == "/ctrl/virtualtime/vtstate"
    assert resp.status_code == 200

@pytest.mark.asyncio
async def test_get_actions_on_vtstate() -> None:
    """Vérifie que get_actions_on_vtstate émet GET /ctrl/virtualtime/vtstate."""
    transport = _MockTransport(status_code=200)
    client = _make_client(transport)

    resp = await get_actions_on_vtstate(client)

    assert transport.last_request is not None
    assert transport.last_request.method == "GET"
    assert transport.last_request.url.path == "/ctrl/virtualtime/vtstate"
    assert resp.status_code == 200

@pytest.mark.asyncio
async def test_set_state_of_virtualtime() -> None:
    """Vérifie que set_state_of_virtualtime émet POST /ctrl/virtualtime/vtstate."""
    transport = _MockTransport(status_code=204)
    client = _make_client(transport)

    resp = await set_state_of_virtualtime(client)

    assert transport.last_request is not None
    assert transport.last_request.method == "POST"
    assert transport.last_request.url.path == "/ctrl/virtualtime/vtstate"
    assert resp.status_code == 204

@pytest.mark.asyncio
async def test_post_vt_run() -> None:
    """Vérifie que post_vt_run émet POST /ctrl/virtualtime/vtrun."""
    transport = _MockTransport(status_code=204)
    client = _make_client(transport)

    resp = await post_vt_run(client)

    assert transport.last_request is not None
    assert transport.last_request.method == "POST"
    assert transport.last_request.url.path == "/ctrl/virtualtime/vtrun"
    assert resp.status_code == 204
