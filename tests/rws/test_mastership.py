# This file is AUTO-GENERATED by contrib/generator/main.py
# DO NOT EDIT MANUALLY — run the generator to regenerate.
"""Tests unitaires auto-générés pour rws/mastership."""
from __future__ import annotations

import pytest
import httpx

from abb_rws_client._core.client import RWSClient
from abb_rws_client.rws.mastership import (
    get_mastership_resources,
    get_mastership_actions,
    post_mastership_request,
    post_mastership_release,
    post_mastership_subscribe,
    get_mastership_domain,
    get_mastership_domain_actions,
    post_mastership_domain_request,
    post_mastership_domain_release,
    post_mastership_domain_subscribe,
)


class _MockTransport(httpx.AsyncBaseTransport):
    """Transport mock retournant une réponse HTTP configurable."""

    def __init__(self, status_code: int = 200, content: bytes = b"") -> None:
        self.status_code = status_code
        self.content = content
        self.last_request: httpx.Request | None = None

    async def handle_async_request(self, request: httpx.Request) -> httpx.Response:
        self.last_request = request
        return httpx.Response(self.status_code, content=self.content, request=request)


def _make_client(transport: _MockTransport) -> RWSClient:
    """Construit un RWSClient branché sur le transport mock."""
    client = RWSClient.__new__(RWSClient)
    client._http = httpx.AsyncClient(
        transport=transport,
        base_url="http://localhost/",
    )
    client.host = "localhost"
    return client


@pytest.mark.asyncio
async def test_get_mastership_resources() -> None:
    """Vérifie que get_mastership_resources émet GET /rw/mastership."""
    transport = _MockTransport(status_code=200)
    client = _make_client(transport)

    resp = await get_mastership_resources(client)

    assert transport.last_request is not None
    assert transport.last_request.method == "GET"
    assert transport.last_request.url.path == "/rw/mastership"
    assert resp.status_code == 200

@pytest.mark.asyncio
async def test_get_mastership_actions() -> None:
    """Vérifie que get_mastership_actions émet GET /rw/mastership."""
    transport = _MockTransport(status_code=200)
    client = _make_client(transport)

    resp = await get_mastership_actions(client)

    assert transport.last_request is not None
    assert transport.last_request.method == "GET"
    assert transport.last_request.url.path == "/rw/mastership"
    assert resp.status_code == 200

@pytest.mark.asyncio
async def test_post_mastership_request() -> None:
    """Vérifie que post_mastership_request émet POST /rw/mastership."""
    transport = _MockTransport(status_code=204)
    client = _make_client(transport)

    resp = await post_mastership_request(client)

    assert transport.last_request is not None
    assert transport.last_request.method == "POST"
    assert transport.last_request.url.path == "/rw/mastership"
    assert resp.status_code == 204

@pytest.mark.asyncio
async def test_post_mastership_release() -> None:
    """Vérifie que post_mastership_release émet POST /rw/mastership."""
    transport = _MockTransport(status_code=204)
    client = _make_client(transport)

    resp = await post_mastership_release(client)

    assert transport.last_request is not None
    assert transport.last_request.method == "POST"
    assert transport.last_request.url.path == "/rw/mastership"
    assert resp.status_code == 204

@pytest.mark.asyncio
async def test_post_mastership_subscribe() -> None:
    """Vérifie que post_mastership_subscribe émet POST /subscription."""
    transport = _MockTransport(status_code=201)
    client = _make_client(transport)

    resp = await post_mastership_subscribe(client)

    assert transport.last_request is not None
    assert transport.last_request.method == "POST"
    assert transport.last_request.url.path == "/subscription"
    assert resp.status_code == 201

@pytest.mark.asyncio
async def test_get_mastership_domain() -> None:
    """Vérifie que get_mastership_domain émet GET /rw/mastership/{domain-name}."""
    transport = _MockTransport(status_code=200)
    client = _make_client(transport)

    resp = await get_mastership_domain(client, "domain_name_test")

    assert transport.last_request is not None
    assert transport.last_request.method == "GET"
    assert transport.last_request.url.path == "/rw/mastership/domain_name_test"
    assert resp.status_code == 200

@pytest.mark.asyncio
async def test_get_mastership_domain_actions() -> None:
    """Vérifie que get_mastership_domain_actions émet GET /rw/mastership/{domain-name}."""
    transport = _MockTransport(status_code=200)
    client = _make_client(transport)

    resp = await get_mastership_domain_actions(client, "domain_name_test")

    assert transport.last_request is not None
    assert transport.last_request.method == "GET"
    assert transport.last_request.url.path == "/rw/mastership/domain_name_test"
    assert resp.status_code == 200

@pytest.mark.asyncio
async def test_post_mastership_domain_request() -> None:
    """Vérifie que post_mastership_domain_request émet POST /rw/mastership/{domain}."""
    transport = _MockTransport(status_code=204)
    client = _make_client(transport)

    resp = await post_mastership_domain_request(client, "domain_test")

    assert transport.last_request is not None
    assert transport.last_request.method == "POST"
    assert transport.last_request.url.path == "/rw/mastership/domain_test"
    assert resp.status_code == 204

@pytest.mark.asyncio
async def test_post_mastership_domain_release() -> None:
    """Vérifie que post_mastership_domain_release émet POST /rw/mastership/{domain}."""
    transport = _MockTransport(status_code=204)
    client = _make_client(transport)

    resp = await post_mastership_domain_release(client, "domain_test")

    assert transport.last_request is not None
    assert transport.last_request.method == "POST"
    assert transport.last_request.url.path == "/rw/mastership/domain_test"
    assert resp.status_code == 204

@pytest.mark.asyncio
async def test_post_mastership_domain_subscribe() -> None:
    """Vérifie que post_mastership_domain_subscribe émet POST /subscription."""
    transport = _MockTransport(status_code=201)
    client = _make_client(transport)

    resp = await post_mastership_domain_subscribe(client)

    assert transport.last_request is not None
    assert transport.last_request.method == "POST"
    assert transport.last_request.url.path == "/subscription"
    assert resp.status_code == 201
