# This file is AUTO-GENERATED by contrib/generator/main.py
# DO NOT EDIT MANUALLY — run the generator to regenerate.
"""Tests unitaires auto-générés pour rws/fileservice."""
from __future__ import annotations

import pytest
import httpx

from abb_rws_client._core.client import RWSClient
from abb_rws_client.rws.fileservice import (
    get_file_service_resources,
    get_directory_listing_of_resources,
    get_directory_actions,
    create_directory,
    post_rename_directory,
    post_copy_directory,
    delete_directory,
    get_file,
    get_file_actions,
    post_rename_file,
    create_copy_of_file,
    put_upload_file,
    delete_file,
    get_file_meta_data,
)


class _MockTransport(httpx.AsyncBaseTransport):
    """Transport mock retournant une réponse HTTP configurable."""

    def __init__(self, status_code: int = 200, content: bytes = b"") -> None:
        self.status_code = status_code
        self.content = content
        self.last_request: httpx.Request | None = None

    async def handle_async_request(self, request: httpx.Request) -> httpx.Response:
        self.last_request = request
        return httpx.Response(self.status_code, content=self.content, request=request)


def _make_client(transport: _MockTransport) -> RWSClient:
    """Construit un RWSClient branché sur le transport mock."""
    client = RWSClient.__new__(RWSClient)
    client._http = httpx.AsyncClient(
        transport=transport,
        base_url="http://localhost/",
    )
    client.host = "localhost"
    return client


@pytest.mark.asyncio
async def test_get_file_service_resources() -> None:
    """Vérifie que get_file_service_resources émet GET /fileservice."""
    transport = _MockTransport(status_code=200)
    client = _make_client(transport)

    resp = await get_file_service_resources(client)

    assert transport.last_request is not None
    assert transport.last_request.method == "GET"
    assert transport.last_request.url.path == "/fileservice"
    assert resp.status_code == 200

@pytest.mark.asyncio
async def test_get_directory_listing_of_resources() -> None:
    """Vérifie que get_directory_listing_of_resources émet GET /fileservice/{environment_variable|device}/{directory}."""
    transport = _MockTransport(status_code=200)
    client = _make_client(transport)

    resp = await get_directory_listing_of_resources(client, "environment_variable_device_test", "directory_test")

    assert transport.last_request is not None
    assert transport.last_request.method == "GET"
    assert transport.last_request.url.path == "/fileservice/environment_variable_device_test/directory_test"
    assert resp.status_code == 200

@pytest.mark.asyncio
async def test_get_directory_actions() -> None:
    """Vérifie que get_directory_actions émet GET /fileservice/{device}|{directory}."""
    transport = _MockTransport(status_code=200)
    client = _make_client(transport)

    resp = await get_directory_actions(client, "device_test", "directory_test")

    assert transport.last_request is not None
    assert transport.last_request.method == "GET"
    assert transport.last_request.url.path == "/fileservice/device_test|directory_test"
    assert resp.status_code == 200

@pytest.mark.asyncio
async def test_create_directory() -> None:
    """Vérifie que create_directory émet POST /fileservice/{device|environment_variable}/{directory}."""
    transport = _MockTransport(status_code=201)
    client = _make_client(transport)

    resp = await create_directory(client, "device_environment_variable_test", "directory_test")

    assert transport.last_request is not None
    assert transport.last_request.method == "POST"
    assert transport.last_request.url.path == "/fileservice/device_environment_variable_test/directory_test"
    assert resp.status_code == 201

@pytest.mark.asyncio
async def test_post_rename_directory() -> None:
    """Vérifie que post_rename_directory émet POST /fileservice/{device|environment_variable}/{directory}."""
    transport = _MockTransport(status_code=200)
    client = _make_client(transport)

    resp = await post_rename_directory(client, "device_environment_variable_test", "directory_test")

    assert transport.last_request is not None
    assert transport.last_request.method == "POST"
    assert transport.last_request.url.path == "/fileservice/device_environment_variable_test/directory_test"
    assert resp.status_code == 200

@pytest.mark.asyncio
async def test_post_copy_directory() -> None:
    """Vérifie que post_copy_directory émet POST /fileservice/{device|environment_variable}/{directory}."""
    transport = _MockTransport(status_code=204)
    client = _make_client(transport)

    resp = await post_copy_directory(client, "device_environment_variable_test", "directory_test")

    assert transport.last_request is not None
    assert transport.last_request.method == "POST"
    assert transport.last_request.url.path == "/fileservice/device_environment_variable_test/directory_test"
    assert resp.status_code == 204

@pytest.mark.asyncio
async def test_delete_directory() -> None:
    """Vérifie que delete_directory émet DELETE /fileservice/{device|environment_variable}/{directory}."""
    transport = _MockTransport(status_code=204)
    client = _make_client(transport)

    resp = await delete_directory(client, "device_environment_variable_test", "directory_test")

    assert transport.last_request is not None
    assert transport.last_request.method == "DELETE"
    assert transport.last_request.url.path == "/fileservice/device_environment_variable_test/directory_test"
    assert resp.status_code == 204

@pytest.mark.asyncio
async def test_get_file() -> None:
    """Vérifie que get_file émet GET /fileservice/{device}|{directory}/{file}."""
    transport = _MockTransport(status_code=200)
    client = _make_client(transport)

    resp = await get_file(client, "device_test", "directory_test", "file_test")

    assert transport.last_request is not None
    assert transport.last_request.method == "GET"
    assert transport.last_request.url.path == "/fileservice/device_test|directory_test/file_test"
    assert resp.status_code == 200

@pytest.mark.asyncio
async def test_get_file_actions() -> None:
    """Vérifie que get_file_actions émet GET /fileservice/{device}|{directory}/{file}."""
    transport = _MockTransport(status_code=200)
    client = _make_client(transport)

    resp = await get_file_actions(client, "device_test", "directory_test", "file_test")

    assert transport.last_request is not None
    assert transport.last_request.method == "GET"
    assert transport.last_request.url.path == "/fileservice/device_test|directory_test/file_test"
    assert resp.status_code == 200

@pytest.mark.asyncio
async def test_post_rename_file() -> None:
    """Vérifie que post_rename_file émet POST /fileservice/{device|environment_variable|directory}/{file}."""
    transport = _MockTransport(status_code=204)
    client = _make_client(transport)

    resp = await post_rename_file(client, "device_environment_variable_directory_test", "file_test")

    assert transport.last_request is not None
    assert transport.last_request.method == "POST"
    assert transport.last_request.url.path == "/fileservice/device_environment_variable_directory_test/file_test"
    assert resp.status_code == 204

@pytest.mark.asyncio
async def test_create_copy_of_file() -> None:
    """Vérifie que create_copy_of_file émet POST /fileservice/{device|environment_variable}/{filename}."""
    transport = _MockTransport(status_code=204)
    client = _make_client(transport)

    resp = await create_copy_of_file(client, "device_environment_variable_test", "filename_test")

    assert transport.last_request is not None
    assert transport.last_request.method == "POST"
    assert transport.last_request.url.path == "/fileservice/device_environment_variable_test/filename_test"
    assert resp.status_code == 204

@pytest.mark.asyncio
async def test_put_upload_file() -> None:
    """Vérifie que put_upload_file émet PUT /fileservice/{device|environment_variable|directory}/{file}."""
    transport = _MockTransport(status_code=200)
    client = _make_client(transport)

    resp = await put_upload_file(client, "device_environment_variable_directory_test", "file_test")

    assert transport.last_request is not None
    assert transport.last_request.method == "PUT"
    assert transport.last_request.url.path == "/fileservice/device_environment_variable_directory_test/file_test"
    assert resp.status_code == 200

@pytest.mark.asyncio
async def test_delete_file() -> None:
    """Vérifie que delete_file émet DELETE /fileservice/{device|environment_variable|directory}/{file}."""
    transport = _MockTransport(status_code=204)
    client = _make_client(transport)

    resp = await delete_file(client, "device_environment_variable_directory_test", "file_test")

    assert transport.last_request is not None
    assert transport.last_request.method == "DELETE"
    assert transport.last_request.url.path == "/fileservice/device_environment_variable_directory_test/file_test"
    assert resp.status_code == 204

@pytest.mark.asyncio
async def test_get_file_meta_data() -> None:
    """Vérifie que get_file_meta_data émet HEAD /fileservice/{device|environment_variable|directory}/{file}."""
    transport = _MockTransport(status_code=204)
    client = _make_client(transport)

    resp = await get_file_meta_data(client, "device_environment_variable_directory_test", "file_test")

    assert transport.last_request is not None
    assert transport.last_request.method == "HEAD"
    assert transport.last_request.url.path == "/fileservice/device_environment_variable_directory_test/file_test"
    assert resp.status_code == 204
