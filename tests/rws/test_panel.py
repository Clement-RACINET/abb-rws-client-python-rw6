# This file is AUTO-GENERATED by contrib/generator/main.py
# DO NOT EDIT MANUALLY — run the generator to regenerate.
"""Tests unitaires auto-générés pour rws/panel."""
from __future__ import annotations

import pytest
import httpx

from abb_rws_client._core.client import RWSClient
from abb_rws_client.rws.panel import (
    get_panel_resources,
    get_panel_actions,
    set_the_language,
    restart_the_controller,
    get_controller_state,
    get_controller_state_actions,
    set_controller_state,
    subscribe_controller_state,
    get_operation_mode,
    get_operation_mode_actions,
    subscribe_operation_mode,
    post_acknowledgement_for_operation_mode,
    get_operation_mode_lock_status,
    post_lock_operation_mode_selection,
    post_unlock_operation_mode_selection,
    get_speed_ratio,
    get_speed_ratio_actions,
    set_speed_ratio,
    subscribe_speed_ratio,
    get_collision_detection_state,
    get_collision_detection_state_actions,
    subscribe_on_collision_detection_state,
)


class _MockTransport(httpx.AsyncBaseTransport):
    """Transport mock retournant une réponse HTTP configurable."""

    def __init__(self, status_code: int = 200, content: bytes = b"") -> None:
        self.status_code = status_code
        self.content = content
        self.last_request: httpx.Request | None = None

    async def handle_async_request(self, request: httpx.Request) -> httpx.Response:
        self.last_request = request
        return httpx.Response(self.status_code, content=self.content, request=request)


def _make_client(transport: _MockTransport) -> RWSClient:
    """Construit un RWSClient branché sur le transport mock."""
    client = RWSClient.__new__(RWSClient)
    client._http = httpx.AsyncClient(
        transport=transport,
        base_url="http://localhost/",
    )
    client.host = "localhost"
    return client


@pytest.mark.asyncio
async def test_get_panel_resources() -> None:
    """Vérifie que get_panel_resources émet GET /rw/panel."""
    transport = _MockTransport(status_code=200)
    client = _make_client(transport)

    resp = await get_panel_resources(client)

    assert transport.last_request is not None
    assert transport.last_request.method == "GET"
    assert transport.last_request.url.path == "/rw/panel"
    assert resp.status_code == 200

@pytest.mark.asyncio
async def test_get_panel_actions() -> None:
    """Vérifie que get_panel_actions émet GET /rw/panel."""
    transport = _MockTransport(status_code=200)
    client = _make_client(transport)

    resp = await get_panel_actions(client)

    assert transport.last_request is not None
    assert transport.last_request.method == "GET"
    assert transport.last_request.url.path == "/rw/panel"
    assert resp.status_code == 200

@pytest.mark.asyncio
async def test_set_the_language() -> None:
    """Vérifie que set_the_language émet POST /rw/panel."""
    transport = _MockTransport(status_code=204)
    client = _make_client(transport)

    resp = await set_the_language(client)

    assert transport.last_request is not None
    assert transport.last_request.method == "POST"
    assert transport.last_request.url.path == "/rw/panel"
    assert resp.status_code == 204

@pytest.mark.asyncio
async def test_restart_the_controller() -> None:
    """Vérifie que restart_the_controller émet POST /rw/panel."""
    transport = _MockTransport(status_code=204)
    client = _make_client(transport)

    resp = await restart_the_controller(client)

    assert transport.last_request is not None
    assert transport.last_request.method == "POST"
    assert transport.last_request.url.path == "/rw/panel"
    assert resp.status_code == 204

@pytest.mark.asyncio
async def test_get_controller_state() -> None:
    """Vérifie que get_controller_state émet GET /rw/panel/ctrlstate."""
    transport = _MockTransport(status_code=200)
    client = _make_client(transport)

    resp = await get_controller_state(client)

    assert transport.last_request is not None
    assert transport.last_request.method == "GET"
    assert transport.last_request.url.path == "/rw/panel/ctrlstate"
    assert resp.status_code == 200

@pytest.mark.asyncio
async def test_get_controller_state_actions() -> None:
    """Vérifie que get_controller_state_actions émet GET /rw/panel/ctrlstate."""
    transport = _MockTransport(status_code=200)
    client = _make_client(transport)

    resp = await get_controller_state_actions(client)

    assert transport.last_request is not None
    assert transport.last_request.method == "GET"
    assert transport.last_request.url.path == "/rw/panel/ctrlstate"
    assert resp.status_code == 200

@pytest.mark.asyncio
async def test_set_controller_state() -> None:
    """Vérifie que set_controller_state émet POST /rw/panel/ctrlstate."""
    transport = _MockTransport(status_code=204)
    client = _make_client(transport)

    resp = await set_controller_state(client)

    assert transport.last_request is not None
    assert transport.last_request.method == "POST"
    assert transport.last_request.url.path == "/rw/panel/ctrlstate"
    assert resp.status_code == 204

@pytest.mark.asyncio
async def test_subscribe_controller_state() -> None:
    """Vérifie que subscribe_controller_state émet POST /subscription."""
    transport = _MockTransport(status_code=201)
    client = _make_client(transport)

    resp = await subscribe_controller_state(client)

    assert transport.last_request is not None
    assert transport.last_request.method == "POST"
    assert transport.last_request.url.path == "/subscription"
    assert resp.status_code == 201

@pytest.mark.asyncio
async def test_get_operation_mode() -> None:
    """Vérifie que get_operation_mode émet GET /rw/panel/opmode."""
    transport = _MockTransport(status_code=200)
    client = _make_client(transport)

    resp = await get_operation_mode(client)

    assert transport.last_request is not None
    assert transport.last_request.method == "GET"
    assert transport.last_request.url.path == "/rw/panel/opmode"
    assert resp.status_code == 200

@pytest.mark.asyncio
async def test_get_operation_mode_actions() -> None:
    """Vérifie que get_operation_mode_actions émet GET /rw/panel/opmode."""
    transport = _MockTransport(status_code=200)
    client = _make_client(transport)

    resp = await get_operation_mode_actions(client)

    assert transport.last_request is not None
    assert transport.last_request.method == "GET"
    assert transport.last_request.url.path == "/rw/panel/opmode"
    assert resp.status_code == 200

@pytest.mark.asyncio
async def test_subscribe_operation_mode() -> None:
    """Vérifie que subscribe_operation_mode émet POST /subscription."""
    transport = _MockTransport(status_code=201)
    client = _make_client(transport)

    resp = await subscribe_operation_mode(client)

    assert transport.last_request is not None
    assert transport.last_request.method == "POST"
    assert transport.last_request.url.path == "/subscription"
    assert resp.status_code == 201

@pytest.mark.asyncio
async def test_post_acknowledgement_for_operation_mode() -> None:
    """Vérifie que post_acknowledgement_for_operation_mode émet POST /rw/panel/opmode."""
    transport = _MockTransport(status_code=204)
    client = _make_client(transport)

    resp = await post_acknowledgement_for_operation_mode(client)

    assert transport.last_request is not None
    assert transport.last_request.method == "POST"
    assert transport.last_request.url.path == "/rw/panel/opmode"
    assert resp.status_code == 204

@pytest.mark.asyncio
async def test_get_operation_mode_lock_status() -> None:
    """Vérifie que get_operation_mode_lock_status émet GET /rw/panel/opmode."""
    transport = _MockTransport(status_code=200)
    client = _make_client(transport)

    resp = await get_operation_mode_lock_status(client)

    assert transport.last_request is not None
    assert transport.last_request.method == "GET"
    assert transport.last_request.url.path == "/rw/panel/opmode"
    assert resp.status_code == 200

@pytest.mark.asyncio
async def test_post_lock_operation_mode_selection() -> None:
    """Vérifie que post_lock_operation_mode_selection émet POST /rw/panel/opmode."""
    transport = _MockTransport(status_code=204)
    client = _make_client(transport)

    resp = await post_lock_operation_mode_selection(client)

    assert transport.last_request is not None
    assert transport.last_request.method == "POST"
    assert transport.last_request.url.path == "/rw/panel/opmode"
    assert resp.status_code == 204

@pytest.mark.asyncio
async def test_post_unlock_operation_mode_selection() -> None:
    """Vérifie que post_unlock_operation_mode_selection émet POST /rw/panel/opmode."""
    transport = _MockTransport(status_code=204)
    client = _make_client(transport)

    resp = await post_unlock_operation_mode_selection(client)

    assert transport.last_request is not None
    assert transport.last_request.method == "POST"
    assert transport.last_request.url.path == "/rw/panel/opmode"
    assert resp.status_code == 204

@pytest.mark.asyncio
async def test_get_speed_ratio() -> None:
    """Vérifie que get_speed_ratio émet GET /rw/panel/speedratio."""
    transport = _MockTransport(status_code=200)
    client = _make_client(transport)

    resp = await get_speed_ratio(client)

    assert transport.last_request is not None
    assert transport.last_request.method == "GET"
    assert transport.last_request.url.path == "/rw/panel/speedratio"
    assert resp.status_code == 200

@pytest.mark.asyncio
async def test_get_speed_ratio_actions() -> None:
    """Vérifie que get_speed_ratio_actions émet GET /rw/panel/speedratio."""
    transport = _MockTransport(status_code=200)
    client = _make_client(transport)

    resp = await get_speed_ratio_actions(client)

    assert transport.last_request is not None
    assert transport.last_request.method == "GET"
    assert transport.last_request.url.path == "/rw/panel/speedratio"
    assert resp.status_code == 200

@pytest.mark.asyncio
async def test_set_speed_ratio() -> None:
    """Vérifie que set_speed_ratio émet POST /rw/panel/speedratio."""
    transport = _MockTransport(status_code=204)
    client = _make_client(transport)

    resp = await set_speed_ratio(client)

    assert transport.last_request is not None
    assert transport.last_request.method == "POST"
    assert transport.last_request.url.path == "/rw/panel/speedratio"
    assert resp.status_code == 204

@pytest.mark.asyncio
async def test_subscribe_speed_ratio() -> None:
    """Vérifie que subscribe_speed_ratio émet POST /subscription."""
    transport = _MockTransport(status_code=201)
    client = _make_client(transport)

    resp = await subscribe_speed_ratio(client)

    assert transport.last_request is not None
    assert transport.last_request.method == "POST"
    assert transport.last_request.url.path == "/subscription"
    assert resp.status_code == 201

@pytest.mark.asyncio
async def test_get_collision_detection_state() -> None:
    """Vérifie que get_collision_detection_state émet GET /rw/panel/coldetstate."""
    transport = _MockTransport(status_code=200)
    client = _make_client(transport)

    resp = await get_collision_detection_state(client)

    assert transport.last_request is not None
    assert transport.last_request.method == "GET"
    assert transport.last_request.url.path == "/rw/panel/coldetstate"
    assert resp.status_code == 200

@pytest.mark.asyncio
async def test_get_collision_detection_state_actions() -> None:
    """Vérifie que get_collision_detection_state_actions émet GET /rw/panel/coldetstate."""
    transport = _MockTransport(status_code=200)
    client = _make_client(transport)

    resp = await get_collision_detection_state_actions(client)

    assert transport.last_request is not None
    assert transport.last_request.method == "GET"
    assert transport.last_request.url.path == "/rw/panel/coldetstate"
    assert resp.status_code == 200

@pytest.mark.asyncio
async def test_subscribe_on_collision_detection_state() -> None:
    """Vérifie que subscribe_on_collision_detection_state émet POST /subscription."""
    transport = _MockTransport(status_code=201)
    client = _make_client(transport)

    resp = await subscribe_on_collision_detection_state(client)

    assert transport.last_request is not None
    assert transport.last_request.method == "POST"
    assert transport.last_request.url.path == "/subscription"
    assert resp.status_code == 201
