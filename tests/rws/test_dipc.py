# This file is AUTO-GENERATED by contrib/generator/main.py
# DO NOT EDIT MANUALLY — run the generator to regenerate.
"""Tests unitaires auto-générés pour rws/dipc."""
from __future__ import annotations

import pytest
import httpx

from abb_rws_client._core.client import RWSClient
from abb_rws_client.rws.dipc import (
    get_dipc_resources,
    get_dipc_actions,
    create_queue,
    get_dipc_queue,
    get_dipc_queue_actions,
    post_send_message,
    get_read_message,
    delete_dipc_queue,
    subscribe_dipc_queue,
    subscribe_dipc_queue_without_reading_message,
)


class _MockTransport(httpx.AsyncBaseTransport):
    """Transport mock retournant une réponse HTTP configurable."""

    def __init__(self, status_code: int = 200, content: bytes = b"") -> None:
        self.status_code = status_code
        self.content = content
        self.last_request: httpx.Request | None = None

    async def handle_async_request(self, request: httpx.Request) -> httpx.Response:
        self.last_request = request
        return httpx.Response(self.status_code, content=self.content, request=request)


def _make_client(transport: _MockTransport) -> RWSClient:
    """Construit un RWSClient branché sur le transport mock."""
    client = RWSClient.__new__(RWSClient)
    client._http = httpx.AsyncClient(
        transport=transport,
        base_url="http://localhost/",
    )
    client.host = "localhost"
    return client


@pytest.mark.asyncio
async def test_get_dipc_resources() -> None:
    """Vérifie que get_dipc_resources émet GET /rw/dipc."""
    transport = _MockTransport(status_code=200)
    client = _make_client(transport)

    resp = await get_dipc_resources(client)

    assert transport.last_request is not None
    assert transport.last_request.method == "GET"
    assert transport.last_request.url.path == "/rw/dipc"
    assert resp.status_code == 200

@pytest.mark.asyncio
async def test_get_dipc_actions() -> None:
    """Vérifie que get_dipc_actions émet GET /rw/dipc."""
    transport = _MockTransport(status_code=200)
    client = _make_client(transport)

    resp = await get_dipc_actions(client)

    assert transport.last_request is not None
    assert transport.last_request.method == "GET"
    assert transport.last_request.url.path == "/rw/dipc"
    assert resp.status_code == 200

@pytest.mark.asyncio
async def test_create_queue() -> None:
    """Vérifie que create_queue émet POST /rw/dipc."""
    transport = _MockTransport(status_code=201)
    client = _make_client(transport)

    resp = await create_queue(client)

    assert transport.last_request is not None
    assert transport.last_request.method == "POST"
    assert transport.last_request.url.path == "/rw/dipc"
    assert resp.status_code == 201

@pytest.mark.asyncio
async def test_get_dipc_queue() -> None:
    """Vérifie que get_dipc_queue émet GET /rw/dipc/{queue-name}."""
    transport = _MockTransport(status_code=200)
    client = _make_client(transport)

    resp = await get_dipc_queue(client, "queue_name_test")

    assert transport.last_request is not None
    assert transport.last_request.method == "GET"
    assert transport.last_request.url.path == "/rw/dipc/queue_name_test"
    assert resp.status_code == 200

@pytest.mark.asyncio
async def test_get_dipc_queue_actions() -> None:
    """Vérifie que get_dipc_queue_actions émet GET /rw/dipc/{queue-name}."""
    transport = _MockTransport(status_code=200)
    client = _make_client(transport)

    resp = await get_dipc_queue_actions(client, "queue_name_test")

    assert transport.last_request is not None
    assert transport.last_request.method == "GET"
    assert transport.last_request.url.path == "/rw/dipc/queue_name_test"
    assert resp.status_code == 200

@pytest.mark.asyncio
async def test_post_send_message() -> None:
    """Vérifie que post_send_message émet POST /rw/dipc/{queue-name}."""
    transport = _MockTransport(status_code=200)
    client = _make_client(transport)

    resp = await post_send_message(client, "queue_name_test")

    assert transport.last_request is not None
    assert transport.last_request.method == "POST"
    assert transport.last_request.url.path == "/rw/dipc/queue_name_test"
    assert resp.status_code == 200

@pytest.mark.asyncio
async def test_get_read_message() -> None:
    """Vérifie que get_read_message émet GET /rw/dipc/{queue-name}."""
    transport = _MockTransport(status_code=200)
    client = _make_client(transport)

    resp = await get_read_message(client, "queue_name_test")

    assert transport.last_request is not None
    assert transport.last_request.method == "GET"
    assert transport.last_request.url.path == "/rw/dipc/queue_name_test"
    assert resp.status_code == 200

@pytest.mark.asyncio
async def test_delete_dipc_queue() -> None:
    """Vérifie que delete_dipc_queue émet DELETE /rw/dipc/{queue-name}."""
    transport = _MockTransport(status_code=204)
    client = _make_client(transport)

    resp = await delete_dipc_queue(client, "queue_name_test")

    assert transport.last_request is not None
    assert transport.last_request.method == "DELETE"
    assert transport.last_request.url.path == "/rw/dipc/queue_name_test"
    assert resp.status_code == 204

@pytest.mark.asyncio
async def test_subscribe_dipc_queue() -> None:
    """Vérifie que subscribe_dipc_queue émet POST /subscription."""
    transport = _MockTransport(status_code=201)
    client = _make_client(transport)

    resp = await subscribe_dipc_queue(client)

    assert transport.last_request is not None
    assert transport.last_request.method == "POST"
    assert transport.last_request.url.path == "/subscription"
    assert resp.status_code == 201

@pytest.mark.asyncio
async def test_subscribe_dipc_queue_without_reading_message() -> None:
    """Vérifie que subscribe_dipc_queue_without_reading_message émet POST /subscription."""
    transport = _MockTransport(status_code=201)
    client = _make_client(transport)

    resp = await subscribe_dipc_queue_without_reading_message(client)

    assert transport.last_request is not None
    assert transport.last_request.method == "POST"
    assert transport.last_request.url.path == "/subscription"
    assert resp.status_code == 201
